# 旧 evidence 目录清理

- 基线 k8s `29f8465312735895544b36b5475c4520881e9a06`，Luna，开工时干净。
- 精确删除该目录 78 份受跟踪历史文件，未见目录内未跟踪文件；空目录一并清理。
  固定 Git 恢复入口已加入历史索引，指南不再声称该目录仍在工作树。
- 未发现现行代码直接依赖该目录；冻结 turn 原文不变。
  implementation-plan 的未来 `docs/evidence/<task-id>/` 存证约定不改，
  删除历史文件不等于取消未来保存验收证据的义务。
- `doc-gate.py --staged`：2 份活动文档及 21 个 turn 检查通过；
  `--all`：76 份活动文档通过；diff whitespace 检查通过。
- 本轮只修改 k8s 文档与历史材料，无代码改动，未重新运行业务测试。
  未删除数据库、镜像或备份，未改迁移或发布锁，不将旧证据删除解释为运行验收完成。
- 按用户决定只提交本地 luna，不合 master、不推送、不同步或部署。
