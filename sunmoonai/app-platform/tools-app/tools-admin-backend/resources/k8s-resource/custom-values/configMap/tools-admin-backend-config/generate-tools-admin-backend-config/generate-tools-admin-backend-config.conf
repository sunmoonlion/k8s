# ============================================================================
# tools-admin-backend ConfigMap YAML 生成配置
# 字段来源：应用 .env 中的非敏感字段
# ============================================================================

TEMPLATE_FILE="templates/configMap/tools-admin-backend-config.yaml"
OUTPUT_FILE="tools-admin-backend-config-generated.yaml"
ENABLED="true"
RESOURCE_TYPE="configmap"

NAMESPACE="${NAMESPACE:-app-platform-dev}"
ENVIRONMENT="${ENVIRONMENT:-development}"
ENV="${ENV:-dev}"

# ============================================================================
# TODO: 在此添加 ConfigMap 数据字段（非敏感配置）
# 每行格式：KEY="${KEY:-默认值}"
# 示例：
# NODE_ENV="${NODE_ENV:-production}"
# PORT="${PORT:-8000}"
# REDIS_HOST="${REDIS_HOST:-redis.data-platform-dev.svc.cluster.local}"
# CASDOOR_ENDPOINT="${CASDOOR_ENDPOINT:-https://casdoor.sunmoonai.com}"
# ============================================================================
