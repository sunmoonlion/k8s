apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: info-admin-backend-pvc
  namespace: app-platform-dev
  labels:
    app: info-admin-backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
