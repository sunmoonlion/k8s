# ② 互评 · dev-plan-refact（评审方：kimi）

> 评审方身份：`~/worktrees/kimi/k8s`，分支 `dev-plan-refact/kimi`。
> ⚠ **利益冲突声明**：我既是评优方又是候选作者。以下 C 块对五家用同一把尺
> （M1–M4、J1–J7 逐条），我把自己排在第四；排我前面的三家，每家我都给了
> 「比我强在哪」的具体出处。

## A. 自述（关于 kimi 的候选）

**①a 提出了什么**：六阶段主干（定义→设计→派工→执行→验收→吸收归档）+ 三横切
（决定只追加、证据可复跑、按可逆性配重量）。人的动作只留在「定义」与「验收」两端。
文档类型 11 类，由「每阶段产物一类 + 横切各一类 + AI 冷启动再两类」推出；
每类给生命周期表；每道门给机器/纪律归属表，并诚实标出 S1/S2 当前只能靠纪律。
对 `inputs/` 的取舍逐条声明（①a §8），未整体照抄任何一份：六段是对九段的合并
（拆解排期并入派工、评审不独立成段），每处合并给了判据。

**①b 怎么推导**：槽位判据先行（§1 的 11 槽 + P-投影/P-自描/P-轮属三条通用判据），
再逐文档论证内部结构（§5），再拆并代价（§3）与 B2 五条（§4），最后 260 行落点表。
独有的论证动作是对 `agent-dev-guide.md` 的 14 章**逐章给判据再回测**（§5.2），
据此判 §8/§10 两章不成立（P-轮属，应迁 `rounds/`）——这与 opus 的 §8/§9/§10 测量
结论独立同向。

**未验证或后来被推翻的断言（标 ⚠）**：

- ⚠ **「71 锚点 / 40 引用 → 拆的代价 prohibitive」（我 ①b §3.4/§4）是沿用任务书 B2
  的措辞，未独立复测。**opus 的 F-9 实测（本评审已复跑确认，见 C 块 M/J6 行）：
  58 处锚、44 份引用，其中 49 处在归档软判，活跃裸行号锚只有 **2 处**。
  我的成本论证因此被削弱一个量级。⚠ 但我的 Q1 结论（不拆）不只靠成本：
  §6 的论证是「对象/验收是 PRD 的定义部件 + I13 单一写入面」，该论证不受影响。
- ⚠ ①a §5「22 条 ⚠ 自检」转引自 `inputs/readiness.md`，未重跑其 grep。
- ⚠ ①b §3.1 引用点枚举（`grep -n '§8'`）当时未实际执行，写的是「迁移时逐处改指针」。

**故意没写的内容及理由**：

- **没给迁移次序/迁移脚本**：我把全部物理移动（guide §8/§10、handoff 待办）标为
  「⑦ 或其后工作单元」，理由是本轮只定架构（任务书 §1.4/§4.2），且物理移动触发
  T2 不可逆面。代价：相比 opus §7 的五步迁移序，我的方案在「怎么落地」上薄。
- **没给决定索引的实现**：只给了规则（轮次前缀 ID + 双向可失败检查）。
  luna 给了可跑的索引器，这一点我承认落后（见 D 块）。
- **没重排 guide 章内小节**：任务书 §2-三点名的正是章内（§3 由 5→18 节）。
  我做了章级回测并判章级成立，但明确把章内重组留为后续工作——
  与 opus F-10 的登记同判。

## B. 候选集冻结（已自验，逐字节一致）

复跑 `call-②.md` §一 的自验脚本，十份全部与通知表一致（行数与 sha256 前缀逐行对上）：

| 家 | commit | 文件 | 行 | sha256（前 16） |
| --- | --- | --- | --- | --- |
| opus | `4810fa62` | `pipeline.md` | 387 | `bc29ce3619cba262` |
| opus | `4810fa62` | `dev-plan-architecture.md` | 801 | `f8b67fb2eb5ba697` |
| luna | `9a2b999c` | `pipeline.md` | 213 | `781dcc882b9fd80e` |
| luna | `9a2b999c` | `dev-plan-architecture.md` | 733 | `4d4349400d971a6e` |
| kimi | `492458d2` | `pipeline.md` | 181 | `1201d96a81155434` |
| kimi | `492458d2` | `dev-plan-architecture.md` | 569 | `71d337c83d269f10` |
| cursor | `8ed6348b` | `pipeline.md` | 426 | `cb3020aff0593624` |
| cursor | `8ed6348b` | `dev-plan-architecture.md` | 526 | `e6833fa8541a502f` |
| qwen | `a7b0f104` | `pipeline.md` | 173 | `ff32c64fdb1f7006` |
| qwen | `a7b0f104` | `dev-plan-architecture.md` | 465 | `49fbbfccee9fed8a` |

## C. 评优（M1–M4、J1–J7 逐条）

### 机械条

| 判据 | opus | luna | kimi | cursor | qwen |
| --- | --- | --- | --- | --- | --- |
| M1 覆盖 260 节 | ✅ 脚本生成，§8 给复跑命令 | ✅ 260 行带 I0x-NNN ID | ✅ 我自检 42+108+57+4+15+10+9+13+2=260 | ✅ 脚本对照 inventory 生成 | ✅ 分表计数 260 |
| M2 无空落点 | ✅ | ✅ | ✅ | ✅ | ✅ |
| M3 doc-gate | ✅（提交钩过） | ✅ | ✅（全仓 210 份绿） | ✅ | ✅ |
| M4 路径命名 | ✅ | ✅ | ✅ | ✅ | ✅ |

机械条五家全绿——**按任务书自己的警告，这不证明放对了地方**，分辨全在 J 条。

### J1（①a 六项完整性）

五家六项都有答案，差异在密度：**cursor** 最完整——两道门的每个条件带「谁判」列
（G1.1–G1.6 / G2.1–G2.6），且 §6.3 单列「只能靠纪律且必须保持 ⚠」五条；
**opus** 次之（九阶段表自带「判据由谁执行」列 + §3.3 硬规则）；**luna** 的门表
（DEV/G0–G6）含「未通过时往哪回」列，是五家唯一写了回路的；**kimi** 六项齐但
机器/纪律只在门级不在条件级；**qwen** 齐但多处「纪律」无载体说明。

### J2（推导关系）

**opus**：类型逐格从九阶段表读出（§4.1），生命周期由「流程中的位置」推出（§4.3）——
推导链最硬。**cursor**：类型由「缺了它哪个阶段没有过关物」反推，且每类给
「为什么不并进邻居」——反证式推导，同样硬。**luna**：类型由阶段交接问题推出，成立。
**kimi**：阶段产物 + 横切推导，成立但推导规则只有一句话。**qwen**：形式上是
阶段→类型表，但类型集合恰是通行名集合（REQ/TLD/ADR/IMPL），推导的约束感最弱——
不过它没有先立分类，不触 §6.3 第 2 条。

### J3（归属判据先于结果）

**opus**：三问判据 + 默认规则表 + 8 条具名例外，落点表脚本生成、改表先改规则——
最严格。**cursor**：每类给正面条件 + 「通不过这里应去邻居的信号」（双向判据）。
**luna**：按「被哪个门消费、回答什么问题」判。**kimi**：11 槽判据 + 三条 P 规则。
**qwen**：A–I 九条单句判据，且落点表多处 `A/B` 双标（判据判不下去时打了和牌，
如其 §1 表 3.2/4.5/5.3/6.2 行）——**判据的约束力最弱**。

### J4（内部结构论证）

**luna** 最强：14 份目标文档每份给栏目顺序 + 归属判据 + 实际正文单元 ID，
且用内嵌 render 工具对 10 个样例回读正文验证——这是唯一一家证明了
「不是表格造假」的（任务书 F2 的正面解法）。**cursor** 对 guide 的「寄居者」
分析（A–F 六分）是对该文最锐利的一次重审。**opus** 逐文档论证 + F-10 诚实登记
章内未证。**kimi** 章级判据回测，独立判出 §8/§10 不成立。**qwen** 章级一句话
理由，最薄。

### J5（拆并代价）

**opus**：五步迁移序 + 每步门禁 + 引用实测（含自我纠错的 F-12）。**cursor**：
每个「不拆/不并/只命名」都给代价表，含「现在不切 guide」的三行代价——
不写动作的代价也写，最完整。**luna**：拆并代价表 + 「无法解析的现行引用阻断切换」。
**kimi**：三处移动各给引用断点/锚点/检索路径。**qwen**：§10.1–10.4 有代价但
多为定性（「12 节内容需要重建上下文」无引用点枚举）。

### J6（B2 锚点方案）

**cursor** 最可执行：四步（零编辑 → 钉 commit 锚免疫 → anchor-gate 入 G2 →
将来迁移图 + 跳转页 + 40 份引用同步），且指出「行号不应再当稳定 ID」。
**opus**：方案建立在实测上（F-9：**活跃裸锚只有 2 处**——本评审复跑其命令，
输出 44/58/49/2 与其数字完全一致），因此它敢提拆分。**luna**：原位保全 +
逐字节 verify，最保守也最稳。**kimi**：五条规则但成本前提被 F-9 削弱（见 A 块 ⚠）。
**qwen**：「锚点转发文件」一句话方案，未展开执行细节。

### J7（Q1/Q2/Q3/Q6/Q8/Q9）

- **Q1**：luna/cursor/qwen/kimi 均判整份保留（kimi 与 cursor 独立给出同构论证：
  对象/验收是合同的条款种类；cursor 多引一条 I13）。**opus 唯一主张拆**
  （`product/functions/acceptance/contract/design`），依据是其 F-9 实测把拆分成本
  从「71 锚」降到「2 处活跃裸锚」。⚠ 这是本轮最大的事实分叉：B2 的措辞描高了门槛，
  而**实测在 opus 一边**（我已复跑确认）。但拆内核仍触 T2 不可逆面，
  且四家独立的「条款种类同一份合同」论证不依赖成本——裁决时两件事要分开看。
- **Q2**：五家一致（rounds 比 ADR 大，决定是其中一格）。opus/kimi/cursor/qwen 同解：
  决定索引 + 轮次前缀 ID；**luna 直接给了可跑的索引器**（`round:<id>:R<n>` +
  表外更正检索），完成度最高。
- **Q3**：五家一致异议所有者起点（protocol ≠ 实施计划一部分），cursor §7.2 的
  四条举证最干净。**这是五家独立同向的结论，且各家论证路径不同，可信度高。**
- **Q6**：opus 11 类 / luna 按阶段产物 / kimi 11 类 / cursor 12 类 / qwen 10 类，
  每类不可并性论证：opus、cursor 最充分。
- **Q8**：**opus 的 `DEC` 类最优**——从本轮 R1 的真实失败推出「所有者拍板没有落点
  会吃掉拍板」，这是五家唯一用已发生故障论证的答案。cursor 的 `working/traces/`
  与 luna 的 `records/work/` 是同洞的不同补法。qwen 的「HANDBOOK + STATUS」
  基本没答（取证没有家）。
- **Q9**：见 Q2 行。luna 实现领先；opus 的 ADR 全局单调编号 + R# 别名迁移最激进
  但最彻底；kimi/cursor/qwen 的轮次前缀 + 索引是同构的保守解。

### 各家总评

**opus**：最强证据纪律（全部量化断言可复跑，且两家关键数字我已复核为真）、
最真的结构洞见（A/B 两条流水线解释了「为什么内容全堆进 B 线容器」——
protocol 975 行 vs implementation-plan 93 行且清单为空，这个诊断五家独有）、
DEC 类、三问机械判据。⚠ 三个保留：① ①a 骨架取自它自己起草的
`inputs/ai-pipeline.md`（已按 R2 声明并给出增量清单；按 `inputs/README.md` 约束 2，
裁决须写清它好在哪，不能只写「与参考一致」）；② 任务书是它代写的，
「文档类型必须从阶段推导」与其 §4 高度契合，它自己在 §8 邀请所有者审查这点——
**我认为应记录为争议但不构成否决**：它的三问判据别家也能用，判据不偏向它的答案；
③ 拆内核的主张收益未被证明（四家的「不拆」论证同样成立），F-9 只证明成本低了，
不证明拆了更好。

**cursor**：概念模型最干净（单条 Change + 常驻面，T2 是加厚形态——这个统一
解释了 protocol 与日常工作的关系，比我的「横切 3」更贴本仓现实）、门条件粒度最细、
对 guide 的寄居者分析最锐利、物理动作最保守（全标「寄居/只命名」，迁移全走后续
Change，每一步独立过门）。⚠ 弱点：① 有意不读 standard/ai-pipeline（§13/§14），
独立性最强但也意味着它的「规范结构」基本是从本仓现行实践归纳的，外部对照为零——
「提出一套规范流程」的增量最小；② 物理上几乎什么都不动，①b 更接近「标注方案」，
Q8/Q9 都是「只命名不创建」。

**luna**：①b 的工程完成度全场最高（render 工具、正文回读、字节级 verify、
可跑的决定索引器）；保留内核原位；「未通过时往哪回」列独有。⚠ 弱点：
① 复杂度与侵入性最高——新增 `design/`、`delivery/`、`records/` 三套目录、
14 份目标文档，吸收成本最大；② 文档内嵌可执行代码，文档即工具，
长期维护形态存疑；③ ①a 引入 DEV/G0–G6 与 guide 现有 G0–G5 词汇相邻，
虽自我声明区分，仍增加一套门词汇；④ ①a 行文最晦涩。

**kimi（自评）**：中段。独立判出 guide §8/§10 应迁出（与 opus 测量同向、方法不同）；
Q1 的「PRD 定义部件」论证与 cursor 独立同构。弱项已列在 A 块：
成本论证引用失准（被 F-9 削弱）、无工具、迁移路径薄。

**qwen**：机械条全绿、结构完整、最贴通行命名；但判据约束力最弱（多处 A/B 和牌）、
内部结构论证最薄、Q8 基本未答、①a 对 AI 前提的改造主要停在 §0 一张表。
它是「正确答案的安全版」——没有错误，也几乎没有独家论证。

### 排序与基座建议

1. **opus** 2. **cursor** 3. **luna** 4. **kimi** 5. **qwen**

**基座建议：opus**，理由：本轮②主比 ①a，而 ①a 的核心问题是「为什么我们的内容
长成今天这样」——只有它给了可量化的诊断（两条流水线 + 22.7% 轮次产物），
且每个关键数字可复跑、我复核为真。⚠ 若所有者认为其利益冲突权重过高，
**备选基座 cursor**（概念干净、风险最低、冲突最小），把 opus 的诊断与 DEC
作为吸收项并入。无论选谁，**不采纳 opus 的拆内核主张**（见 C 块 Q1 行：
成本降低 ≠ 拆分有收益，四家独立的不拆论证站得住）。

## D. 值得吸收的点（逐条，不论基座是谁）

1. **opus · 两条流水线区分**（其 pipeline §1）：「开发流程」一词在本仓同时指
   产品流水线与决策流水线，B 线成熟 A 线空，内容因此全堆进 B 线容器。
   这是五家唯一对「乱在哪」给出机制性解释的，且可量化复核（698/3082 = 22.7%，已复跑）。
2. **opus · `DEC` 类（所有者决定落点）**（其 §4.4）：从本轮 R1 的真实作废推出，
   直接补 Q8 的洞——「拍板没有落点，于是被降级成待议题」。
3. **opus · F-9 / F-12 及推论**（其 ①b §9）：任务书 B2 的锚点数字描高一个量级
   （实测 44/58/49/2，我已复跑确认）；及「实测命令必须显式排除候选自身产物，
   排除项写进命令」——建议进 `constraints.md`。
4. **opus · 「凡声称机器执行的判据，必须有一条能跑出非平凡结果的命令」**
   （其 §3.3，由 findings F-7 推出）：与我 ①a §5 的诚实边界同向但更可判。
5. **cursor · G1/G2 条件级「谁判」列**（其 §3.1/§3.2）：机器/纪律的归属做到
   条件粒度而不是门粒度，我 ①a §5 只做到门粒度。
6. **cursor · §6.3「只能靠纪律且必须保持 ⚠」清单**：把判不了的原因逐条写出
   （语义、冷启动隔离、语义错链），比标一个 ⚠ 更防自欺。
7. **cursor · 寄居者标记 + 分阶段迁移**（其 ①b §2.2/§3.2）：不当场搬家、
   先类型改判再逐步物理迁出，每步独立过 G1/G2——比我「⑦ 再做」的笼统延期更可执行。
8. **cursor · B2 四步方案**（其 ①b §4）：含「行号不当稳定 ID、迁移图、跳转页、
   引用方同步」四件，是五家最完整的可执行版。
9. **cursor · 「并行轮是同一条 Change 的加厚形态」**（其 §1/§3.3）：比各家
   「protocol 是横切/子例程」的表述都更准确地统一了 T0–T2 与日常开发。
10. **luna · 可跑的决定索引器**（其 ①b §六）：`round:<round-id>:R<n>` 全局键 +
    表外更正检索 + 「未命中只表示没找到」的诚实语义——Q9 从「命名」变成「实现」。
11. **luna · 机器结果字段 schema**（其 pipeline §二）：`gate_id, subject_commit,
    environment, command, expected/observed_scope, exit_code, evidence_ref,
    checked, not_checked`，缺输入记 `UNKNOWN`——把「可复跑证据」落成记录形状。
12. **luna · 「每处阶段拆分都对应一种失败、批准对象或返工入口」**（其 §一末段）：
    阶段拆分的判据表述，比「产物/落点/判据」三件套多一个可检验维度。
13. **luna · render/verify 实跑**（其 ①b §二/§七）：用工具证明落点表不是表格造假，
    直接回应任务书 F2——吸收时不必要它的 14 份目录，但要它的**验证方法**。
14. **kimi · Q1「PRD 定义部件」论证与章级回测法**（我 ①b §6/§5.2）：
    若基座主张拆内核，这条（及 cursor 的 I13 论证）是必须正面回应的反方；
    章级「给判据再回测」可用于裁决后验收吸收方的 guide 重组。
15. **qwen · 稳定落点编码**（其 ①b 各表的 `REQ-05.3` 式 ID）：给每节一个
    与行号无关的地址，与 cursor「行号不当稳定 ID」互补，可进迁移图格式。
16. **qwen · 「渗透节」标注法**（其 §1 归属说明）：不拆文件但逐节标出 TLD 成分，
    是「不拆派」内部最细的诚实标注，可与 cursor 的 C-CONTRACT-* 子类合并采用。
