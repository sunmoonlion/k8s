# Info formal release no longer uses component tags or directory scanning.
# The committed deployment/bundle/release.json locks immutable digests.
INFO_APP_NAMESPACE="app-platform-dev"
INFO_APP_RELEASE="r5-info-formal-001"
