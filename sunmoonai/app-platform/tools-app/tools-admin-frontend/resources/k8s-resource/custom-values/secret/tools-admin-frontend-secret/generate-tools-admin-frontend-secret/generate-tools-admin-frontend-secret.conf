# ============================================================================
# tools-admin-frontend Secret YAML 生成配置
# 注意：此文件包含真实密钥，不提交到 git
# ============================================================================

TEMPLATE_FILE="templates/secret/tools-admin-frontend-secret.yaml"
OUTPUT_FILE="tools-admin-frontend-secret-generated.yaml"
ENABLED="true"
RESOURCE_TYPE="secret"

NAMESPACE="${NAMESPACE:-app-platform-dev}"
ENVIRONMENT="${ENVIRONMENT:-development}"
ENV="${ENV:-dev}"

# ============================================================================
# TODO: 在此填写敏感凭据（真实密码）
# 每行格式：KEY="${KEY:-实际值}"
# 示例：
# DATABASE_URL="${DATABASE_URL:-postgresql://user:password@postgresql.svc:5432/dbname}"
# REDIS_PASSWORD="${REDIS_PASSWORD:-yourredispassword}"
# CASDOOR_CLIENT_SECRET="${CASDOOR_CLIENT_SECRET:-yourclientsecret}"
# ============================================================================
