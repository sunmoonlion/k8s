apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tools-admin-frontend-pvc
  namespace: app-platform-dev
  labels:
    app: tools-admin-frontend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
