# ============================================================================
# Llm Admin Backend ConfigMap YAML 生成配置
# 字段来源：llm-admin-backend/app/core/config.py + .env
# ============================================================================

TEMPLATE_FILE="templates/configMap/llm-admin-backend-config.yaml"
OUTPUT_FILE="llm-admin-backend-config-generated.yaml"
ENABLED="true"
RESOURCE_TYPE="configmap"

# 命名空间和环境
NAMESPACE="${NAMESPACE:-app-platform-dev}"
ENVIRONMENT="${ENVIRONMENT:-development}"
ENV="${ENV:-production}"

# ============================================================================
# ConfigMap 数据内容（非敏感，对应 config.py 字段）
# ============================================================================
# 基础
ENV="${ENV:-production}"
LOG_LEVEL="${LOG_LEVEL:-INFO}"

# Redis（k8s 内部 Service 地址，不是外网 NodePort）
REDIS_HOST="${REDIS_HOST:-redis-sunmoonai-master.data-platform-dev.svc.cluster.local}"
REDIS_PORT="${REDIS_PORT:-6379}"
REDIS_DB="${REDIS_DB:-0}"
REDIS_USER="${REDIS_USER:-llm_admin_backend}"

# Casdoor BFF（非敏感字段）
CASDOOR_ENDPOINT="${CASDOOR_ENDPOINT:-https://casdoor.sunmoonai.com}"
CASDOOR_CLIENT_ID="${CASDOOR_CLIENT_ID:-440db4e5a480c02c370d}"
CASDOOR_REDIRECT_URI="${CASDOOR_REDIRECT_URI:-https://llm-admin-api.sunmoonai.com/api/auth/callback}"
CASDOOR_ORGANIZATION="${CASDOOR_ORGANIZATION:-llm-admin}"
CASDOOR_APPLICATION="${CASDOOR_APPLICATION:-app-llm-admin}"
CASDOOR_VERIFY_SSL="${CASDOOR_VERIFY_SSL:-false}"

# Session
SESSION_TTL_SECONDS="${SESSION_TTL_SECONDS:-3600}"

# 前端跳转地址
FRONTEND_BASE_URL="${FRONTEND_BASE_URL:-https://llm-admin.sunmoonai.com}"

# Celery producer（API 发任务默认队列，需与 celeryworker 一致）
CELERY_QUEUE="${CELERY_QUEUE:-llm.admin.default}"
