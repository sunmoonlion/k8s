# ① 提案 ｜ `dev-plan-refact` 轮（**第二版，重开**）

> 发出 2026-09-08。⚠ **第一次尝试已作废**，理由与处置见 [`rulings.md`](rulings.md) R1。
> 题目与验收标准见 [`dev-plan-refact.md`](dev-plan-refact.md)，工单见 [`round.md`](round.md)。
> ⚠ **本通知与任务书在本环节冻结，不再改动。**
>
> ⚠ **基线不写在本文里。**判据是：**你工作区的 `HEAD` 就是基线**——
> `git -C <你的工作区> rev-parse HEAD`。`round.md` 的 `baseline` 记的是它的父提交
> （一份文件不能包含自己的哈希），两者指向同一份输入。
> 上一版在这里硬写过哈希、重定基线四次都没跟着改，由 `luna` 按 `GO.md` §5 停下来报出。

## 一句话

**先提出一套规范的开发流程结构，再从它推导出文档架构，再把 `dev-plan/` 现有 260 节
逐节融合进去。次序不可颠倒。**

## ⚠ 先读这一段：上一次为什么作废

上一版任务书把所有者已经拍板的映射（`request-lifecycle`≈PRD、当前在做的≈TLD……）
降级成「不预设」的待议题，又在预判失败形态里把**「直接套 PRD/ADR/TLD」写成扣分项**。

于是两份已交候选都只答了「文档怎么分类与安置」，**没有一份答得出「一条需求怎么走完」**。
那是任务书设计出来的结果，不是参赛方的失误。

**两份旧候选作废，不作参考。**枚举 260 节那份苦工改由机器做一次
（[`inventory.md`](inventory.md)），五家共用，所以废稿不造成重复劳动。

## 你是谁、和谁比

`proposers = ["opus", "luna", "kimi", "cursor", "qwen"]`——**五家，含 opus**。
⚠ **裁决方是项目所有者本人**。吸收方（`integrator`）在 ③ 之后由所有者指定。

**身份自证**：候选首行写你的目录名（`~/worktrees/<名>/k8s`）。
⚠ **身份只能来自 worktree 目录名**——产品名、模型名、界面一律不是证据。

## 交什么：**两份，缺一即未交**

都落在**你自己分支**上，路径固定（**不带名字**——候选与主线同路径，分支本身标明作者）：

| # | 路径 | 内容 |
| --- | --- | --- |
| 1 | `sunmoonai/docs/dev-plan/pipeline.md` | **①a**：你提出的规范开发流程结构 |
| 2 | `sunmoonai/docs/dev-plan/dev-plan-architecture.md` | **①b**：260 节逐节安置进 ①a 的落点表与论证 |

⚠ **不要写主线，不要写别人的 worktree，不要写 `~/master/`。**

⚠ **判定对两条路径取 `all()`**：只交 `dev-plan-architecture.md` 会被 `round-status.py`
判为**未交**，不是「交了一半」。这条判据是本轮新加的——上一版恰恰是「缺 ①a 却通过了」。

### ①a 至少要回答（任务书 §2 一）

| 必答 | 说明 |
| --- | --- |
| 阶段 | 每个阶段**产出什么、存在哪、凭什么算过关**。⚠ 缺第三件的阶段不存在，它只是一段时间 |
| 门 | 「可以开工了」和「做完了」分别是什么条件 |
| 文档类型 | ⚠ **必须从阶段推导，不得自己独立立一套** |
| 生命周期 | 每类文档谁能改、能不能被取代、旧版怎么办 |
| 判据归属 | 每个门是**机器执行**还是**靠纪律**。⚠ 混在一张清单里是最常见的自欺 |
| AI 前提 | 开发全部由 AI 承担、人只提需求拍板验收，你的流程要在这个前提下成立 |
| 命名 | PRD / ADR / TLD / SDD 或自造都行。⚠ **用通行名不加分也不扣分**，判的是这一格的规则说清楚没有 |

## 三条最容易翻车的

⚠ **`B13` 只交 ①b 直接否决。**这是上一版两份候选的实际形态，本轮立为一票否决项。

⚠ **`B14` 不得把 `inputs/` 任何一份当既定框架照抄。**那四份**全部由 opus 起草**，
而 opus 是本轮参赛方；它们是参考，**不是基准**。
`standard-pipeline.md` 原来自称「外部基准 / 尺子」，该定性已撤销。
采用其中的划分允许，但要说明为什么它装得下我们的内容。

⚠ **零丢弃是底线，不是交卷条件。**落点表齐全只证明没丢东西，**不证明放对了地方**。
归属判据 / 每份文档内部结构的论证 / 拆并代价，**三者缺一否决**（任务书 §2 三）。

## 只读输入

| 类 | 内容 |
| --- | --- |
| **要安置的** | 任务书 §3.1 的 9 份，5320 行 **260 节**；逐节清单见 [`inventory.md`](inventory.md)（脚本生成，五家共用，**不要手改**） |
| **参考（非基准）** | [`inputs/`](inputs/README.md) 四份 + 一份已取代的旧任务书。⚠ 利益冲突约束见该目录 README |
| **另可读** | `rounds/` 全部 64 份归档、`archive/` 五份历史稿（**只为核对落点，不作规范依据**）、`working/request-baseline/` 15 份 |

⚠ **不得以「我之前查过」作为依据。**只读输入之外的断言一律附可复跑证据或标 ⚠。

## 隔离

⚠ **提案冻结前不得读其他候选。**看到已有答案之后产出的内容属于评审或改进，
**不再是独立候选**。

⚠ **`opus` 的额外约束**：本轮任务书、`inputs/` 四份参考、`inventory.md` 均由 opus 起草，
而 opus 同为参赛方（[`rulings.md`](rulings.md) R2）。
**opus 的候选必须显式声明哪些结论来自 `inputs/`**，不得把自己旧稿的主张当成独立结论重提。

## 交卷

```bash
git add sunmoonai/docs/dev-plan/pipeline.md \
        sunmoonai/docs/dev-plan/dev-plan-architecture.md
git commit -m "① dev-plan-refact 候选（<你的名字>）"
sha256sum sunmoonai/docs/dev-plan/pipeline.md \
          sunmoonai/docs/dev-plan/dev-plan-architecture.md
```

**交卷 = 你自己分支上的可达 commit。**未提交的工作区文件不算候选。
把两份的 `sha256sum` 写进提交信息或候选首部。

## 自查（交卷前跑一次）

```bash
cd ~/master/k8s
python3 sunmoonai/docs/dev-plan/protocol/round-status.py      # 你那一格该变 ✅
python3 sunmoonai/docs/dev-plan/doc-gate.py --all             # 必须全绿
```

## 观察窗

`W` = 已交付各家用时的中位数。逾期记该家 Attempt `FAILED(timeout)`，**Task 不失败**。
