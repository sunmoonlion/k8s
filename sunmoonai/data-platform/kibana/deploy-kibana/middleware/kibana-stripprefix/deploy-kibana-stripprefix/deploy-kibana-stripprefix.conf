# Kibana StripPrefix 中间件部署配置

# 基础配置
KIBANA_PROJECT_ID="sunmoonai"
KIBANA_NAMESPACE="data-platform-dev"
ENVIRONMENT="development"

# 中间件配置文件路径（相对于脚本目录）
KIBANA_STRIPPREFIX_FILE="$(dirname "$(dirname "${BASH_SOURCE[0]}")")/kibana-stripprefix.yaml"
