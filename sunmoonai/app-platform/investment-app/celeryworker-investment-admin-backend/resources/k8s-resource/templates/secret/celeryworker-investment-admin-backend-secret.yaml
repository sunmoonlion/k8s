apiVersion: v1
kind: Secret
metadata:
  name: celeryworker-investment-admin-backend-secret
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: celeryworker-investment-admin-backend
    component: celery-worker
    backend: investment-admin-backend
type: Opaque
stringData:
  CELERY_BROKER_URL: "${CELERY_BROKER_URL}"
  CELERY_RESULT_BACKEND: "${CELERY_RESULT_BACKEND}"
