apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: jenkins-policy-ratelimit
  namespace: cicd-platform-dev
  labels:
    app: cicd-platform-ingress
    component: jenkins-policy-ratelimit
    project: sunmoonai
spec:
  rateLimit:
    average: 30
    burst: 60

