# agent-dev-guide.md 编辑记录（2026-09-14，opus）

architecture 第七节：「预览不等于完成编辑……正式切换仍须逐条核等价」。切换第 2 步
（`11c24924`）只把源节原文按落点表拼进栏目；本次在其上做编辑，**不改任何规则**。
对照方法：`git diff 11c24924 -- sunmoonai/docs/dev-plan/agent-dev-guide.md`，或用
architecture 的 `show <ID>` 取冻结原文。

## 改了什么

| 位置 | 改动 | 理由 |
| --- | --- | --- |
| 文件头 | 换成读者导语：手册回答什么、按什么顺序、不讲什么及去哪找 | 原头是迁入说明，不是给读者的 |
| 六个栏目开头 | CONFIG 的归属判据句换成导语，标明对应 pipeline 哪个阶段 | 判据句是给安置审议看的，读者读不懂 |
| 每节前的源坐标注释 | 删去；文末加「附：各节来源」表（29 个单元，含 1 个无正文章标题） | 可追溯性不丢，正文不被打断 |
| 跨文件引用 | 「路径 + 标题」的纯文本改为 Markdown 链接，节名作链接文字（21 处） | 可点击；节名即新位置的标题 |
| 执行者的共同纪律 ⚠ 句 | 「……采信规则」采信规则与」去重复词 | 切换时 §N→标题造成的重复 |
| 反模式 引言 | 同一链接连写两次改为一次，说明「那里 / 本节」之别 | 同上 |
| Agent 硬约束自检 | 表头「本文结论」→「本手册的做法」 | 语境 |
| 任务条目格式 | 注明这是实施计划的条目格式；加一句它与持久记录模板的分工；「测试层次（见下）」改指 `delivery/verification.md`「测试层次」 | 该节来自 implementation-plan，「见下」指的那节已迁到验证规程 |
| T2 七环节的操作闭环 | 「本节承接历史稿的流程导读……」改为「是 round-protocol 的操作摘要，不一致时以协议为准」 | 跨页内容是投影，必须指回展开处（architecture 第一节） |
| 交付规则·单一权威 | 「架构语义以 development-plan 为准，本文件只描述……」改为以 TLD 与三份 SDD 为准、实施计划只描述……；「规则 T4」→「constraints T4」 | 原句是 implementation-plan 的自称；architecture 已把架构取向从 development-plan 迁到 TLD/SDD（其第二节 development-plan 一行） |
| 删除与迁移门 | 删「本补充版的边界：」前缀与「本次不修改这些外部文件」 | 只在当时那一版成立；规则本身保留 |

## 没改什么

- 所有规则、表格、代码块、⚠ 限制与例外，逐字未动；各节先后顺序按 architecture。
- 「通知、取件与人的检视面」仍写「开临时检视 worktree」：改为读轮目录归档是遗留事项第 7 项，
  待所有者拍板，编辑不代改。
- T2 摘要与现行协议是否逐条一致未核（例如现行协议已有 ④b）；已标「以协议为准」，
  逐条对齐应随遗留事项第 7–10 项的协议修订一起做。
