# Architecture v2 Info no longer uses component tags or directory scanning.
# The committed architecture-v2/bundle/release.json locks immutable digests.
INFO_APP_NAMESPACE="app-platform-dev"
INFO_APP_RELEASE="r5-info-formal-001"
