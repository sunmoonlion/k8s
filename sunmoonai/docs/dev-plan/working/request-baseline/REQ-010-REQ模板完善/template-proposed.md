# REQ-XXX：<短名>

<!-- 本文件是 TEMPLATE.md 的替换提案，源自 REQ-010，待 REQ-010 D1 拍板后落到共享分支。
     行文风格与原模板一致：注释即填写指引，正文只留骨架。 -->

> 状态：PROPOSED ｜ 提出日期：YYYY-MM-DD ｜ 评审日期：—

<!-- 命名：文件夹 REQ-<三位序号>-<短名>，序号全局递增不复用、短名不含日期/状态，
     详见 AGENTS.md §5 命名规范。
     序号无自动递增机制：创建时由创建者（通常是接请求的助手；手动建同规）
     看本目录现有最大序号 +1 填写；提出日期填创建当天，评审日期评审后补
     （之前保持 —）。
     一个 REQ 文件夹 = 一次开发任务请求的闭环（提议→审核→定任务→实施→评估→按需更新 baseline，
     不必每步都走）：request.md（本文件）+ 按需派生 baseline.md / plan-*.md / handoff.md。 -->

<!-- 全局约定，不在正文重复：
     1. 事实性断言须可落到 file:line 或可重跑的命令。不确定的写「未核实」，不省略。
     2. 引用决策点一律用 ID：`REQ-006 D7`，不带章节号——章节会移动，ID 不会。
     3. 目标态事实写进 baseline.md 或任务文件，本文件只留引用，不复述。
     4. 任何一栏不适用时，显式写「不适用」及理由。空栏无法与「忘了填」区分。 -->

## ① 原始需求

<!-- 用户原话，一字不改，自相矛盾之处也照留。这是意图证据，任何阶段不得改写。
     后续评审若与 ① 产生张力，在 ② 里记录该张力，不回头修 ①。 -->

## ② 架构评审

<!-- 结论四选一：采纳 / 修改后采纳 / 不采纳 / 待澄清。
     理由须引用 baseline 相关文档的 §编号；若与现有架构冲突，
     必须摆出「按 request 做的代价 vs 维持现状的代价」两个选项，由用户拍板。 -->

- 结论：
- 理由：
- 边界：
- 冲突与代价对比（如有）：

<!-- 边界四问都要答，缺它是 REQ 互相渗透的主因：
     含什么 / 不含什么 / 不含的那部分归谁 / 与 ① 的张力（如有）。
     「不含 X」后面必须紧跟 X 的归宿，否则会产生无人认领的僵尸议题。 -->

<!-- 改判时不覆写原文。追加「YYYY-MM-DD 修正：<一句话结论>」小节，写清触发、
     原判错在哪、新判是什么；被作废的决策点标注作废，不删除。
     理由：作废的判断本身是证据，它记录了当时的信息状态。 -->

## ③ 决策点

<!-- 每个取舍一行，无论已定或待定。ID 一旦分配不复用、不重排——Dn 是跨文件引用的锚点。
     「决策」列写结论 + 理由，且理由须能独立成立，不依赖读者已读过上文。
     范例（REQ-005 D10）：
       | D10 | 技能与校验器的绑定强度 | 建议配对 / 无校验器的技能不得入库 |
         **不得入库**。技能在构造上不被强制执行，无校验器则质量无法度量，等于装饰品 |
     反例：「**不得入库**。理由见上文讨论」——无法被别处引用，等于没写。 -->

| 编号 | 决策 | 备选 | 决策与理由 |
| --- | --- | --- | --- |
| D1 |  |  |  |

## ④ 落地去向

<!-- 提议阶段填计划产出；实施后改为实际去向：本文件夹 baseline.md §X /
     某仓的代码位置 / 新 ADR / baseline/ 的哪份文件。
     「本 REQ 不产生代码」也要显式写出。
     不采纳的写：无产出，保留本记录以备条件变化后重新评审。 -->

## ⑤ 验收标准

<!-- 每条须可由第三方判定，不含「合理」「清晰」「完善」一类不可判定词。
     无此节者不得进入 IMPLEMENTED——「做完了」将不可证伪。
     决策型 REQ 同样要填，其验收对象是决策的完备性（例如「③ 中无待定项」）。 -->

1.

## ⑥ 依赖与关联

<!-- 取代关系须双向登记。只登记一侧会留下僵尸 REQ：它既未被拒，也无人认为它还活着
     （实例见 REQ-001）。 -->

| 关系 | 对象 | 说明 |
| --- | --- | --- |
| 前置 |  | 未完成则本 REQ 无法开工 |
| 取代 / 被取代 |  | 双向登记 |
| 引用其决策 |  | 只引用，不重复做决定 |

## ⑦ 状态流转

<!-- PROPOSED → REVIEWED → ADOPTED/REJECTED → IMPLEMENTED
     每次实质改动追一行。「说明」列写改判依据，而非改了什么——
     改了什么 diff 里有，为什么改只有这里有。 -->

| 日期 | 状态 | 说明 |
| --- | --- | --- |
| YYYY-MM-DD | PROPOSED | 用户提出 |
