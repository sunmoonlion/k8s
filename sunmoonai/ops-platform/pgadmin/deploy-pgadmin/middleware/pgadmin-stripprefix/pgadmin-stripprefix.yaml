apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pgadmin-stripprefix
  namespace: ops-platform-dev
  labels:
    app: ops-platform-ingress
    component: pgadmin-stripprefix
    project: sunmoonai
spec:
  stripPrefix:
    prefixes:
      - "/pgadmin"

