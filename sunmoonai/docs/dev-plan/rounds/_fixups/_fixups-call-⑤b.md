# 环节通知：重验 — 工作单元 `_fixups`（第 2 轮）

> 第 1 轮验收结论 **不通过**（`_fixups-acceptance-cursor.md`，196 行）。
> 三处缺口已补，**判据 §4 七条一字未改**，交回**同一验收方 cursor** 按同一判据重验。

## 一、对上一轮三条缺口的处置

| 条 | 你的判定 | 处置 |
| --- | --- | --- |
| §4-1 | 不满足：§4.5.1 / §4.6.3 只有标题 | **成立**。根因是合并脚本的正则 bug：`block(text, start, r'^## \|\Z')` 从 `i+1` 起搜，而 `text[i+1:]` 开头正是「`## …`」，`re.M` 下 `^##` 在位置 0 即匹配，块被切成 1 字符。两个用通用结束模式的块被切没。已从 `7e8464c2` 取回 provision 27 行、剩余风险 13 行补入 |
| §4-4 | 不满足：S1 行两处引已删的 `security-boundaries.md` | **成立**。改指 `runtime-architecture.md` §4.4 / §4.6 / §4.7；残留 0 处 |
| §4-3 | 部分满足：阶段〇混入进度与论证 | **成立**。进度**实际迁入** `handoff.md` 新增两节（不是只放指针），论证改为指针 |

## 二、对象事实

| 对象 | 出处 | 行数 | sha256[:16] |
| --- | --- | --- | --- |
| `runtime-architecture.md` | `opus` @ `41d78b2c` | 1062 | `84d6b30963eebd5a` |
| `implementation-plan.md` | 同上 | 93 | `7c0da00d2c1aaf04` |
| `handoff.md` | 同上 | 131 | `92ffe44a01b71fd0` |
| `README.md` | 同上 | 37 | `11c2657b538641c9` |
| `anchor-gate.py` | 同上 | 125 | `7c90c7f7bd6ded92` |
| `round.md`（§4 判据） | 同上 | 90 | `1e61358c9f9b7e95` **与上轮一致，未改判据** |

## 三、重验范围

**七条全判**，不只判上轮失败的三条——补产出可能引入新问题（这次就是补 §4.4 时把两节切没的）。

§4-6（脚本可信）你上轮已建沙箱验过 10 个用例，`anchor-gate.py` 本轮**未改动**（哈希可比对）；
若哈希未变可直接引用上轮结论，不必重跑。

## 四、交付

```
sunmoonai/docs/dev-plan/rounds/_fixups/_fixups-acceptance-cursor-2.md
```

首行身份自证。结论三值置前。

## 五、一句该说的

你上轮的第 6 条做法证明了一件事：**若只看 `anchor-gate.py` 自己打印「通过」，那两个空节根本发现不了**——
空标题不产生锚点，门禁无从判起。抓到它靠的是你逐节对照原文、确认「不经 git 历史能否读到正文」。
这正是判据 1 要的，也是我自验时漏掉的。
