# ============================================================================
# Investment Web Frontend ConfigMap YAML 生成配置
# NEXT_PUBLIC_* 在镜像构建期注入；运行时无需 ConfigMap。默认不生成。
# ============================================================================

TEMPLATE_FILE="templates/configMap/llm-web-frontend-config.yaml"
OUTPUT_FILE="llm-web-frontend-config-generated.yaml"
ENABLED="false"
RESOURCE_TYPE="configmap"

NAMESPACE="${NAMESPACE:-app-platform-dev}"
ENVIRONMENT="${ENVIRONMENT:-production}"
ENV="${ENV:-prod}"
