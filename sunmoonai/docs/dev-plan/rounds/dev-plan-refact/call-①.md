# ① 提案 ｜ `dev-plan-refact` 轮

> 发出 2026-09-07。**基线 `69234636`。**
> 题目与验收标准见 [`dev-plan-refact.md`](dev-plan-refact.md)，工单见 [`round.md`](round.md)。
> ⚠ **本通知与任务书在本环节冻结，不再改动。**

## 一句话

**为 `dev-plan/` 提出一套组织架构，并把其下全部文档重构进去——零丢弃是底线，有机融合是题目。**

## 你是谁、和谁比

`proposers = ["opus", "luna", "kimi", "cursor", "qwen"]`——**五家，含 opus**。
⚠ **裁决方是项目所有者本人**，不是 opus。吸收方（`integrator`）在 ③ 之后由所有者指定。

**身份自证**：候选首行写你的目录名（`~/worktrees/<名>/k8s`）。
⚠ **身份只能来自 worktree 目录名**——产品名、模型名、界面一律不是证据。

## 交什么

一份候选，落在**你自己分支**上，路径 `sunmoonai/docs/dev-plan/dev-plan-architecture.md`。
⚠ **不要写主线，不要写别人的 worktree，不要写 `~/master/`。**

四部分，缺一不可（详见任务书 §2）：

| | 内容 |
| --- | --- |
| **一** | 补完 `archive/agent-dev-refact.md` 的 76 节落点（`B8`：验到正文级） |
| **二** | **先**提出组织架构：有哪几类、各装什么、彼此什么关系、新文档进来怎么判归属 |
| **三** | **有机融合**：每一节凭什么在那一节。⚠ 这是本轮重心 |
| **四** | 把 §3 只读输入的 **10 份文档 320 节**逐节安置，零丢弃 |

## 三条最容易翻车的

⚠ **`B11` 零丢弃是底线，不是交卷条件。**落点表齐全只证明没丢东西，**不证明放对了地方**。
归属判据 / 每份文档内部结构的论证 / 拆并代价，**三者缺一否决**。

⚠ **`B13` 候选必须自证。**裁决方是所有者，**不会逐节读 320 节**。
落点表骨架要脚本生成；每条归属判据要给出「随便挑一节，怎么验它放对了」的方法；
**争议点自己标出来**——藏起来的争议点裁决方发现不了，但验收方会发现，那时代价更大。

⚠ **`B10` 不得拿导读表充当架构。**`agent-dev-guide.md` 已 2388 行，
加一张「按角色读哪几节」的表是**症状缓解**；本轮要的是**为什么这些内容在这一份、那些不在**。

## 只读输入

见任务书 §3 的 10 份清单（6093 行 320 节）。另可读 `rounds/` 全部归档、
`archive/` 四份历史稿（**只为核对落点，不作规范依据**）。

⚠ **`B14`：不得以「我之前查过」作为依据。**本轮只读输入之外的断言，
一律附可复跑证据或标 ⚠。

## 隔离

⚠ **提案冻结前不得读其他候选，也不得读发起方倾向。**
看到已有答案之后产出的内容属于评审或改进，**不再是独立候选**。

⚠ **`opus` 的额外约束**（`round.md` 三）：opus 兼任传输通道，
**必须先冻结自己的候选并公布 sha256，再执行任何取件动作**——顺序由 commit 时间机械核。

## 交卷

```
git add sunmoonai/docs/dev-plan/dev-plan-architecture.md
git commit -m "① dev-plan-refact 候选（<你的名字>）"
```

**交卷 = 你自己分支上的可达 commit。**未提交的工作区文件不算候选。
提交后把 `sha256sum` 写在提交信息或候选首部。

## 观察窗

`W` = 已交付各家用时的中位数。逾期记该家 Attempt `FAILED(timeout)`，**Task 不失败**。
