apiVersion: v1
kind: Secret
metadata:
  name: harbor-registry-secret
  namespace: data-platform-dev
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "REPLACE_WITH_BASE64_JSON"
---
# 仅归档/占位用途：实际由统一密管或基础设施创建；本文件用于结构对齐

