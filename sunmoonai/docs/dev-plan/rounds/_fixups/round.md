# 工作单元 `_fixups`：runtime 轮发布后的四次改动，追认与独立验收

> 起草：opus（改动的执行者）｜2026-09-05
> **本文件在任何验收动作之前落盘。验收判据（§4）此刻冻结。**

```toml
round_id  = "_fixups"
status    = "DONE"             # 第 2 轮独立验收通过（cursor，七条全满足，无条件）
tier      = "bootstrap"
final_path = "sunmoonai/docs/dev-plan/runtime-architecture.md"
round_dir  = "sunmoonai/docs/dev-plan/rounds/_fixups"
prefix     = "_fixups"
baseline   = "dc5e6127"
proposers  = []
arbiter        = ""
acceptor       = "cursor"    # 所有者 2026-09-05 指派；非 opus，符合 §1.3 第 2 条
```

## 1. 事由：一次协议违反，由所有者指出

`runtime` 轮 ⑦ 发布后（`dc5e6127`），执行者 opus 对已发布产物做了**四次**改动，
**没有开工作单元、没有先冻判据、没有独立验收方**。所有者 2026-09-05 指出：
「你改冻结文件按程序不是应该走 T0 吗」。

对照 `round-protocol.md @ runtime/protocol` §1.3「任何档位都不能省的三条」：

| 条 | 四次改动的实际情况 |
| --- | --- |
| 1 **验收判据先于产出冻结** | **0/4 守住**。每次都是做完之后才陈述判据（「裸路径引用为 0」「零缺口」「门禁全绿」）——**做完再定标准等于自证** |
| 2 **产出方 ≠ 验收方** | **0/4 守住**。四次全部由 opus 自做自验：自己跑 `doc-gate` / `anchor-gate`，自己宣布通过 |
| 3 **不可逆动作由人确认** | ✅ **4/4 守住**。每次写主线都是所有者执行 `publish-*.sh`，opus 未直接写主线 |

**裁定留痕也缺**：§1.1 要求「裁定要留痕，T0 不必建目录，但把裁定记在任务条目里」。
第一次改动只在对话里说了「档位 T0」（而「对话里说的不算」是本项目自己的规矩），后三次无裁定。

## 2. 被追认的四次改动

| # | commit（master） | 内容 |
| --- | --- | --- |
| C1 | `db5228ba` | 最终稿对 `refact-fable.md` 的引用全部改钉 commit；§7「与 §8 处置的对齐」移出到 `rounds/runtime/`；删除 `refact-fable.md`；新增 `anchor-gate.py` |
| C2 | （随 C3 发布） | 恢复 `refact-fable.md` 并加「被推翻 / 仍有效 / 删除条件」抬头（E-12：删早了，只验了可复核性没验可读性） |
| C3 | （随 C4 发布） | 把仍有效内容迁出为 `security-boundaries.md` / `roadmap.md`，再删 `refact-fable.md` |
| C4 | 待发布 | 按 `README.md` 声明的分工，把上述两份并回 `runtime-architecture.md` 与 `implementation-plan.md`，删掉那两份 |

C2–C4 三次连续返工，每次都由所有者指出方向错误（依赖方向 → 修法 → 结构）。

## 3. 档位裁定：`bootstrap`，并登记判据本身的问题

裁定 **`bootstrap`**，理由与 `refact-fable.md @ 7e8464c2` §6 给 R0 / S1 的相同：
`T0` 的定义要求「验收条引用已签策略里的任务类包」，而策略首版是 R1 的产物，**尚不存在**，
故本单元的验收条只能是 §4 自列，效力来自本文落盘 + 所有者确认。

⚠ **同时登记一个判据本身的问题（`C4` 开放问题的第五个实例）**：
按 §1.1，`runtime-architecture.md` 属**权威层**（它是 S1 与后续文档的依据），
**命中任一条即 T2**。严格照判据，改一处锚点形式也要开七环节完整轮——这显然不成比例。
`C4`「档位判据是否偏紧：权威层几乎覆盖 `dev-plan/` 全部文件」早已登记、一直未解决，
本单元的四次改动是它的第 1–4 个实例。**本单元不解决 `C4`，只登记。**

## 4. 验收判据（此刻冻结，验收方按此逐条判）

1. **内容零缺口**：`refact-fable.md @ 7e8464c2` 中「仍有效」的 11 节，在活跃文档里逐节可寻，
   且**不经 git 历史**即可读到。逐节给出新落点的 `文件:节号`。
2. **无第二真源**：`security-boundaries.md`、`roadmap.md`、`refact-fable.md` 三者均已不在
   `dev-plan/` 顶层；同一内容不在两处并存。
3. **分工合规**：新增内容的落点符合 `README.md`「三份文档的分工，别混写」表——
   架构论证不进 `implementation-plan.md`，任务本体不进架构文档。
4. **引用形态**：活跃文档中对 `refact-fable.md` 的**证据性**引用一律钉 `@ 7e8464c2`；
   **规范性**引用一律指向新落点；裸路径行号锚为 0。
5. **门禁**：`doc-gate.py --all` 与 `anchor-gate.py` 均退出 0。
6. **`anchor-gate.py` 自身可信**：验收方须**独立复跑**它对至少 3 个已知悬空锚与 3 个已知有效锚的判定，
   确认不误报也不漏报——**不得只看它自己打印「通过」**（本轮记录的「判据给假答案」已发生六次）。
7. **未动冻结物**：`rounds/runtime/task.md` 相对 `runtime/h1` 逐字节一致。

**判据 6 是本单元的重点**：`anchor-gate.py` 是 opus 写的、用来验证 opus 的改动，
若不独立复核，等于换个形式的自证。

## 5. 角色

| 角色 | 谁 |
| --- | --- |
| 执行者 | opus（改动已做完，这是追认） |
| **独立验收方** | **cursor**（所有者 2026-09-05 指派） |
| 确认 | 项目所有者 |

## 6. 所有者要裁的两件

1. **是否接受追认**。追认已发生的改动是**朝省事方向**（把「没走流程」变成「事后补流程」），
   按方向不对称须人确认。不接受的话，处置是回退 C1–C4 并重做——
   `runtime-architecture.md` 有 `dc5e6127` 的版本可回。
2. **指派验收方**。
