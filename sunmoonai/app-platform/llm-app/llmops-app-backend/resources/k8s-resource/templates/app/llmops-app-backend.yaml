apiVersion: apps/v1
kind: Deployment
metadata:
  name: llmops-app-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: llmops-app-backend
spec:
  replicas: ${REPLICAS:-1}
  selector:
    matchLabels:
      app: llmops-app-backend
  template:
    metadata:
      labels:
        app: llmops-app-backend
    spec:
      imagePullSecrets:
      - name: ${LLMOPS_BFF_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: llmops-app-backend
        # Harbor 镜像路径：仓库/项目/镜像名:标签
        # 由 deploy-llmops-backend.sh 计算并导出 LLMOPS_BFF_FULL_IMAGE_NAME
        image: ${LLMOPS_BFF_FULL_IMAGE_NAME}
        # Always: 总是从 Harbor 拉取最新版本，避免使用节点本地的旧镜像
        # 注意：如果使用 IfNotPresent，节点本地有旧镜像时不会从 Harbor 拉取，导致使用旧版本
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: ${LLMOPS_BFF_CONFIGMAP_NAME:-llmops-service-config}
        - secretRef:
            name: ${LLMOPS_BFF_SECRET_NAME:-llmops-service-secret}
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data-storage
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
          subPath: ${PVC_SUB_PATH:-}
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-llmops-app-backend-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: llmops-app-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: llmops-app-backend
spec:
  selector:
    app: llmops-app-backend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP


