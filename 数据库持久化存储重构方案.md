# 数据库持久化存储重构方案

> **状态**：待实施（集群重置时同步执行）  
> **背景**：当前 dev 环境使用 `nfs-1`、`nfs-2`、`nfs-3` 共 3 个 StorageClass（分别对应 3 台节点的 NFS 服务器），在 kind 本地环境下存在兼容性和性能问题，需重构为统一方案。

---

## 一、当前问题

### 1.1 NFS 用于数据库存储的缺陷

| 问题 | 说明 |
|------|------|
| **性能差** | 每次 I/O 经过网络，数据库依赖的 fsync 性能急剧下降 |
| **一致性风险** | NFS 缓存一致性弱，网络抖动时可能导致数据不一致 |
| **文件锁不可靠** | NFS 的 POSIX 文件锁实现不完整，PostgreSQL/MongoDB 等依赖文件锁的数据库存在风险 |
| **kind 不兼容** | kind 节点镜像不含 NFS 客户端（`nfs-common`/`rpcbind`），Pod 挂载直接失败 |

### 1.2 kind 环境额外问题

- kind 节点本身是 Docker 容器，NFS 挂载需穿透「宿主机 → Docker容器（kind节点）→ Pod」三层，路径映射复杂，权限问题频发
- 即使强行挂载成功，性能和稳定性也无法满足数据库需求

### 1.3 当前现状（使用 nfs-1/nfs-2/nfs-3 的 dev-values 文件）

| 组件 | 文件 |
|------|------|
| Redis | `data-platform/redis/resources/custom-values/dev-values.yaml` |
| PostgreSQL | `data-platform/postgresql/resources/custom-values/dev-values.yaml` |
| MongoDB | `data-platform/mongodb/resources/custom-values/dev-values.yaml` |
| Neo4j | `data-platform/neo4j/resources/custom-values/dev-values.yaml` |
| Elasticsearch | `data-platform/elasticsearch/resources/custom-values/dev-values.yaml` |
| RabbitMQ | `messaging-platform/rabbitmq/resources/custom-values/dev-values.yaml` |
| Harbor | `cicd-platform/harbor/resources/custom-values/dev-values.yaml` |
| Jenkins | `cicd-platform/jenkins/resources/custom-values/dev-values.yaml` |
| Casdoor | `app-platform/casdoor-app/casdoor/resources/custom-values/dev-values.yaml` |
| pgAdmin | `ops-platform/pgadmin/resources/custom-values/dev-values.yaml` |
| Traefik | `ingress-platform/traefik/resources/custom-values/dev-values.yaml` |
| 以上对应的 `dev-values-persist.yaml` | Redis / PostgreSQL / MongoDB / Neo4j / Elasticsearch / RabbitMQ |

---

## 二、设计目标

1. **kind 和远程云服务器使用同一份 dev-values.yaml**，不需要因环境不同而修改代码
2. **数据库存储稳定可靠**，fsync 语义正确，无 NFS 的一致性风险
3. **迁移成本低**，尽量少改配置文件
4. 为将来生产环境高可用存储（Longhorn 等）预留扩展空间

---

## 三、推荐方案：两端统一使用 `local-path-provisioner`

### 3.1 方案概述

```
kind（本地开发）                    远程云服务器（C1/C2）
       ↓                                    ↓
local-path-provisioner              local-path-provisioner
       ↓                                    ↓
StorageClass: "local-path"         StorageClass: "local-path"
       ↓                                    ↓
          dev-values.yaml 完全一致，无需修改
```

- `local-path-provisioner` 是 Rancher 开源的轻量级动态存储方案
- **kind 内置支持**，安装后 StorageClass 名为 `local-path`
- **普通 k8s 集群同样支持**，一行命令安装，行为与 kind 内完全一致
- 数据存储在节点本地磁盘（默认 `/opt/local-path-provisioner/`），性能接近裸盘

### 3.2 与 NFS 方案对比

| 对比项 | NFS（当前） | local-path-provisioner（目标） |
|--------|------------|-------------------------------|
| kind 兼容性 | ❌ 不兼容 | ✅ 原生支持 |
| 数据库 fsync | ❌ 性能差/语义不正确 | ✅ 本地磁盘，完全正确 |
| 文件锁 | ❌ 实现不完整 | ✅ 标准 POSIX |
| kind/远程代码一致性 | ❌ 需要两套配置 | ✅ 同一份配置 |
| 安装复杂度 | 需要独立 NFS 服务器 | 一行 kubectl apply |
| HA 能力 | 依赖 NFS 服务器 | 单节点（开发够用） |

### 3.3 为何不需要创建 3 个 local-path StorageClass？

原来需要 3 个 NFS StorageClass（`nfs-1`/`nfs-2`/`nfs-3`）是因为 NFS 的 SC 与服务器**强绑定**，要把数据分散到 3 台节点，必须创建 3 个 SC 并在 values.yaml 中显式指定：

```
nfs-1 → NFS服务器节点1 → 数据固定落到节点1
nfs-2 → NFS服务器节点2 → 数据固定落到节点2
nfs-3 → NFS服务器节点3 → 数据固定落到节点3
```

`local-path-provisioner` 的机制完全不同——它使用 **`WaitForFirstConsumer`** 模式，PV 不预先创建，而是等 Pod 被调度到某个节点后，自动在该节点本地创建 PV：

```
1 个 local-path SC
       ↓
Pod 调度到节点2 → 自动在节点2创建 PV → 数据落到节点2
Pod 调度到节点3 → 自动在节点3创建 PV → 数据落到节点3
```

数据分散到哪个节点，由 **Pod 调度 + podAntiAffinity** 决定（见第七节），而不是由 StorageClass 决定。

| 对比项 | NFS（3个SC） | local-path（1个SC）|
|--------|-------------|-------------------|
| 数据分散控制者 | StorageClass 显式指定 | Pod调度自动决定 |
| 新增节点时 | 需新建 SC | 无需改动，自动感知 |
| kind 兼容性 | ❌ | ✅ |
| values.yaml 复杂度 | 高（需区分 nfs-1/2/3）| 低（统一写 local-path）|

**结论：所有 dev-values.yaml 中的 `nfs-1`、`nfs-2`、`nfs-3` 统一替换为 `local-path` 即可。**

### 3.4 局限性（已知，可接受）

- 数据绑定在特定节点，节点磁盘故障数据丢失 → **dev/test 环境可接受**
- 不支持 `ReadWriteMany`（多 Pod 共同挂载） → 数据库本身不需要此模式
- 生产环境如需 HA 存储，可替换为 **Longhorn**，StorageClass 名字保持一致，values.yaml 无需改动

---

## 四、实施步骤

### Step 1：在两端集群安装 local-path-provisioner

**kind（本地）**：kind 已内置，无需安装，默认 StorageClass 名即为 `local-path`。

**远程集群（C1 / C2）**：
```bash
kubectl apply -f https://raw.githubusercontent.com/rancher/local-path-provisioner/master/deploy/local-path-storage.yaml
```

验证安装：
```bash
kubectl get storageclass
# 应看到 local-path (default)
```

### Step 2：将 dev-values.yaml 中所有 NFS StorageClass 替换为 `local-path`

涉及文件共 **22 个**（见 1.3 节），`nfs-1`、`nfs-2`、`nfs-3` 全部替换为 `local-path`，**无需创建多个 SC**（原因见 3.3 节）。

批量替换命令：

```bash
# 在 k8s 目录下执行（Linux/macOS）
find . -name "dev-values*.yaml" -exec sed -i 's/nfs-[0-9]*/local-path/g' {} +
```

或 Windows PowerShell：
```powershell
Get-ChildItem -Recurse -Filter "dev-values*.yaml" |
  ForEach-Object {
    (Get-Content $_.FullName) -replace 'nfs-\d+', 'local-path' |
    Set-Content $_.FullName
  }
```

### Step 3：验证替换结果

```bash
grep -r "nfs-2" --include="dev-values*.yaml" .
# 应无任何输出
```

### Step 4：集群重置时同步部署

重置集群时，按正常部署流程执行，`local-path-provisioner` 会自动在 `/opt/local-path-provisioner/` 下为每个 PVC 创建独立目录。

---

## 五、生产环境（prod）说明

生产环境 `prod-values.yaml` 目前使用 `fast-ssd` StorageClass（本地 SSD），**无需修改**，已是正确方案。

若将来生产环境需要多副本 HA 存储，建议迁移到 **Longhorn**：
- 安装 Longhorn 并创建 StorageClass 名为 `fast-ssd`（与当前名字一致）
- prod-values.yaml 无需任何修改

---

## 六、数据目录说明

## 七、Affinity 配置说明

### 7.1 local-path-provisioner 自动处理的部分

`local-path-provisioner` 创建 PV 时会**自动设置 `nodeAffinity`**，将该 PV 绑定到创建它的节点。Kubernetes 调度器自动遵守此约束，确保 Pod 重启后仍调度回原节点。**此部分无需手动配置。**

### 7.2 需要手动配置 podAntiAffinity 的场景

对于**多副本**数据库（Redis Sentinel、Elasticsearch 集群等），必须配置 `podAntiAffinity`，强制各副本分散到不同节点。否则多个副本落在同一节点，该节点故障时副本和数据同时丢失，失去高可用意义。

**各组件 dev-values.yaml 中 affinity 配置示例：**

**Redis Sentinel（3副本）**：
```yaml
master:
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchLabels:
              app.kubernetes.io/name: redis
          topologyKey: kubernetes.io/hostname

replica:
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchLabels:
              app.kubernetes.io/name: redis
          topologyKey: kubernetes.io/hostname
```

**Elasticsearch（多节点）**：
```yaml
master:
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchLabels:
              app.kubernetes.io/component: master
          topologyKey: kubernetes.io/hostname

data:
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchLabels:
              app.kubernetes.io/component: data
          topologyKey: kubernetes.io/hostname
```

**MongoDB ReplicaSet（3副本）**：
```yaml
affinity:
  podAntiAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      - labelSelector:
          matchLabels:
            app.kubernetes.io/name: mongodb
        topologyKey: kubernetes.io/hostname
```

### 7.3 单副本组件（无需配置）

| 组件 | 副本数 | 是否需要 podAntiAffinity |
|------|--------|------------------------|
| Redis（dev 单主）| 1 | ❌ 不需要 |
| PostgreSQL | 1 | ❌ 不需要 |
| MongoDB（dev 单节点）| 1 | ❌ 不需要 |
| Neo4j | 1 | ❌ 不需要 |
| Redis Sentinel | 3 | ✅ **需要** |
| Elasticsearch | 3+ | ✅ **需要** |
| MongoDB ReplicaSet | 3 | ✅ **需要** |
| RabbitMQ 集群 | 3 | ✅ **需要** |

> **注意**：`requiredDuring...` 是硬性要求，节点数不足时 Pod 会 Pending。dev 环境节点数够的情况下使用；若节点数不足可改为 `preferredDuring...`（软性偏好）。

---

## 八、数据目录说明

`local-path-provisioner` 默认数据目录：`/opt/local-path-provisioner/<namespace>_<pvc-name>_<pv-name>/`

如需修改数据目录（如存到更大的磁盘分区），修改 local-path-provisioner 的 ConfigMap：
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: local-path-config
  namespace: local-path-storage
data:
  config.json: |
    {
      "nodePathMap": [{
        "node": "DEFAULT_PATH_FOR_NON_LISTED_NODES",
        "paths": ["/data/local-path-provisioner"]
      }]
    }
```
