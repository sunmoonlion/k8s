apiVersion: apps/v1
kind: Deployment
metadata:
  name: celeryworker-llm-admin-backend
  namespace: app-platform-dev
  labels:
    app: celeryworker-llm-admin-backend
    component: celery-worker
    backend: llm-admin-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celeryworker-llm-admin-backend
  template:
    metadata:
      labels:
        app: celeryworker-llm-admin-backend
        component: celery-worker
        backend: llm-admin-backend
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: celeryworker
        image: harbor.sunmoonai.com:30443/k8s-images/llm-admin-backend:1.0.0
        imagePullPolicy: Always
        command:
        - sh
        - -c
        - |
          set -eu
          if [ -n "$(printenv CELERY_INCLUDE_MODULES)" ]; then
            exec celery -A "$(printenv CELERY_APP_MODULE)" worker \
              -I "$(printenv CELERY_INCLUDE_MODULES)" \
              -l "$(printenv CELERY_LOG_LEVEL)" \
              -Q "$(printenv CELERY_QUEUE)" \
              -c "$(printenv CELERY_CONCURRENCY)"
          fi
          exec celery -A "$(printenv CELERY_APP_MODULE)" worker \
            -l "$(printenv CELERY_LOG_LEVEL)" \
            -Q "$(printenv CELERY_QUEUE)" \
            -c "$(printenv CELERY_CONCURRENCY)"
        envFrom:
        - configMapRef:
            name: llm-admin-backend-config
        - secretRef:
            name: llm-admin-backend-secret
        - configMapRef:
            name: celeryworker-llm-admin-backend-config
        - secretRef:
            name: celeryworker-llm-admin-backend-secret
        env:
        - name: CELERY_PRODUCER_SERVICE
          value: "llm-admin-backend"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - celery
            - -A
            - app.worker
            - inspect
            - ping
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          exec:
            command:
            - celery
            - -A
            - app.worker
            - inspect
            - ping
          initialDelaySeconds: 10
          periodSeconds: 15
