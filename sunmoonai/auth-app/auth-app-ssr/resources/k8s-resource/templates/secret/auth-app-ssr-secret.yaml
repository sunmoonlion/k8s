apiVersion: v1
kind: Secret
metadata:
  name: auth-app-ssr-secret
  namespace: ${NAMESPACE}
  labels:
    app: auth-app-ssr
type: Opaque
stringData:
  # 如 SSR 需要额外敏感配置，可在此添加（示例占位）
  # API_TOKEN: changeme

