# 对五家终审的处置：`refact-fable.md` 12:40 版

> 日期：2026-09-05 ｜ 处置者：fable ｜ 对象：同目录 `review-final-{opus,luna,cursor,kimi,qoder}.md`（均针对 12:15 版）
> 所有者两条裁定：**(a) 主仓写权限移出 VM**（agent 只推候选仓，主线 push 由 Windows 执行）；**(b) 协议补「机器可读块」算改协议，按 T2 开轮**（R0′）。
> 处置词：采纳 / 部分采纳 / 不采纳。每条给落点，方便复核。

## 0. 先认一条错

3.13.1 写「本 VM 全 root」——错。opus 与 luna 在宿主 shell 复核：`uid=1003(zym)`。错因：我在 Cursor 沙箱内取证，`/proc/self/uid_map` 为 `0 1003 1`，
user namespace 把 uid 1003 映射成 0。结论（agent 与人共享 credential domain）不变，理由重写；取证声明规则与 §8 第 9 条由此新增（3.13.0）。

## 1. 五家收敛点

| 问题 | 谁提 | 处置 | 落点 |
| --- | --- | --- | --- |
| H5 强制点在 VM 可写面（hook / 脚本），主仓可被 agent 直接 push | luna C2、opus §3 | **采纳**（所有者裁定 a）：主仓对 VM 只读，候选推候选仓，⑦ 由 Windows 执行 | 3.13.2 ②、3.13.5、§3.3 H5 行、§8 第 2/6 条 |
| 跨仓 tag 不能实现所描述的回执对象 | luna C1 | **采纳**：回执 = 回执仓 `main` 上自己的签名 commit + yaml，`target_commit` 为普通字段 | 3.13.3、3.13.4 |
| 回执仓「只验存在不验签」挡不住 Windows agent 用缓存凭据推未签名 tag | kimi F1 | **采纳 ①+②**：VM 用 GitHub 账户端点在线取公钥集验签；Windows 推送不走 GCM，用带口令 SSH key | 3.13.2 ①③、3.13.5 |
| `paths` 过宽把权威文件卷进 T0；gates 脚本在 paths 内 | cursor F3、opus 4.3、kimi F2 | **采纳**：包纪律加在包定义上，`check-policy.py` 签策略前校验；gates 集中 `scripts/gates/`；示例收窄 | 3.6、§5.1、R1 验收 |
| §8 第 4 条靠正文 `rg` 比状态词会误报 / 漏报 | luna D4、kimi F4、opus 5.3 | **采纳**：`policies/state-machine.toml`（头部 `source = request-lifecycle.md @ <commit>`），判据比 schema 与实现 | §5.1、§8 第 4 条 |
| 修订记录「H7 取消例外」歧义 | luna C3、kimi F3 | **采纳**：改为「H7『取消』为表内唯一例外」；正文四处一致保留例外 | 文首、3.3 |
| `_probe` 一处真 push 一处 dry-run | opus 5.1 | **采纳**：统一 dry-run，对主仓与回执仓各做一次 | 3.13.5 第 3 条、3.13.6 |
| `expires` 缺默认与语义 | kimi F5、opus 5.2 | **采纳**：缺省 = 收件箱截止，否则 +24h；只约束放行窗口，放行后不追溯 | 3.13.4 |

## 2. 逐家

### opus

| 条 | 处置 | 落点 / 理由 |
| --- | --- | --- |
| §2 取证错误 | **采纳**，见 §0 | 3.13.0、3.13.1 |
| §2.2 B 档成本基于错误前提；agent 用户须不在 `sudo` / `docker` 组 | **采纳** | 3.13.2 加固表 B 行 |
| §3 判定代码可改 | **采纳**（并入所有者裁定 a）：强制点移到凭据层，脚本改了也发不起写 | 3.13.5 规则 1、3.13.6 |
| 4.1 `independence` 在 luna/kimi 这对上判不了 | **采纳**：改 `provider / runtime / model_family` 三事实字段，首版按 `runtime` 分组，⚠ 未验证 | 3.2、R3、§7-6 |
| 4.2 bootstrap 是绕过判据 | **部分采纳**：登记 §7-14；判据属 round-protocol，是它的 T2，本文不改 | §6 调整说明、§7-14 |
| 4.3 `paths` 过宽 | **采纳**（三家同点） | 3.6 |
| 5.1 / 5.2 / 5.3 | **采纳** | 3.13.6 / 3.13.4 / §8 第 4 条 |
| §6 加第 9 条 | **采纳** | §8 第 9 条 |

### luna

| 条 | 处置 | 落点 / 理由 |
| --- | --- | --- |
| C1 跨仓 tag | **采纳** | 3.13.3 |
| C2 H5 不可强制 | **采纳**（所有者裁定 a） | 3.13.2 ② |
| C3 H7 矛盾 | **采纳**：是修订记录措辞错，正文例外保留；所有者裁定原意即保留例外 | 文首 |
| C4 T2 两次触点 | **采纳**：T0/T1/T2 = 0/1/2 | 3.6、§8 第 5 条 |
| C5 `RUNNING` 不可判 | **采纳**：orchestrator 单写 `events.jsonl`；落地前覆盖声明合并 `QUEUED`/`RUNNING` 标 ⚠ | 3.1.1、3.8、§7-17、§8 第 3 条 |
| C5 Attempt 语义 | **采纳「显式声明」**，选项二（Attempt 产出 typed Artifact）：child Task 方案会让一轮 T2 生成七个 Task。R5 内核边界声明里明写；若内核维护者视为扩充则走 T2 | 3.1.1 |
| C6 取证不一致 | **采纳**，见 §0；S1 以生产 agent 相同身份重跑 | 3.13.0 |
| D1 `route_proposal / effective / delta` | **采纳** | 3.6 工单字段 |
| D2 `intake_author` | **采纳**：同票禁任 proposer / arbiter / acceptor，分发前机械拦；T2 可由 owner 直接出题 | 3.6、R1 验收 |
| D3 独立性字段 | **采纳**（与 opus 4.1 合并） | 3.2 |
| D4 机器可读 schema | **采纳** | §5.1、§8 第 4 条 |
| D5 禁词误报 | **部分采纳**：词表缩到 supervisor 及中文别名；`dispatcher` 等不禁；职责唯一性靠 §8 第 1 条与 `roles_allowed` 只取已注册角色 | §5.1 |

### cursor

| 条 | 处置 | 落点 / 理由 |
| --- | --- | --- |
| F1 开工时无 diff | **采纳**：开工门 / 完工门两张表 | 3.6、§3.3 H1/H2 行、§8 第 5 条 |
| F2 tag 名字空间与组合 | **采纳，形状不同**：回执改为 yaml 后按 `transitions` 字段查找，组合回执天然可被每个 transition 查到，不需要多重 ref | 3.13.3 |
| F3 `paths` 过宽 | **采纳** | 3.6 |
| F4 四处措辞 | **采纳** | 3.4、3.13.4、3.13.1、§5.2 |
| 残留 1 中途 H 全 Task 暂停 | **采纳**：明写为开发 Profile 的 guard 选择 | 3.1.1 |
| 残留 2 §7-12 | **已决**（所有者裁定 b） | §7-12、R0′ |

### kimi

| 条 | 处置 | 落点 / 理由 |
| --- | --- | --- |
| F1 验签 | **采纳 ①+②** | 3.13.2、3.13.5 |
| F2 gates 与 paths 相交 | **采纳**：gates 集中 `scripts/gates/`，包 `paths` 禁覆盖 | 3.6 |
| F3 H7 措辞 | **采纳** | 文首 |
| F4 边表数据文件 | **采纳** | §5.1 `state-machine.toml` |
| F5 `expires` | **采纳** | 3.13.4 |

### qoder

| 条 | 处置 | 落点 / 理由 |
| --- | --- | --- |
| C1 摩擦风险 | **采纳**为风险行 + 观察值 | §7-15 |
| C2 T0 口径窄，多放宽松包 | **部分采纳**：多个**窄**包可以，宽包不许（与 cursor F3 冲突）；跨包升 T1；比例 < 20% 触发重审 | 3.6、§7-15 |
| C3 bootstrap 与 §8 循环 | **不采纳**：把「冻结判据」与「判据已满足」混了。先冻结验收标准再干活正是 H1 的定义；S1 产出是 §8 第 6 条的实证，不是冻结前提。S1 前置措辞改为「§8 判据已冻结」防误读 | §6 |
| C4 取消恢复成本 | **采纳**：H7 例外保留，副作用清单格式由 L0 校验 | §7-16 |
| C5 `check-no-owner-creds.sh` 非边界 | **采纳**：明写「配置卫生检查，非边界」 | 3.13.5 第 3 条、§5.1 |
| C6 `lifecycle.md` 估行 | **采纳**：600–800 | §5.1 |

## 3. 未采纳的两条与理由

1. **qoder C3**（见上）。
2. **luna C5 的 child Task 方案**：选了另一选项，理由已写进 3.1.1。

## 4. 请 luna 复核

luna 的结论是 REQUEST CHANGES，条件是「回执改为回执仓自己的签名 manifest」与「主仓写权限移出 agent credential domain」。
两条都已按所有者裁定落实（3.13.2、3.13.3、3.13.5）；C3/C4/C5 的正文矛盾已修。请复核 12:40 版是否可撤销 REQUEST CHANGES。
