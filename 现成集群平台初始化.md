# 现成集群平台初始化（阶段二）

本文档专门说明：在 **Kind 或任意已有集群**上，用最少脚本复现「平台层」核心能力，并**与远程部署（deploy-infrastructure-all）在名单与约定上保持一致**。不修改 deploy-infrastructure-all 主流程，仅新增「对现成集群」的独立脚本。

---

## 1. 目标与范围

- **目标**：Kind（或任意当前 KUBECONFIG 指向的集群）创建与远程**相同**的命名空间集合、以及一份可用的本地存储（local-path），使同一套应用/平台部署命令在 Kind 与远程上都能找到相同 namespace 和 StorageClass。
- **范围**：仅 2 类脚本——(1) 命名空间；(2) 本地存储（local-path）。不做 NFS、云存储、节点污点、镜像预加载等（迁移指南中已约定 Kind 用最小方案）。
- **入口**：脚本的输入仅为「当前 KUBECONFIG 或 context」；执行前由用户或连接管理器/统一模板已设好 KUBECONFIG。

---

## 2. 「与远程一致」的含义

| 维度 | 一致 | 不一致（刻意简化） |
|------|------|--------------------|
| **配置来源** | 与远程**同源**：读 `deploy-infrastructure-all/deploy-infrastructure-all.conf` 中 Step07、Step09 相关变量。 | 不读 SERVER_*、不依赖 SSH/节点列表。 |
| **命名空间** | 名单（environments × platforms）、命名规则（`platform-environment`）、是否应用策略与 Step07 一致。 | 执行方式：本机 `kubectl`，不 SSH 到远程节点。 |
| **存储** | 只做 **local-path**：StorageClass 名称、是否默认、reclaim policy 等与 Step09 本地存储配置一致。 | 不做 NFS、云 CSI；不在节点上创建目录或装 NFS，仅集群内 apply provisioner + StorageClass。 |

这样应用层 YAML 中 `namespace: app-platform-dev`、`storageClassName: local-storage` 等在 Kind 与远程上无需修改。

---

## 3. 脚本一：命名空间（对应 Step07）

### 3.1 职责

- 根据「环境列表 × 平台列表」创建命名空间，命名为 `platform-environment`。
- 可选：对命名空间打 label、应用策略（与 Step07 的 `NAMESPACE_PLATFORM_APPLY_POLICIES` 一致；当前 Step07 策略为占位，可同步只做「打 label」或预留策略扩展）。

### 3.2 配置（与远程同源）

从 **deploy-infrastructure-all.conf** 读取（脚本内 source 该 conf，或显式解析以下变量）：

| 变量 | 含义 | 示例/默认 |
|------|------|-----------|
| `NAMESPACE_PLATFORM_ENVIRONMENTS` | 环境列表，逗号分隔 | `dev,prod` |
| `NAMESPACE_PLATFORM_PLATFORMS` | 平台列表，逗号分隔 | `app-platform,data-platform,infrastructure,...` |
| `NAMESPACE_PLATFORM_APPLY_POLICIES` | 是否应用命名空间策略 | `true` / `false` |

- 不依赖 `STEP07_TARGET`、`SERVER_*`、`get_defined_server_indices` 等；仅用上述名单在本机执行。
- 若 conf 不存在或变量为空，脚本内应提供合理默认值（与 conf 中当前默认一致），避免硬编码多份名单。

### 3.3 行为

1. 解析 `NAMESPACE_PLATFORM_ENVIRONMENTS`、`NAMESPACE_PLATFORM_PLATFORMS`（去引号、trim、按逗号 split）。
2. 对每个 `(platform, environment)`：`namespace=platform-environment`；若不存在则 `kubectl create namespace "$namespace"`，并打 label（若有约定）。
3. 若 `NAMESPACE_PLATFORM_APPLY_POLICIES=true`，对每个 namespace 执行策略逻辑（当前 Step07 中策略为占位，可先与 Step07 保持一致：仅日志或预留扩展）。
4. 使用当前 shell 的 `KUBECONFIG`（不写死路径）；脚本开头可检查 `kubectl cluster-info` 或 `kubectl get ns` 是否可用。

### 3.4 位置与命名

- **路径**：`sunmoonai/kind-infrastructure/apply-namespaces-existing-cluster.sh`
- **命名**：`apply-namespaces-existing-cluster.sh` 表示「对任意现成集群应用命名空间」，Kind 与其它已有集群共用。

---

## 4. 脚本二：NFS 存储（Kind 与远程一致、换节点可见）

### 4.1 职责

在**已存在**的 Kind 集群内部署 **nfs-subdir-external-provisioner**，连接 **WSL 宿主机**上的 NFS 服务（需先在 WSL 安装并 export `/data/kind-nfs`），创建 StorageClass **nfs-2**（与远程 dev 常用一致）。数据在 WSL 文件系统，多节点共享、换节点可见，关集群后数据仍在

### 4.2 前置条件

- WSL 上已安装并启动 `nfs-kernel-server`，已 export `/data/kind-nfs`（见《kind使用指南.md》或前文「WSL 上装 NFS server」）。
- 当前 `KUBECONFIG` 指向目标 Kind 集群，且集群可访问。

### 4.3 行为

1. 使用当前 `KUBECONFIG`，检查集群与 Helm 可用。
2. 添加 Helm 仓库并安装 nfs-subdir-external-provisioner，`nfs.server` 为宿主机 IP（默认 `172.17.0.1`，可设 `KIND_NFS_SERVER_HOST`），`nfs.path` 为 `/data/kind-nfs`（可设 `KIND_NFS_PATH`）。
3. 创建 StorageClass `nfs-2`（可设 `KIND_NFS_STORAGE_CLASS_NAME`）。部署时由 values（如 dev-values）指定 `storageClassName: nfs-2` 即可，Kind 与远程一致。

### 4.4 位置与命名

- **路径**：`sunmoonai/kind-infrastructure/apply-nfs-existing-cluster.sh`。

---

## 5. 使用方式（与迁移指南一致）

- **何时用**：Kind 集群创建好后，在部署应用/平台前，先对「当前 KUBECONFIG 指向的集群」执行命名空间 + NFS 存储（前置：WSL 已运行 `wsl-setup-nfs-server.sh`）。
- **顺序**：先命名空间，再 NFS 存储。
- **谁设 KUBECONFIG**：由用户手动 `export KUBECONFIG=...`，或先运行连接管理器/统一模板的 `setup_kubectl_environment()` 再在同一个 shell 中执行本类脚本。
- **唯一入口**：`kind-up.sh` 依次执行「创建集群 → 命名空间 → NFS」，直接调用 `apply-namespaces-existing-cluster.sh` 与 `apply-nfs-existing-cluster.sh`。

---

## 6. 实施检查清单（实现时可按此核对）

- [x] 脚本目录：`sunmoonai/kind-infrastructure/`。
- [x] **命名空间脚本** `apply-namespaces-existing-cluster.sh`：  
  - [x] 从 deploy-infrastructure-all.conf 解析 `NAMESPACE_PLATFORM_*`（source 后使用）；无 conf 时使用与 conf 默认一致的默认值。  
  - [x] 仅使用本机 `kubectl`，不 SSH；不依赖 SERVER_*、CLUSTER、get_defined_server_indices。  
  - [x] 创建 namespace；`NAMESPACE_PLATFORM_APPLY_POLICIES` 与 Step07 行为一致（当前为占位/日志）。  
  - [x] 脚本内不写死 KUBECONFIG 路径。
- [x] **NFS 存储脚本** `apply-nfs-existing-cluster.sh`：供 Kind 使用 WSL 上的 NFS，创建 StorageClass nfs-2；前置条件为 WSL 已配置 NFS 并 export /data/kind-nfs（见 `wsl-setup-nfs-server.sh`）。
- [ ] 在《迁移指南.md》或《kind使用指南.md》中增加「现成集群平台初始化」小节，指向本文档及 `sunmoonai/kind-infrastructure/`。
- [x] **唯一入口**：`kind-up.sh` 已实现「创建集群 + 命名空间 + NFS」一键完成。

---

## 7. 参考

- **策略与切换**：《策略.md》
- **迁移节奏与阶段二/三**：《迁移指南.md》第 3 节
- **Kind 操作**：《kind使用指南.md》
- **远程 Step07/Step09 实现**：`sunmoonai/infrastructure/steps/step07_create_namespaces.sh`、`step09_storage.sh` 及 `deploy-infrastructure-all/deploy-infrastructure-all.conf`
- **阶段二脚本**：`sunmoonai/kind-infrastructure/apply-namespaces-existing-cluster.sh`、`apply-nfs-existing-cluster.sh`、`wsl-setup-nfs-server.sh`（WSL 上一次性执行）
