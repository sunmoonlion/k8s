apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodebullworker-llm-web-backend
  namespace: app-platform-dev
  labels:
    app: nodebullworker-llm-web-backend
    component: node-bull-worker
    backend: llm-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodebullworker-llm-web-backend
  template:
    metadata:
      labels:
        app: nodebullworker-llm-web-backend
        component: node-bull-worker
        backend: llm-web-backend
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: nodebullworker
        image: harbor.sunmoonai.com:30443/k8s-images/llm-web-backend:1.0.0
        imagePullPolicy: Always
        command:
        - sh
        - -c
        - |
          set -eu
          export NODE_ENV="${NODE_ENV:-production}"
          export QUEUE_ON="${QUEUE_ON:-true}"
          exec node dist/worker
        envFrom:
        - configMapRef:
            name: llm-web-backend-config
        - secretRef:
            name: llm-web-backend-secret
        - configMapRef:
            name: nodebullworker-llm-web-backend-config
        - secretRef:
            name: nodebullworker-llm-web-backend-secret
        env:
        - name: QUEUE_WORKER_SERVICE
          value: "llm-web-backend"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
