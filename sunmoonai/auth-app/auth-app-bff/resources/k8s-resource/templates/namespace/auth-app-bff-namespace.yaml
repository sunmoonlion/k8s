# Auth App BFF 命名空间配置
# 用途：创建应用专用的 Kubernetes 命名空间
#
apiVersion: v1
kind: Namespace
metadata:
  name: ${NAMESPACE}
  labels:
    app: app-platform
    component: auth-app-bff
    environment: ${ENVIRONMENT}
    managed-by: deploy-script

