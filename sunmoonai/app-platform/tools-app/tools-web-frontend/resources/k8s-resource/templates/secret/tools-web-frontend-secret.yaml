apiVersion: v1
kind: Secret
metadata:
  name: tools-web-frontend-secret
  namespace: ${NAMESPACE}
  labels:
    app: tools-web-frontend
type: Opaque
stringData:
  # web-frontend 为纯 UI（Next.js），鉴权与 Session 由 web-backend 承担。
  # 不在前端 Pod 注入 Casdoor / Redis 密钥；本 Secret 预留供将来扩展。
