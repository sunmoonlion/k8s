# k8s 环境事实表

> 本文档只记录"会变化但当前有效"的环境事实。  
> 稳定的架构说明见 `docs-claude/整体架构.md`。  
> **提交前确认：本文件不包含任何证书私钥；密码等凭据仅供内部联调，不对外公开。**

---

## 1. 集群与节点（当前）

### C1 集群

| 角色 | 主机名 | 公网 IP | 备注 |
|------|--------|---------|------|
| control-plane | hsy-local-1 | `115.190.64.131` | SSH `zym@115.190.64.131 -p 1022` |
| Worker1 ⭐ | hsy-local-2 | `101.126.151.0` | **Traefik Pod 实际运行节点**，DNS 指向此节点 |
| Worker2 | hsy-local-3 | `115.190.61.238` | |

### C2 集群

| 角色 | 公网 IP | 备注 |
|------|---------|------|
| control-plane | `115.190.37.57` | SSH `zym@115.190.37.57 -p 1022` |
| Worker1 ⭐ | `115.190.153.150` | **Traefik Pod 实际运行节点**，DNS 指向此节点 |
| Worker2 | `115.190.86.211` | |

> ⭐ Traefik 为单副本 Deployment，必须将域名 DNS 解析到实际承载 Pod 的节点；跨节点转发在当前网络配置下会失败。

---

## 2. 域名解析（当前）

| 域名 | 解析 IP | 用途 |
|------|---------|------|
| `www.sunmoonai.com` | `101.126.151.0`（C1 Worker1） | 统一 Web 入口 + TCP 代理入口 |
| `harbor.sunmoonai.com` | `101.126.151.0`（C1 Worker1） | Harbor 镜像仓库（C2 共用此仓库） |

> C2 集群若需独立域名，DNS 应指向 C2 Worker1（`115.190.153.150`）。

---

## 3. 对外端口（当前）

| 服务 | 端口 | 协议 | 暴露方式 |
|------|------|------|----------|
| HTTP | `80` | TCP | iptables → NodePort 30080 |
| HTTPS | `443` | TCP | iptables → NodePort 30443 |
| PostgreSQL | `30444` | TCP | Traefik TCP IngressRoute |
| MongoDB | `30445` | TCP | Traefik TCP IngressRoute |
| Redis | `30446` | TCP | Traefik TCP IngressRoute |
| RabbitMQ AMQP | 不对外 | — | ClusterIP only |

---

## 4. 数据库凭据（当前）

> 仅供联调使用。生产环境应通过 Kubernetes Secret 注入，不硬编码在配置文件中。

| 服务 | 用户名 | 密码 | 数据库 |
|------|--------|------|--------|
| PostgreSQL | `sunmoonai_dev` | `zym123` | `mydb` / `llmops_db` / `auth_db` |
| Redis | — | `StrongRedisPassw0rd!` | — |
| MongoDB | `root` | `zym123` | — |
| RabbitMQ | `admin` | `zym123` | — |

### 联调连接串

```bash
# PostgreSQL
postgresql://sunmoonai_dev:zym123@www.sunmoonai.com:30444/mydb

# Redis
redis://:StrongRedisPassw0rd!@www.sunmoonai.com:30446/0

# MongoDB
mongodb://root:zym123@www.sunmoonai.com:30445/

# Harbor
harbor.sunmoonai.com:30443
```

---

## 5. 命名空间（当前）

| 命名空间 | 用途 |
|----------|------|
| `ingress-platform-dev` | 入口层（Traefik） |
| `cicd-platform-dev` | CI/CD（Harbor、Jenkins） |
| `data-platform-dev` | 数据平台 |
| `messaging-platform-dev` | 消息平台（RabbitMQ） |
| `ops-platform-dev` | 运维工具 |
| `app-platform-dev` | 业务应用 |

---

## 6. 更新规则

- 改 IP、端口、域名、节点角色时，先改本文件，再改脚本或 values。
- 不在本文件写证书私钥；如需记录账号字段，仅写 `<from-secret>` 占位。
- 凭据变更时同步更新上方"数据库凭据"表，并确认 K8s Secret 已更新。

---

最后更新：2026-04-01  
状态：生效中
