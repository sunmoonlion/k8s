apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-admin-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: knowledge-admin-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: knowledge-admin-backend
  template:
    metadata:
      labels:
        app: knowledge-admin-backend
    spec:
      imagePullSecrets:
      - name: ${KNOWLEDGE_ADMIN_BACKEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: knowledge-admin-backend
        image: ${KNOWLEDGE_ADMIN_BACKEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: ${KNOWLEDGE_ADMIN_BACKEND_CONFIGMAP_NAME:-knowledge-admin-backend-config}
        - secretRef:
            name: ${KNOWLEDGE_ADMIN_BACKEND_SECRET_NAME:-knowledge-admin-backend-secret}
        - secretRef:
            name: ${KNOWLEDGE_ADMIN_BACKEND_BROWSER_OIDC_SECRET_NAME:-knowledge-admin-backend-browser-oidc}
        - secretRef:
            name: ${KNOWLEDGE_INFO_INGEST_BINDING_SECRET_NAME:-knowledge-info-ingest-service-binding}
        - configMapRef:
            name: ${CELERYWORKER_KNOWLEDGE_ADMIN_BACKEND_CONFIGMAP_NAME:-celeryworker-knowledge-admin-backend-config}
        - secretRef:
            name: ${CELERYWORKER_KNOWLEDGE_ADMIN_BACKEND_SECRET_NAME:-celeryworker-knowledge-admin-backend-secret}
${KNOWLEDGE_ADMIN_BACKEND_DATABASE_ENV_FROM}
${KNOWLEDGE_ADMIN_BACKEND_OBJECT_STORAGE_ENV_FROM}
${KNOWLEDGE_ADMIN_BACKEND_ELASTICSEARCH_ENV_FROM}
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
${KNOWLEDGE_ADMIN_BACKEND_ELASTICSEARCH_VOLUME_MOUNT}
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-knowledge-admin-backend-pvc}
${KNOWLEDGE_ADMIN_BACKEND_ELASTICSEARCH_VOLUME}
---
apiVersion: v1
kind: Service
metadata:
  name: knowledge-admin-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: knowledge-admin-backend
spec:
  selector:
    app: knowledge-admin-backend
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
