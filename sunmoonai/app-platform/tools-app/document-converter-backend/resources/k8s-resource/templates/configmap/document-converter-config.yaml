apiVersion: v1
kind: ConfigMap
metadata:
  name: document-converter-config
  namespace: ${NAMESPACE}
  labels:
    app: document-converter
data:
  # 服务器与项目
  PROJECT_NAME: "${PROJECT_NAME}"
  SERVER_NAME: "${SERVER_NAME}"
  SERVER_HOST: "${SERVER_HOST}"

  # CORS（可按需调整，留空时服务端代码允许全部）
  BACKEND_CORS_ORIGINS: '${BACKEND_CORS_ORIGINS}'

  # 服务配置
  DOCUMENT_CONVERTER_SERVICE_PORT: "${DOCUMENT_CONVERTER_SERVICE_PORT}"
  DOCUMENT_CONVERTER_UNIFIED_HOST: "${DOCUMENT_CONVERTER_UNIFIED_HOST}"

  # Sentry 监控配置（非敏感部分）
  SENTRY_ENVIRONMENT: "${SENTRY_ENVIRONMENT}"
  SENTRY_RELEASE: "${SENTRY_RELEASE}"
  SENTRY_TRACES_SAMPLE_RATE: "${SENTRY_TRACES_SAMPLE_RATE}"
  SENTRY_PROFILES_SAMPLE_RATE: "${SENTRY_PROFILES_SAMPLE_RATE}"

