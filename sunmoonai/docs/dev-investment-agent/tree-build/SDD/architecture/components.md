# 系统由哪几部分组成

**六块加评测，各跑在哪，映射到哪个仓。**一条求助怎么走完九站见 [九站](../../../pipeline.md)；
部署在哪台机器见 [拓扑](topology.md)；模块之间的接口见 [通道](channels.md)。

```text
用户机器                                   公网边缘                      我们的内网站点
┌──────────────────────────┐   ┌────────────────────┐   ┌──────────────────────────────────────┐
│ 浏览器 ── 网页（产品界面）│──▶│ TLS · 静态网页     │──▶│ 工作台后端（investment-backend）      │
│                          │   │                    │   │  账房 · 编排 · 审批分发 · 验收 · 交付 │
│ 本地代理（runtime 仓）   │   │ 会合点（relay）     │   │  app-server 客户端                    │
│  codex exec-server       │◀─▶│  三类出站客户配对  │◀──│ 沙箱池：每用户一个 codex app-server   │
│  出站桥 · 审批弹窗       │   │  与透传            │   │  （BYOK，用户的 key）──▶ 模型厂商     │
│  本地上限 · 工作区白名单 │   └────────────────────┘   │ 知识服务（knowledge-app）经 MCP        │
│  用户的工作区与项目      │                            │ 身份（Casdoor）· PostgreSQL · 评测     │
└──────────────────────────┘        深处只出站 ◀────────│                                      │
                                                        └──────────────────────────────────────┘
```

**Codex 拆成两半**：模型循环（app-server）在沙箱里，工具执行（exec-server）在用户机器上；两半之间是 Codex 自己的远端环境协议，经会合点透传。这一点已用探针证明（`runtime` 仓 `probe/REPORT-2026-09-23-remote-exec.md`）。

## 六块加评测

按**职责**切，不按信任域；隐私不作约束之后，信任域不再是切分依据。

| 模块 | 跑在哪 | 承担什么 | 不承担什么 | 映射到 |
| --- | --- | --- | --- | --- |
| [`0001-workbench`](../modules/0001-workbench.md) 工作台后端 | 内网 | 四本账；方向盘；顾问（编排）；审批分发；验收；交付；对每个用户 app-server 的客户端 | 不调模型；不执行工具 | `investment-backend`（FastAPI） |
| [`0002-web`](../modules/0002-web.md) 网页 | 边缘（静态、无状态）→ 内网 API | 登录；工作区与项目选择；与 Codex 对话；求助；进度；审查；底稿；key 与费用 | 不持凭据；不含业务逻辑 | `investment-web-frontend`（Next.js） |
| [`0003-sandbox`](../modules/0003-sandbox.md) 沙箱池 | 内网 k8s | 每用户一个 app-server 进程与它的 `CODEX_HOME`；注入用户 key；出站桥去会合点 | 不执行用户工具；不存 Task 真源 | 新，`k8s` 仓部署单元 |
| [`0004-relay`](../modules/0004-relay.md) 会合点 | 边缘 | 三类出站客户的认证、配对、透传；健康与配额 | 不存状态；不解析业务 | 新，独立小服务 |
| [`0005-agent`](../modules/0005-agent.md) 本地代理 | 用户机器 | 启动 `codex exec-server`；出站桥；根目录白名单；本地上限；审批弹窗；版本配对 | 不跑模型循环；不接触 key | `runtime` 仓 |
| [`0006-knowledge`](../modules/0006-knowledge.md) 知识服务 | 内网 | 自有数据与用户资料的检索、领域工具、口径与计算，经 MCP | 不装观点；不整批下发 | `knowledge-app`（+ `info-app` 采集） |
| [`0007-eval`](../modules/0007-eval.md) 评测 | 内网 | 金标准、三道门、四臂对比、样本沉淀 | 不参与在线路径 | 新，先落 `investment-backend` |

**Casdoor**、**PostgreSQL**、**对象存储**沿用平台现状，不占模块（[项目总览](../../../../project-guide/overall-architecture.md)）。

## 每一块在九站里的位置

| 站 | 网页 | 工作台 | 沙箱 | 会合点 | 本地代理 | 知识服务 |
| --- | --- | --- | --- | --- | --- | --- |
| 写 message | ● | 质量信号 | | | | |
| 提交 | ● | ● 幂等 | | | | |
| 受理 | | ● | | | | |
| 派发 | | ● 方向盘接手，发 turn | ● 收 turn | 透传 | | |
| 执行 | | 看事件 | ● 循环 | ● 透传 | ● 工具 | ● MCP |
| 中断 | ● 审查 | ● Interaction | 审批请求 | 透传 | ● 上限弹窗 | |
| 封装 | | ● 明文入库 | 产出 | | 落工作区 | |
| 验收 | | ● | 验收 Attempt | | | |
| 交付 | ● 回放 | ● cursor | | | | |

## 退役了什么

| 退役 | 原因 | 顶上的是 |
| --- | --- | --- |
| 桌面应用（Electron）、`desktop-app` 仓 | 界面回到网页；机构装不装桌面应用不再是问题 | `0002-web` + `0005-agent` |
| 本地 runtime 驱动完整 Codex | 循环搬到沙箱 | `0003-sandbox` + `0005-agent` |
| 加密与密钥整族（`F-CRYPTO-*`、`I16` `I17` `I20`） | 隐私不作约束 | 明文入库；`AT-21` 只守 key |
| 设备配对、设备私钥、派发签名 | 没有跨网派发；沙箱和代理都在我们的协议内 | 会合点令牌（`C-I9`） |
| 租约、fencing、断线暂停与对账 | 单委托、无并行、循环在云 | Codex 的 25 秒恢复窗 + `WAITING(ENVIRONMENT)` + 方向盘交接 |
| 三层类别判定与领域识别引流 | 卖点与入口分开 | 用户选专家包 + 质量信号 |
| LangGraph Pilot 链 | 顾问不调模型 | 编排 = 对同一个 app-server 逐步发 turn |
| "让用户能验证"、runtime 必须开源 | 云端看得到一切，验证失去对象 | `D14` |
| 本地内容检查 | 检查在云端 | 验收里的确定性检查 |
