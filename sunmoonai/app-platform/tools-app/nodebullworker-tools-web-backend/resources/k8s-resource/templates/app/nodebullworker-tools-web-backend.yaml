apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodebullworker-tools-web-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: nodebullworker-tools-web-backend
    component: node-bull-worker
    backend: tools-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodebullworker-tools-web-backend
  template:
    metadata:
      labels:
        app: nodebullworker-tools-web-backend
        component: node-bull-worker
        backend: tools-web-backend
    spec:
      imagePullSecrets:
      - name: ${NODEBULLWORKER_TOOLS_WEB_BACKEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: nodebullworker
        image: ${NODEBULLWORKER_TOOLS_WEB_BACKEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        command:
        - sh
        - -c
        - |
          set -eu
          export NODE_ENV="${NODE_ENV:-production}"
          export QUEUE_ON="${QUEUE_ON:-true}"
          exec node dist/worker
        envFrom:
        - configMapRef:
            name: ${TOOLS_WEB_BACKEND_CONFIGMAP_NAME:-tools-web-backend-config}
        - secretRef:
            name: ${TOOLS_WEB_BACKEND_SECRET_NAME:-tools-web-backend-secret}
        - configMapRef:
            name: ${NODEBULLWORKER_TOOLS_WEB_BACKEND_CONFIGMAP_NAME:-nodebullworker-tools-web-backend-config}
        - secretRef:
            name: ${NODEBULLWORKER_TOOLS_WEB_BACKEND_SECRET_NAME:-nodebullworker-tools-web-backend-secret}
        - secretRef:
            name: ${NODEBULLWORKER_TOOLS_WEB_BACKEND_QUEUE_REDIS_SECRET_NAME:-tools-web-backend-nodebull-redis-conn}
        env:
        - name: QUEUE_WORKER_SERVICE
          value: "tools-web-backend"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
