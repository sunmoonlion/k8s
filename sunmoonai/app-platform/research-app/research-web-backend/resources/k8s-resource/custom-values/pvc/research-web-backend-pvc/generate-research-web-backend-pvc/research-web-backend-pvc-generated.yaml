apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: research-web-backend-pvc
  namespace: app-platform-dev
  labels:
    app: research-web-backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
