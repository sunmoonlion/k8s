apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodebullworker-info-web-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: nodebullworker-info-web-backend
    component: node-bull-worker
    backend: info-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodebullworker-info-web-backend
  template:
    metadata:
      labels:
        app: nodebullworker-info-web-backend
        component: node-bull-worker
        backend: info-web-backend
    spec:
      imagePullSecrets:
      - name: ${NODEBULLWORKER_INFO_WEB_BACKEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: nodebullworker
        image: ${NODEBULLWORKER_INFO_WEB_BACKEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        command:
        - sh
        - -c
        - |
          set -eu
          export NODE_ENV="__CONTAINER_NODE_ENV_DEFAULT__"
          export QUEUE_ON="__CONTAINER_QUEUE_ON_DEFAULT__"
          exec node dist/worker
        envFrom:
        - configMapRef:
            name: ${INFO_WEB_BACKEND_CONFIGMAP_NAME:-info-web-backend-config}
        - secretRef:
            name: ${INFO_WEB_BACKEND_SECRET_NAME:-info-web-backend-secret}
        - configMapRef:
            name: ${NODEBULLWORKER_INFO_WEB_BACKEND_CONFIGMAP_NAME:-nodebullworker-info-web-backend-config}
        - secretRef:
            name: ${NODEBULLWORKER_INFO_WEB_BACKEND_SECRET_NAME:-nodebullworker-info-web-backend-secret}
        - secretRef:
            name: ${NODEBULLWORKER_INFO_WEB_BACKEND_QUEUE_REDIS_SECRET_NAME:-info-web-backend-nodebull-redis-conn}
        env:
        - name: QUEUE_WORKER_SERVICE
          value: "info-web-backend"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
