# =============================================================================
# Harbor Registry Secret 部署配置（Redis）# =============================================================================

SECRET_NAME="harbor-registry-secret"
SECRET_NAMESPACE="${SECRET_NAMESPACE:-data-platform-dev}"
SECRET_TYPE="kubernetes.io/dockerconfigjson"

# Docker服务器地址
# 优先使用主配置文件中）REDIS_IMAGE_REGISTRY（如果已设置）# 如果主配置文件未加载，则使用默认
DOCKER_SERVER="${REDIS_IMAGE_REGISTRY:-harbor.sunmoonai.com:30443}"
DOCKER_USERNAME="admin"
DOCKER_PASSWORD="Ha!r1359admin"

RESTART_COMPONENTS="false"
RESTART_PRIORITY="50"

