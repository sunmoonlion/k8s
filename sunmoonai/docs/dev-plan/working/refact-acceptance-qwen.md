# Refact 轮 · 环节 ⑤ 验收 · qwen

> 作者：qwen（工作目录 `~/worktrees/qwen/k8s`，分支 `qwen`）｜日期：2026-09-04
> 角色：**验收方**。裁定 R2 改派（顺位 `kimi(4) → qwen(5) → cursor(12)`；判据为既得利益最小，非质量）。
> 依据：`refact-acceptance-call.md`（`27f0d077`）——§3 判据、§4 五项独立复核、§5 交付四栏、§6 停滞判据。
> 纪律：按 commit 取件、不看工作区；引用行号同时给冻结点 `c501c723`；**不直接改裁决稿**（异议写第 3 栏交裁决方处置）；验收亦是产出，自陈盲区（第 4 栏）。
>
> **R2 代价，正面接住**：qwen 是本轮两处取证被纠正的家（`profiles.py` 跨仓错位、`constraints.md` I12 编号出处）。这两类错正是本次验收重点（§4.4），抽查样本与选取方式写明于第 2 栏 §2.4，并专门回扫裁决稿有无重犯我这两类错。

## 0. 对象事实（按 commit 亲核，非工作区）

| 对象 | 分支 · commit | sha256 | 行数 |
| --- | --- | --- | --- |
| 冻结点 | `refact-integration` · `c501c723` | —（下三行为其内容） | — |
| 裁决稿 `development-lifecycle-agent.md` | `c501c723`（= HEAD `27f0d077`，冻结后未变） | `a602b3443e59fb2e633b08de1d447455a81df63ae7e574d12f955e28def7009c` | 1603 |
| 处置记录 `refact-disposition.md` | `refact-integration` | `f253009ccbd3e049fe8608fd98e9ddcf11cbb4bec0f1c60e9c4dcbeb69e2cf83` | 244 |
| 裁定记录 `refact-rulings.md` | `refact-integration` | `0551f2c019f6e7be56b8fc33dced91761d75df048bbbe37d88ce87a24997a742` | 66 |
| 基座（luna，环节③ 起点） | `f8bc48e3` | `3749a0c00bfe6258525f809ec7cc4cb396e4b9c7cc6877d88abfc10f27a5e3e2` | 1439 |
| master 原始基座（opus 未改，§4.1 比对基准） | `master` | `a9533a45c6c19ba65aaab5e2146e93ffe6a3d6958f587ff1d3e9a916f121f2e5` | 1161 |
| qwen 候选（环节① 冻结未动） | `qwen` · `0c6f0fc3` | `ea2bb7abf2d96047b4a203c24a2c0467107919c21d92fbfa2938e155468675fd` | 1560 |
| qwen 评审（环节② 冻结未动） | `qwen` · `d26166a5` | `9fdce417413fd3d91e8b29ab57d7726ad8246bd56bfb66c8cb4ad71ef21070ad` | 190 |
| qwen 异议（环节④） | `qwen` · `10267649` | `e222d84c42924c75a8088348fe5710aceb1ab81ca5003a8749df2a49be136bec` | 87 |

`master..c501c723` 共 25 个提交（第 26 是验收通知本身 `27f0d077`）。以下所有行号均指冻结点 `c501c723` 的裁决稿，除非另注。

## 1. 11 条验收标准逐条判定（`refact-task.md` §12）

> 两条硬规矩：① 只按**冻结的标准**判定，不为让它通过而静默改标准；② 某条不通过，先分「**产出没达标** / **标准本身过期**」，判成后者写明理由**交裁决方处置**，不自行改标准。

判定汇总：**11 条全数通过**。逐条证据（行号均在冻结点 `c501c723`）：

| # | 标准（`refact-task.md` §12，冻结不改） | 判定 | 独立复核证据 |
| --- | --- | --- | --- |
| 1 | 冻结节逐字未改（§7 清单全成立） | 通过 | 见 §2.1：七节 master vs `c501c723` 分段 sha256 逐一 IDENTICAL |
| 2 | §8 七块到位、核心断言带 `file:line`／可复跑锚点 | 通过 | 七块落 §4.5(376 路线转向)／§4.6(423 两 SDK 两轴非对称)／§4.7(444 Port 三态探针)／§4.8(500 Harness 门禁+过渡补法)／§4.9(530 双 runtime 部署进程恢复)／§4.10(573 专用 Agent 构建法)／§4.11(586 OpenClaw 借鉴不转向)；锚点抽验见 §2.4（24 处全 OK） |
| 3 | 用 `F-*`／`I*`／`AT-*`／A-C-D-I-T-R 编号锚定，不写散文 | 通过 | §8.1 矩阵(1152-1163) 逐格带 `F-EXEC-01..08`／`F-INTERACT-01/02`＋`AT-05/06/07/08/09/11/12/13/14/15/20/21`＋`I3/I5/I10/I11/I12/I14/I15`；本验收独立数得十行功能项全覆盖，非散文 |
| 4 | 交出 §9.2 两条腿映射表 | 通过 | §8.1「`F-EXEC-*`／`F-INTERACT-*` 双腿落地矩阵」(1136)，四列（功能 ID／Codex 腿／Harness 腿／需补法与验收）＋三档对应表(1143-1145) |
| 5 | supervisor 消歧完成，无两处指代不同层而未说明 | 通过 | §0.3 定名：调度监督器(dispatch supervisor，上层确定性代码，符号 `TaskRouter`，77)／执行监督 Agent(execution supervisor，Attempt 内，78)／第三套＝产品子 Task 编排(只引用不定义，80-81)；§6 改名「Attempt 内的执行监督 Agent」(750)；§6.9 内层三不得(871)；冻结区裸词规则(109「只有写全『调度监督器』时才指上层」) |
| 6 | §9.3 存续声明显式处理＋引用清理清单 | 通过 | §11.1 挑明与 `development-lifecycle-human.md` 头部的当下矛盾（D-C15）＋ §11.2 删除时引用清理表；处置记录 §9 行 6 同判 |
| 7 | 无具体 Profile 字段表（§10） | 通过 | grep `investment-finance-analysis`／`financial_query`／`knowledge_retrieve`／六个工具／行数上限＝零命中；411「金标准」是 Gate 0 可证伪条件、581「本文不冻结某个财务 Profile 的字段、工具清单、数据集或金标准」明拒写 |
| 8 | 不重定义 `request-lifecycle.md` 对象 | 通过 | 头部(19-26，D-C16)：本文「定义怎么把它建出来」，`I1–I15`／`AT-01…AT-22` 一律以 `request-lifecycle.md` 为准；1256「`request-lifecycle.md` 穷举，本文不重定义」 |
| 9 | 「dsh 需要系统 Node」不得出现（§4 ③） | 通过 | 全文 `Node` 仅两处且均否定式：437「SDK 不需系统 Node…不得复活『缺 Node 阻断』」、1411 §11.3 唯一未验证项「wheel 能否从内网 PyPI 取得」（供应链、⚠）；无正面断言 |
| 10 | 「能构建专业 agent」与「能接进生产控制面」两轴分开 | 通过 | §4.6 标题「两个轴、非对称能力」(423)；398-399 分列「架构方向＝更合理」与「当前生产成熟度＝⚠ 尚未更好（部署门禁与 wire 门禁无运行证据）」两行，不混为一句 |
| 11 | 未验证结论标 ⚠，不假装已门禁化 | 通过 | ⚠ 散标 9 处＋ §11.3「本轮未验证清单」(1402) 集中登记（表头 1407，1405 明写只登「未验证」、不登「已知不支持」） |

无一条判为「标准本身过期」，故无交裁决方处置的标准腐坏项。

## 2. §4 五项独立复核（裁决方自查不算验收，以下独立复算）

### 2.1 §4.1 冻结区逐字节

方法：按 `## ` 顶层标题切段，对 §5 / §7 / §12 / §13 / §14 / 附录 A / 附录 B 各段，用同一函数分别取 `master` 与 `c501c723` 的 blob、去尾换行后 `sha256sum` 比对（附录 B 取到 EOF，含 ①–⑨ 模板）。结论可复现。

结论：**通过**。七节按 `## ` 顶层标题切段（附B 因内含 `## ①`–`## ⑨` 模板，改取「标题→EOF」以免在内嵌标题处早退），master 与 `c501c723` 同法 sha256 逐一相同：

| 冻结节 | sha256（前 12，master＝freeze） | 行数 | 结论 |
| --- | --- | --- | --- |
| §5 | `6445745b75ed` | 129 | IDENTICAL |
| §7 | `d1a07a8bc82e` | 224 | IDENTICAL |
| §12 | `aa0b566a981c` | 21 | IDENTICAL |
| §13 | `059049074b6a` | 37 | IDENTICAL |
| §14 | `9e20bbe6f244` | 40 | IDENTICAL |
| 附录 A | `8a38203ebe40` | 25 | IDENTICAL |
| 附录 B（含 ①–⑨） | `8a23f30489e2` | 59 | IDENTICAL |

复跑（附B 用标题→EOF；其余节用「命中即起、遇下一 `## ` 即止」）：

```bash
A=sunmoonai/docs/dev-plan/working/development-lifecycle-agent.md
secEOF(){ git show "$1:$A" | awk -v p="$2" 'index($0,p)==1{f=1} f{print}'; }
secEOF master "## 附录 B" | sha256sum   # == secEOF c501c723 "## 附录 B"
```

行数链：master 1161 → 基座 luna 1439 → 冻结稿 1603，增量全落在冻结节之外。

### 2.2 §4.2 处置记录与实际 diff 对账

方法：`git log --oneline master..c501c723`（25 提交，一主张一提交）逐条对照处置记录 §8（③ 整合）与 §10（④ 异议）清单；三查——「写了接受、diff 里没有」→ 不通过；「diff 里有、处置没登记」→ 不通过；「部分接受」是否写清接受到哪、为何止于此。

结论：**通过**。`master..c501c723` 共 25 提交，逐条对账：

- **构成**：11 个 ④ 异议轮过程提交（`b20607f1` 发起 … `c501c723` ④ 处置）＋ 1 个 ③ 处置记录（`f6937446`）＋ 13 个 ③ 整合提交（`ec575636` 取基座 … `e9307d51` §11.3）。11＋1＋13＝25 ✓。
- **一主张一提交**：处置记录 §8「整合提交清单」13 行与 13 个整合提交一一对应（`ec575636`／`fdaa6659`／`6962add8`／`616cd9e1`／`8260ef6e`／`c74af02a`／`0fe5ec2f`／`12e1aa51`／`b78d2d9e`／`1d0dc706`／`15cabea7`／`b2af5a44`／`e9307d51`）；④ 六条异议各对应提交见 §2.5。
- **三查**：① 处置记「接受／部分接受」的主张，均能在 §8 或 §10 找到落点提交，并在冻结稿对应节核到落稿文字（D-C16→头部、D-C9→§6.9、D-C12→§11.3、D-C17/D-C20→§0.3、D-K2/D-Q1→§4.5、D-Q3/D-K1/O-L1→§8.1 等，已随 §3／§2.3／§2.5 抽核）；② 未见「diff 有、处置未登记」的孤儿改动（13 整合提交全在 §8 表内）；③「部分接受」四条均写清止于何处——D-Q3（只并入 dsh 侧 `ctx.tools.guard()`，删 `profiles.py` 锚点）、D-K3（删「失败退回自研 LangGraph」）、D-C20（中文留「调度监督器」、英文采 `TaskRouter`、不采 `Dispatcher`）、D-L4（只并入「句柄可序列化落 PostgreSQL」纪律、不并入字段清单，守 §10）。
- **一处时点提示（非缺陷、非异议）**：处置记录 §9 自查写「整合稿 1593 行」，冻结稿为 1603 行——§9 是 ③ 收口时点快照，④ 异议轮又加 10 行（O-L1..L5＋D-K8）至 1603；按时间戳读不矛盾。

### 2.3 §4.3 有没有把「未验证」写成「已支持」

方法：核 §8.1 矩阵三档判定 + §11.3 未验证集中清单 + 本轮新加三档对应表（`available→已支持`、`implicit_fallback→需补法`、`explicit_unsupported→当前缺失`）；确认 `F-EXEC-02` dsh 腿（④ 经 O-L1 二次改判 `implicit_fallback`）与对应表自洽。

结论：**通过**。

- 三档对应表(1143-1145)：`available→已支持`、`implicit_fallback→需补法`、`explicit_unsupported→当前缺失`，与 §4.7 三态探针定义(492-494)一致。
- `F-EXEC-02` dsh 腿(1155)＝`implicit_fallback`（④ 经 O-L1 二次改判）：正文「`sessionId`…只是来源标签，不自动完成 Attempt↔根 session 唯一 binding…逐 Turn completion correlation 另属上游门禁 G3」——按对应表读作「需补法」，自洽 ✓；未写成「已支持」。
- 反污染条款(1147)：「不得把 SDK 原语冒充产品能力：`implicit_fallback` 被读成『已支持』正是 09-03 那次」——显式禁止「未验证→已支持」的塌缩。
- §11.3(1402) 只登记「未验证」、不登记「已知不支持」（后者在 §8.1 矩阵，1405 明写），两类不混。

### 2.4 §4.4 锚点出处正确性（本次重点）

方法：两类出处错各抽查——**跨仓错位**（标注为 Codex/Harness/OpenClaw 能力的锚点，路径须确在对应外部仓 `~/repo/<name>`）；**编号出处**（裸 `I*` 引用须写明出处文档，因 `constraints.md` 是 I1–I8、`request-lifecycle.md` 是 I1–I15，区间重叠）。抽样方式：从冻结稿正则提取全部 `file:line` 锚点与裸 `I*`，按仓分组各抽代表项亲验路径存在性与行号在界内，并专门回扫裁决稿有无重犯 `profiles.py`／`constraints.md I12` 两类错。

结论：**通过**。抽样方式：从冻结稿正则提取全部 `file:line` 锚点，按仓分组，每组抽代表项亲验「路径在对应仓存在＋行号在文件界内」；再对本轮暴露的两类错做定向回扫。抽验 24 处，全部 OK。

**类一 · 跨仓错位**（标注为某 SDK 能力的锚点，路径须确在该外部仓）：

- Codex（`~/repo/codex`）：`client.py:773`(864L)、`client.py:430`、`api.py:544`(811L)、`async_client.py:161`(378L)、`model-provider-info/src/lib.rs:57`(636L)
- Harness（`~/repo/deepseek-harness`）：`adding-a-tool.md:59`(101L)、`preset/README.md:12`(53L)、`protocol/README.md:52`(126L)、`protocol/src/types.ts:66`(119L)、`sdk-runtime/README.md:5`(36L)、`client.py:94`(589L)
- OpenClaw（`~/repo/openclaw`）：`config-tools.md:127`(938L)、`why-openclaw.md:121`(264L)、`selection.ts:102`(1021L)、`resolve-route.ts:57`(898L)、`VISION.md:129`(144L)
- 部署（本仓 `~/master/k8s`，非外部 SDK）：`20-runtime.yaml:283`(795L)、`00-prerequisites.yaml:106`(201L)、`30-network-policies.yaml:221`(296L)

定向回扫我 D-Q3 那类错：冻结稿 `profiles.py`／`permits_tool` **仅 1 处命中**(1150)，且明确作**反面实例**——「某候选以本仓休眠的 `AgentProfile.permits_tool` 佐证 Harness 腿工具门」；未拿本仓代码当租用 SDK 的能力证据，D-Q3 纠正已落实。

**类二 · 编号出处**（裸 `I*` 须写明出处文档）：

- 头部(24-26)立分写纪律：`constraints.md` 是 `I1`–`I8`、`request-lifecycle.md` 是 `I1`–`I15`，「写『constraints I8』『产品 I12』，不写裸 `I3`」，并点名「本轮已有候选把产品 `I12` 误标为 `constraints.md`」——正是我 D-C5 的错，被固化为纪律。
- 全部 `constraints.md` 引用（24/53/59/106/391/412/656/875/1372/1501）均指向 A/C/D/T 规则，**无一处**把 `I9`–`I15` 挂到 `constraints.md`。
- 抽验裸 `I*`：543「锚定 constraints I8、产品 I12」正确区分两套；91/105/392/480/515/570/600-603/874/1155/1157/1159 的 `I*` 均在头部全局归属（`I1–I15`＝`request-lifecycle.md`）覆盖下，无孤立误标。

**重点自陈**：这两类正是 R2 点名要我正面接住的弱项。抽验范围内未见裁决稿重犯；裁决稿反把我的两处错分别固化为「反面实例」与「编号分写纪律」，出处链干净。

### 2.5 §4.5 ④ 六条异议是否真的落到稿上

方法：逐条核对「应落点 + 提交」——O-L1（§8.1，`f4ca8fd1`）、O-L2（§0.3，`7db445a8`）、O-L3（§4.5 Gate 0 第 3 条，`2ee77993`）、O-L4（§9.2，`44863162`）、O-L5（§4 标题，`2ee77993`）、qwen-1（处置记录 §5.3 D-Q6→D-K8，`cf57cb68`）；确认落点文字与提交改动量吻合。

结论：**通过**。六条逐条核到冻结稿落点行号（`c501c723`；末条落处置记录）：

| 异议 | 提交 | 落点（行） | 落稿文字（亲核） |
| --- | --- | --- | --- |
| O-L1 `F-EXEC-02` 改判 `implicit_fallback` | `f4ca8fd1` | §8.1 1155 | dsh 腿＝`implicit_fallback`；「逐 Turn correlation 另属上游门禁 G3」 |
| O-L2 §0.3 关闭模型直接选路 | `7db445a8` | §0.3 95-98 | 「模型不得决定路线…输出只是不具决策权的建议与证据…不得默认落通用档…模型节点不能拥有队列、权限或终态」 |
| O-L3 Gate 0 第 3 条按 constraints 拆 Worker | `2ee77993` | §4.5 412 | 「不得进入现有通用 Celery worker；若…满足 `constraints.md`『什么时候才拆出专用 Worker』的门槛，则拆专业 Worker…仍属同一 Backend 的运行角色」 |
| O-L4 超时改称审计结果/reason code、不新增状态 | `44863162` | §9.2 1249-1252 | 「超时是独立的审计结果与 reason code，不折成 `auto-deny`…落账时超时写 `approval_timeout`」（是 reason code，非新状态） |
| O-L5 §4 标题改「sandbox、Git 物化与执行器接入架构」 | `2ee77993` | §4 标题 306 | `## 4. sandbox、Git 物化与执行器接入架构` |
| qwen-1 D-Q6 改挂 D-K8 | `cf57cb68` | 处置记录 §5.3 114＋116-120 | `D-Q6`→`D-K8`、`qwen`→`kimi`＋出处更正（原条目划线保留，拒绝结论不变、只改归属） |

五条落裁决稿、一条落处置记录，与通知 §4.5 表「应落点＋提交」逐行吻合；O-L3 与 O-L5 同提交 `2ee77993`（一次改两处，合理）。

## 3. 异议（如有；不改裁决稿，交裁决方处置）

**验收结论：判定通过**（11 条标准全数通过、五项独立复核全数通过；详见第 1、2 栏）。按通知 §6，通过则进 ⑥，由项目所有者确认（⑥ 不设逾期默认）。

**异议：无。** 未发现需交裁决方处置的裁决错误：

- 我据证提出的 ④ 异议（D-Q6 出处误挂）已被采纳（`cf57cb68` 改挂 kimi、重编号 D-K8，原条目划线保留），本轮无遗留。
- R2 点名的我的两类弱项（跨仓错位、编号出处）在裁决稿中均未重犯，反被固化为纪律（§2.4）。
- 六条 ④ 异议全部忠实落稿（§2.5），冻结区逐字节未动（§2.1）。

**非异议的观察（不影响通过判定，供裁决方知悉；我不改稿）**：

1. 处置记录 §9 自查「整合稿 1593 行」是 ③ 收口时点数，冻结稿经 ④ 加 10 行为 1603；按时间戳读不矛盾，裁决方如愿意可在 §9 补一句时点标注。
2. 处置记录 §9 自查称「`AT-*` 13 个」，本验收在 §8.1 矩阵独立数得 12 个不同 `AT-*`（AT-05/06/07/08/09/11/12/13/14/15/20/21）；差额或来自矩阵外的个别引用，属计数口径差异，不构成 criterion 3 缺陷（编号锚定充分）。

## 4. 盲区自陈（`round-protocol.md`：验收亦是产出）

验收亦是产出，自陈盲区如下：

1. **未实跑**：全部判定基于文档与源码静态核对，未真起 Codex/Harness runtime、未跑 Gate 0 spike、未在内网 PyPI 取 wheel——`runtime-verified` 一档本验收无法触及，只核到 `defined`／`wired` 层的文档自洽与锚点存在性。
2. **§4.4 是抽验非全量**：从冻结稿提取的 `file:line` 锚点中抽 24 处亲验「路径存在＋行号在界内」，未逐字核对每处锚点的**语义**是否精确支撑其论断（行号在界内 ≠ 该行文字恰好证明该主张）；选取偏向三外部仓＋部署＋我的两类弱项，可能漏掉未抽样处的语义漂移。
3. **`AT-*` 语义未逐条核**：只数了 §8.1 的 `AT-*` 覆盖，未回 `request-lifecycle.md` 逐条核对每个 `AT-*` 编号原文语义与矩阵用义是否一致。
4. **未读其他验收/评审产物**：本轮除 ④ 三家异议原文外，未读任何别家评审或验收稿，避免从众；对「多家一致但都未取证」的情形，以自带 `file:line`／可复跑命令者为凭。
5. **利益关联**：我是候选作者之一（qwen，环节① 冻结 `0c6f0fc3`），且是本轮两处取证被纠正的家。R2 已登记此代价并要求我正面接住 §4.4——我已把 §4.4 列为最重抽查项、并定向回扫自己的两类错，但「由被纠正者验收纠正了自己的稿」本身仍是一层需裁决方知悉的利益关联。我的候选／评审／异议在验收期间保持冻结未动（§0 表三行 sha 复核一致：`ea2bb7ab…`／`9fdce417…`／`e222d84c…`）。
6. **§4.1 方法的前提**：「逐字节一致」依赖按 `## ` 顶层标题切段；附B 因内含 `## ①`–`## ⑨` 模板须特殊处理（标题→EOF），否则会像本次初算那样在 `## ②` 处早退、只比到 5 行。七节 sha 全等已排除漏检，但此法对「冻结节内部藏改动、而切段边界恰未覆盖」在理论上敏感——已用附B 的 EOF 取法堵住该口。
