# 获准继续本机 KIND 身份切换

本次「允许」回应上一轮明确的询问：在备份恢复验证后，为 Info、Knowledge、Investment
切换独立数据库和消息队列账号、更新对应 Secret，并在验证后撤销旧共享账号的权限；
不删除业务数据、不清空队列、不影响其它应用。详细对象沿用
[上一轮切换范围](../../0011-none/others/identity-cutover-scope.md)。
只操作本机 kind-kind，不部署云端；维护窗口和唯一 codex-smoke 摄入绑定授权保留。

起点：k8s luna/master/两端工位 `ea2ef9a71e5f853873dc98cb51a1386ecc9e0cf0`；
四 App 及十二镜像沿用上一轮固定候选，不重新构建或覆盖正式别名。
执行前重新核集群 UID、实际配置、数据版本与备份；批准是 reported，不伪造 attested。

工作单元：真实备份/隔离恢复→分角色供给及撤权/恢复演练→固定发布 bundle→
维护窗口内 Info→Knowledge→Investment 串行迁移/验证→提交、同步、运行状态交回。
每单元失败停止后续切换；不以清账、跳检查或恢复宽权限制造通过。
恢复材料保存于 Git 外私有目录，公开证据不含凭据/业务正文。

约束沿用 D1–D9、I3/I8、T3–T5、R1–R7：独立数据域、独立迁移 Job、不复用身份、
固定源码与 digest、备份可恢复、模板已验策略优先、子仓提交先可达，不修改历史正式锁。
当前实测 kube-system UID 为 `5d71ab3a-ea5a-4535-adc6-d7698d820249`，数据库为 PG 17.6，
三 App 原运行副本均 ready；此前误按 16+ 泛称不影响版本要求，恢复使用相同 PG 镜像。
