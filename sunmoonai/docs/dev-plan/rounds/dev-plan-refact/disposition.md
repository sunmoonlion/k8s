# ③ 裁决 · 处置记录 ｜ `dev-plan-refact` 轮

> 裁决方 / 整合方 **cursor**（R4、R6）。基座 **luna**（R5）。
> ⚠ 裁决方同时是 ①② 参赛方。出处为 `cursor` 的条目，裁定栏标 **（自处置）**。
> 未使用 `artifact-lifecycle-notes.md` 作处置理由（该文不是本轮判据）。

票数不是依据。五家排序是四个不同答案（opus 推 cursor、luna 推 luna、kimi/qwen 推 opus、
cursor 推 luna）。基座由 R5 已定，不是本记录重选。

---

## 一、基座与四件必须正面处理的事

基座从 commit 取，不从工作区取：

```text
git show dev-plan-refact/luna:sunmoonai/docs/dev-plan/pipeline.md
# 213 行  sha256 前 16 位 781dcc882b9fd80e   落盘提交 `1ebf35e8`
git show dev-plan-refact/luna:sunmoonai/docs/dev-plan/dev-plan-architecture.md
# 733 行  sha256 前 16 位 4d4349400d971a6e
```

| # | 事项 | 裁定 | 理由（可独立成立） |
| --- | --- | --- | --- |
| 1 | Q3 `protocol/` 落哪一格 | **部分接受** luna 的附件/调用关系；**部分接受** 异议四家的类型划分 | luna 反驳成立：逻辑上的实施附件与物理独立文件可同时成立；仅证明不是同一种内容，不足以推翻调用关系。异议四家举证也成立：生命周期、读者时刻、内容种类与 `implementation-plan.md` 不同，故类型上标横切常驻，不并进 IMPL 目录树。接受到「SDP 引用其版本、规程不过期」为止；止于「写成实施计划的一章」。提交 `a442b12f` |
| 2 | luna L1 预览≠完成编辑 | **接受** | 基座把源正文逐节接入栏目、保留旧标题与旧相对链接。整合已把正式标题钉在栏目列，旧 `§N` 只作源坐标，失效自指改读 `Ixx-xxx`。不能只交生成器。提交 `9df55316` |
| 3 | opus 迁移表（F-15） | **拒绝** | `readiness.md:339` 引的 `request-lifecycle.md:450` 是 §6.1 的 `I3`，不是 `#4.2 WAITING`。表只有两行，错了一半。吸收锚点分类，不吸收该改写。提交 `9dd87650` |
| 4 | luna K1（round-status 超称） | **接受指控**；**拒绝** kimi 原文表述 | 在 `~/master/k8s` 读 `round-status.py`：`parse_round` 解析工单配置，`stage_table` 查产物路径是否作为 git 提交出现，`verify` 查冻结区/处置对账/锚点。没有普通工作单元字段齐全检查，也没有 260 行落点检查。kimi pipeline L127/L131 超称。cursor ① 把该脚本限于「轮次产物在不在 / `--verify` 能判的部分」，不在超称范围内。提交 `bfd46c40` |

B2 活跃裸行号锚（采纳相关表述前已复跑，2026-09-08，`~/master/k8s`）：排除 `archive/` 与历史 `rounds/` 后，活文档裸锚确认 `agent-dev-guide.md:2765`（`:627`）；本轮 `inputs/readiness.md:339`（`:450`）是第二处。本轮仍不拆内核——理由是预算与「拆了更好」未被证明，不是 71 处不可保全。提交 `3831034e`。

---

## 二、处置表（机器可读；验收方由此算出）

格式：`出处家｜条目｜裁定`。裁定 ∈ {接受, 部分接受, 拒绝}。
出处为 `cursor` 的条目，裁定栏后加 **（自处置）**。

| 出处家 | 条目 | 裁定 |
| --- | --- | --- |
| luna | 每个阶段写「未通过时往哪回」 | 接受 |
| luna | 三个完整走查例 | 接受 |
| luna | 可跑的决定索引器 `round:<id>:R<n>` | 接受 |
| luna | 机器结果字段 schema（缺输入记 UNKNOWN） | 接受 |
| luna | 阶段拆分对应一种失败、批准对象或返工入口 | 接受 |
| luna | render/verify 证明落点表不是表格造假 | 接受 |
| luna | 本轮内核逐字节不拆 | 接受 |
| luna | Q3：protocol 是 SDP 引用的执行附件 | 部分接受 |
| luna | L1 预览不等于完成编辑 | 接受 |
| luna | L2 handoff 文档面待办应迁计划 | 接受 |
| luna | L3 可读映射为人的审阅面 | 接受 |
| luna | K1 round-status 说法超出实现 | 接受 |
| opus | 两条流水线 A 产品 / B 决策，关系是调用 | 接受 |
| opus | 三问归属判据 | 接受 |
| opus | B2 活跃裸锚是 2 处不是 71 | 接受 |
| opus | DEC 类：不走轮次的所有者拍板 | 接受 |
| opus | 声称机器执行必须有非平凡命令 | 接受 |
| opus | F-9–F-12 判据自身必须先被验证 | 部分接受 |
| opus | ①b 两行迁移表（:450 → WAITING） | 拒绝 |
| opus | 本轮物理拆内核 | 拒绝 |
| opus | 按目录立 ROUND 类型 | 拒绝 |
| kimi | 双向检验：删阶段则类无来源，删类则产物无落点 | 接受 |
| kimi | 对 inputs/ 的装得下 / 装不下表（B14） | 接受 |
| kimi | protocol 不是实施计划：生命周期/读者/内容种类 | 部分接受 |
| kimi | Q8 落 `docs/evidence/<task-id>/`，T2 仍落 rounds/ | 部分接受 |
| kimi | Q9 索引两个方向都能失败 | 接受 |
| kimi | P-投影 / P-自描：规则随本体，投影有权威指针 | 接受 |
| kimi | handoff D1/D2 任务本体迁实施计划 | 接受 |
| kimi | 决定索引文件 `rounds/decisions-index.md` | 部分接受 |
| kimi | S3/S6 把工单字段齐全、落点齐全归 round-status | 拒绝 |
| cursor | 常驻面不是阶段 | 接受（自处置） |
| cursor | 操作手册从 AI 冷启动推出，不是 TLD | 部分接受（自处置） |
| cursor | 判据归属第三档：只能纪律且必须永远 ⚠ | 接受（自处置） |
| cursor | 类型按内容走，不按目录走 | 接受（自处置） |
| cursor | 每类问「缺它哪个阶段无过关物 / 为何不并邻居」 | 部分接受（自处置） |
| cursor | 有意留白是已决定的范围边界 | 接受（自处置） |
| cursor | protocol ≠ 实施计划的一部分（四条举证） | 部分接受（自处置） |
| cursor | Q8 槽 `working/traces/` | 拒绝（自处置） |
| cursor | 用 G1/G2 替换 luna 的 DEV/G0–G6 | 部分接受（自处置） |
| cursor | 寄居者标记 + 分阶段迁移 | 部分接受（自处置） |
| cursor | B2 四步迁移方案（本轮执行） | 部分接受（自处置） |
| cursor | 并行轮是同一条 Change 的加厚形态 | 接受（自处置） |
| qwen | X1 协作协议横切 | 接受 |
| qwen | 合同含 TLD 渗透节，本轮不拆、引用不复制 | 接受 |
| qwen | 发布后运行反馈与契约旧版并行 | 部分接受 |
| qwen | 稳定落点编码 `REQ-05.3` | 部分接受 |
| qwen | 约束委员会 | 拒绝 |
| qwen | DoD「已记录或已获授权」析取 | 拒绝 |

### 部分接受接到哪、为什么止于此

| 条目 | 接到哪 | 为什么止于此 |
| --- | --- | --- |
| luna Q3 / kimi 三条 / cursor 四条举证 / qwen X1 | 调用附件 + 横切常驻 + 物理独立 | 不停在「不是同一种内容所以不能附件」（luna 反驳挡住）；也不把 protocol 写进 IMPL 目录 |
| opus F-9–F-12 | 立判据的人必须能复跑、测量对象不得算进结果 | 本轮不改 `constraints.md` / 协议（B1） |
| kimi Q8 | 产品仓 `docs/evidence/` 原地；轮外工作 `records/work/`；拍板 DEC | 不合并成一只袋子，也不新增 `working/traces/` |
| kimi decisions-index | 双失败检查并进 luna 已有检索器 | 不另建一份与检索器并行的索引文件 |
| cursor 操作手册 | guide 只装「每一次怎么执行」 | 不另立第十二类取代 luna 的十四目标 |
| cursor 类型两问 | 用来压缩十四目标的必要性 | 不另造一套 12 类分类 |
| cursor G1/G2 | 常驻面承担「操作系统」；两道总门可作为阅读摘要 | 不替换 DEV/G0–G6 编号（与 guide 运行时 G0–G5 相邻的问题 luna 已声明区分） |
| cursor 寄居者+分阶段迁移 | 本轮预览重组、不发布物理搬家 | 不当场切开 guide |
| cursor B2 四步 | 将来若动内核，作为迁移预案 | 本轮不执行 |
| qwen 运行反馈 | luna S6 已有运行验收与维护接续 | 不另加 S7 阶段 |
| qwen 稳定 ID | luna `Ixx-xxx` 已是与行号分离的地址 | 不改成 `REQ-05.3` 第二套编号（luna 已因 Q9 撞车语境被扣过「多造一套」） |

### 拒绝的独立理由

| 条目 | 为什么拒绝 |
| --- | --- |
| opus 迁移表 | F-15 实错，被指方自认；错向改写不能当保全方案 |
| opus 拆内核 | 成本降低 ≠ 拆了更好；四家「不拆」有独立理由（实现者要同时打开三份、章已类型化）。本轮预算也不覆盖正式引用切换 |
| opus ROUND 类型 | `rounds/` 四种生命周期不能共用一条规则 |
| kimi round-status 超称 | 脚本无该能力，见 §一 第 4 行 |
| cursor `working/traces/`（自处置） | 会变成第三只取证袋；kimi/luna 已有槽 |
| qwen 约束委员会 | 主线 `grep -rn '约束委员会' sunmoonai/docs --include='*.md'` 在本整合读取的冻结输入里不是既有角色；本轮不发明授权主体 |
| qwen DoD 析取 | 未授权动作只因留记录而算完成，与权力表和「技术可写不等于动作获批」冲突 |

---

## 三、验收方计算（协议 §13，逐条适用）

1. **不得是裁决方 / 整合方** → 排除 `cursor`（R4/R6：本轮两条规则同时指向它）；
2. **不得是基座作者** → 排除 `luna`；
3. 在余下的家里取 **「接受 + 部分接受」条数最少** 的一家：

| 家 | 接受 | 部分接受 | 拒绝 | **计入合计** |
| --- | --- | --- | --- | --- |
| `opus` | 5 | 1 | 3 | **6** |
| `kimi` | 5 | 3 | 1 | **8** |
| `qwen` | 2 | 2 | 2 | **4** ← 最少 |
| `cursor` | 5 | 6 | 1 | 11（排除，不参与第 3 步） |
| `luna` | 11 | 1 | 0 | 12（排除，不参与第 3 步） |

cursor 行含（自处置），仅供 ⑤⑥ `grep`，不进入第 3 步。

> **验收方 = `qwen`。**

并列未发生。名次与验收资格无关。指定结果与计算过程已写入本记录，不得事后更换。

⚠ 机制后果（R6）：本表决定谁来审整合方。上表按主张本身裁，不按「让谁验收」裁。
qwen 计入最少，是因为它可吸收的独家主张本来就少，两条实质缺陷（约束委员会、DoD 析取）被拒绝。

---

## 四、提交登记（`round-status.py --verify` 对账用）

`verify` 只对首条最终路径 `pipeline.md` 做 `master..<裁决方>` 提交→处置记录对账。
下列哈希必须能在本文被搜到。`8ed6348b` 是 ① 候选，已被 `1ebf35e8` 基座落盘覆盖，不是本环节主张。

| commit | 触及 pipeline.md 的主张 |
| --- | --- |
| `8ed6348b` | ① 候选（已被基座覆盖） |
| `1ebf35e8` | 基座落盘 |
| `d288f00c` | 常驻面不是阶段 |
| `4a6b8926` | 两条流水线 |
| `a442b12f` | Q3 |
| `e374a473` | 双向检验 |
| `359b9f8a` | B14 表 |
| `0dc706bb` | 非平凡命令 |
| `ae7a278a` | 判据第三档 |
| `bfd46c40` | K1 |
| `994d859f` | DEC |
| `1bf29e38` | 类型按内容 |
| `13b32634` | 操作手册边界 |
| `4d3cfc90` | 类型两问 |
| `553ff7a6` | 拒绝 qwen DoD 析取 |

architecture.md 上的其余主张提交（不进 pipeline 对账，仍一条一提）：
`ddc5383f` `55a5ed60` `3baef3bd` `9df55316` `88f70434` `a115dd5c` `3831034e` `9dd87650` `ba170f11` `6efd2a46` `2229a352` `237b6cff`。

---

## 五、覆盖声明

**查了：** luna 两份基座全文；五家评审 D 块；`call-③.md` 四件必答题；F-15 原文与 `request-lifecycle.md:450`；`round-status.py` 的 `parse_round` / `stage_table` / `verify`；活跃裸行号锚复跑（命令见 §一）。

**没查：** luna RUNNER 是否仍能对改过 I08-009 落点后的表做双向核（L3：审阅面是第八节表，不把生成器当发布物）；opus §8 的 58/44/49/10/8 全套命令（只复跑了活文档裸锚这一截）；`doc-gate.py --all` 以外的产品仓测试。

**没读作判据：** `artifact-lifecycle-notes.md`。
