apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: info-admin-frontend-pvc
  namespace: app-platform-dev
  labels:
    app: info-admin-frontend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
