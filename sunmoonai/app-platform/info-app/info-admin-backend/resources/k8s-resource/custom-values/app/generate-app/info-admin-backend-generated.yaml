apiVersion: apps/v1
kind: Deployment
metadata:
  name: info-admin-backend
  namespace: app-platform-dev
  labels:
    app: info-admin-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: info-admin-backend
  template:
    metadata:
      labels:
        app: info-admin-backend
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: info-admin-backend
        image: harbor.sunmoonai.com:30443/app-images/info-admin-backend:1.0.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: info-admin-backend-config
        - secretRef:
            name: info-admin-backend-secret
        - secretRef:
            name: info-admin-backend-postgresql-conn
        - secretRef:
            name: info-admin-backend-redis-conn
        - secretRef:
            name: info-admin-backend-mongodb-conn
        - configMapRef:
            name: info-admin-backend-s3
        - secretRef:
            name: info-admin-backend-s3
        - configMapRef:
            name: info-admin-backend-elasticsearch
        - secretRef:
            name: info-admin-backend-elasticsearch
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: /app/data
        - name: elasticsearch-ca
          mountPath: /var/run/secrets/sunmoonai/elasticsearch
          readOnly: true
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: info-admin-backend-pvc
      - name: elasticsearch-ca
        secret:
          secretName: info-admin-backend-elasticsearch
          items:
          - key: ca.crt
            path: ca.crt
---
apiVersion: v1
kind: Service
metadata:
  name: info-admin-backend
  namespace: app-platform-dev
  labels:
    app: info-admin-backend
spec:
  selector:
    app: info-admin-backend
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
