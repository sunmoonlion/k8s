# 继续本机受控身份切换的部署接入

承接 [上一轮检查点](../../0013-none/others/preparation.md)，k8s/luna 起点 `44c2880b`、
tpl-app/luna 起点 `9f6d003`，两工作区干净。四对象修复已经完成，不再重做 PUT。
授权仍为本机 KIND 三 App 维护、独立身份/Secret 切换、codex-smoke 既有绑定；
不清队列、不删业务数据、不重放历史任务、不部署云端。上轮准备提交尚未同步。

本单元先封住旧 broker helper/Helm 入口回写静态共享 definitions 的路径，
再使 Investment 开发 apply 只消费准备好的独立身份，不隐式供给旧账号、切换旧 LOGIN。
测试必须包含查询失败、标记缺失但新身份已存在、未知/损坏定义、正式路径未变；
读取失败不能当成“未接管”。保护是误操作门禁，不声称能阻止持有集群凭据者手工绕过。
后续仍需在线窄供给及实测、固定发布包、提交同步与串行维护切换，不能以字段门禁代替身份验收。

本轮继续到候选冻结与既有授权范围的提交同步：四 App 父仓只更新 development-source-lock，
不动组件源码；k8s 更新三个 development input/bundle/.conf，沿用已验证 Harbor digest。
Knowledge 同样改为只读验证检索绑定，避免开发 apply 隐式修改其它工作负载。
发布包须由源码锁可重复生成，三包 `.conf`/digest/head/独立 Secret 引用同时验证；
合并/同步仅快进、先查独有提交和脏树，不 force/realign。失败保留现场。
源代码同步可先完成，在线供给与业务切换未验过前不能报告部署成功。

规则 D1–D9/I3/I8/T3–T5/R1–R7 沿用：独立领域/运行身份、固定候选、独立 Migration Job、
真实备份/恢复、无业务数据清理，模板公共策略已先行验证，不复制为三套领域实现。
