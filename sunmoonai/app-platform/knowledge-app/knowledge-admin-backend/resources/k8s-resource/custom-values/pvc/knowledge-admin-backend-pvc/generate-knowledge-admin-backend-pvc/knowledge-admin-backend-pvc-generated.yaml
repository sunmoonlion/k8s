apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: knowledge-admin-backend-pvc
  namespace: app-platform-dev
  labels:
    app: knowledge-admin-backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
