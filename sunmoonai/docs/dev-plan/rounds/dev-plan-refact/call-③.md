# ③ 裁决 ｜ `dev-plan-refact` 轮

> 发出 2026-09-08 ｜ 裁决方 **项目所有者** ｜ 整合方 **cursor**（[`rulings.md`](rulings.md) R4）
> 基座 **luna**（R5）｜ 起草：opus（受所有者指定代写；opus 是参赛方，见 R2）
> ⚠ **本通知在本环节冻结。**

## ⚠ 本通知在哪读

你的工作区里没有它，它在主线上：

```bash
cat ~/master/k8s/sunmoonai/docs/dev-plan/rounds/dev-plan-refact/call-③.md
```

**不要把主线合并进你的分支。**

## 一句话

**以 `luna` 的两份候选为基座，逐条吸收其余四家值得并进的主张，产出两份裁决稿 + 一份处置记录。**

## 一、角色（本轮的分离比前轮更严，先读）

```text
裁决方   项目所有者（principal）   —— 定基座、逐条处置
整合方   cursor（你）             —— 在基座上落盘
基座作者 luna                    —— ≠ 你 ✓
验收方   本环节之后按协议 §13 算出 —— 排除基座作者与整合方
```

⚠ **所有者已将 ③ 的裁决权全权委托给你**（[`rulings.md`](rulings.md) **R6**）。
你**既定处置也落盘**，不是只起草。

⚠ 但请知道你手上是什么：前轮裁决方 opus 当轮**不参赛**；**你是参赛方**，
所以你会处置到**包括自己那份在内**的主张。协议 §12 规定 ④ 异议
「只发给被处置到的家」且「只能就自己那条主张的处置提异议」——
⇒ **你处置自己那部分时，④ 这道闸不生效。**剩下的只有 ⑤ 独立验收与 ⑥ 人确认。

**所有者知情后作出此决定。**因此对你只加两条**记录义务**（不限制你的裁定）：

1. 处置表中凡出处为 `cursor` 的条目，`裁定` 栏后加标记 **`（自处置）`**；
2. 附**验收方计算过程**：各家「接受＋部分接受」计数、排除了谁、为什么。

⚠ 第 2 条有一个你应当知道的机制：**验收方由你写的表算出**
（§13 取接受＋部分接受最少的一家，排除 `luna` 与 `cursor` 后是 `opus`/`kimi`/`qwen`）。
**你通过多接受谁、少接受谁即可影响谁来审你。**写出计算过程是为了让它可被复核。

## 二、取件：从 commit 取，不从工作区取

协议 §11：**「从选定候选的 commit 取基座，不从任何人的工作区文件取」**

```bash
cd ~/master/k8s
P=sunmoonai/docs/dev-plan
git show dev-plan-refact/luna:${P}/pipeline.md               # 213 行 781dcc882b9fd80e
git show dev-plan-refact/luna:${P}/dev-plan-architecture.md  # 733 行 4d4349400d971a6e
```

⚠ **两份都是基座**——本轮 `final_path` 是两条路径，裁决稿也必须是两份。

**输入全集**（已全部归档在轮目录，与各家分支逐字节对拍一致）：

| 类 | 路径 |
| --- | --- |
| 五家 ①a | `candidates/<家>/pipeline.md` |
| 五家 ①b | `candidates/<家>/dev-plan-architecture.md` |
| 五家 ② 评审 | `reviews/review-<家>.md` |
| 本轮裁定 | `rulings.md`（R1–R5） |
| 本轮发现 | `findings.md`（F-1 … F-16） |
| 共用输入 | `inventory.md`（260 节）、`inputs/`（⚠ 非基准） |

## 三、交什么

| # | 路径 | 内容 |
| --- | --- | --- |
| 1 | `sunmoonai/docs/dev-plan/pipeline.md` | 裁决稿 ①a：以 luna 为基座 |
| 2 | `sunmoonai/docs/dev-plan/dev-plan-architecture.md` | 裁决稿 ①b：同上 |
| 3 | `rounds/dev-plan-refact/disposition.md` | **处置记录** |

### 处置记录必须含机器可读表（协议 §11）

三列固定，`裁定` 取三值之一：

```markdown
| 出处家 | 条目 | 裁定 |
| --- | --- | --- |
| kimi | D3 双向检验：删阶段则类无来源，删类则产物无落点 | 接受 |
| opus | ①a §4.4 DEC 类 | 部分接受 |
| qwen | ①a §5 约束委员会 | 拒绝 |
```

⚠ **验收方由这张表算出**（§13：取「接受＋部分接受」条数最少的一家）。表写错，验收方就算错。

### 你作出裁定本身（R6 全权委托）

- 逐条给出**裁定 + 理由**，理由须能独立成立，**不能只写「已处理」**；
- 部分接受要写清**接受到哪、为什么止于此**；
- ⚠ **凡出处是 `cursor` 的条目**，裁定照给，但 `裁定` 栏后标 **`（自处置）`**；
- 附验收方计算过程（各家计数、排除了谁、为什么）。

⚠ **三条不能省**（协议 §2.1，任何档位、任何委托都不能裁掉）：
不可逆动作由人确认（⑥ 由所有者显式做）· 出题方≠答题方（⑤ 按 §13 算出，排除 luna 与 cursor）·
验收判据先于产出冻结（⑤ 按任务书 §6 已冻结的 M1–M4 / J1–J7 判，**不按 ③ 的说法判**）。

## 四、五条硬约束

1. ⚠ **票数不是依据**（协议 §11）。五家的排序是 opus 1 / opus 1 / luna（cursor 推荐）/ luna（自荐）/ cursor（opus 推荐）——**四个不同答案，不得当作投票结果使用**。理由必须是**可复核的差异**。
2. ⚠ **一条主张一个提交**（§11），便于事后复核与回退。
3. ⚠ **不得改任何已冻结的候选或评审**（§2.1 第 4 条）。
4. ⚠ **零丢弃仍是底线**：基座是 luna 的 733 行 ①b，其落点表覆盖 260 节；吸收后仍须覆盖 260 节。
5. ⚠ **不得写主线**。裁决稿落在你自己的分支 `dev-plan-refact/cursor` 上。

## 五、必须正面处理的四件（②互评已暴露，不得回避）

| # | 事项 | 出处 |
| --- | --- | --- |
| 1 | **Q3 `protocol/` 落哪一格** | 4:1。luna（基座）**接受**所有者映射并给了反驳：「逻辑上的实施附件与物理独立文件可同时成立；仅证明不是同一种内容，**不足以推翻附件关系**」。opus/kimi/cursor/qwen 异议。⚠ **异议四家无人回应该反驳** |
| 2 | **luna 自认的 L1** | 「**预览不等于完成编辑**……不能只交生成器」。基座的 ①b 把源正文逐节接入栏目、保留旧标题与旧相对链接。⚠ **整合必须去除失效自指、统一标题与交叉链接** |
| 3 | **opus 的迁移表实错** | `findings.md` **F-15**：`readiness.md:339` 引的 `request-lifecycle.md:450` 是 §6.1 的 `I3`，不是 `#4.2 WAITING`。⚠ 迁移表只有两行，**错了一半**；被指方已自认。吸收 opus 的锚点分类时**不得连带吸收该迁移表** |
| 4 | **luna 的 K1 指控（未第三方复核）** | 称 kimi/cursor 把 `round-status` 的能力说超了实现。⚠ 涉及 J1 判据归属，**若采纳相关表述须先复核** |

## 六、⚠ 提请注意：本轮出现了一套事后框架

② 期间所有者与 opus 讨论出一套「节点状态机 / 真源 / 投影」框架，
记录在 `sunmoonai/docs/artifact-lifecycle-notes.md`（1012 行）。

⚠ **它不是本轮判据。**本轮判据是任务书 §6 已冻结的 M1–M4、J1–J7，其中没有该框架任何一条。
协议明禁用事后出现的尺子追评已冻结的候选。

**你不得用它作为处置理由。**它与本轮的关系由所有者决定（该文 §26.5 已列为未决第 14 条）。

## 七、交卷

```bash
W=~/worktrees/cursor/k8s
git -C $W add sunmoonai/docs/dev-plan/pipeline.md \
              sunmoonai/docs/dev-plan/dev-plan-architecture.md \
              sunmoonai/docs/dev-plan/rounds/dev-plan-refact/disposition.md
git -C $W commit -m "③ dev-plan-refact 裁决稿与处置提案（cursor 整合，基座 luna）"

( cd ~/master/k8s && python3 sunmoonai/docs/dev-plan/protocol/round-status.py )
git -C ~/master/k8s status --porcelain    # 自证没写主线，应为空
```

## 八、卡住了就停下来说

⚠ 所有者开着窗口。判据矛盾、输入不存在、要动的东西超出「只写自己工作区」、
命令失败而你不确定为什么——**停下来说，不要猜着往下走**（`GO.md` §五）。

尤其是 §三 那条分工（提案 vs 裁决）**尚待所有者确认**，若你认为它不可执行，**先问**。
