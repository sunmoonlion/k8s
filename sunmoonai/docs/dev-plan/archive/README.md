# `archive/` —— 不参与当前工作的历史件

> 建于 2026-09-07。本目录**不参与任何轮次**：不得作为候选、评审或验收的取件对象，
> 也不计入任何落点表。**只可确认其存在，不得引用其内容。**

## 目录里有两类东西，语义不同，别混

### 甲、降级前的逐字节原稿备份（2 份）

| 文件 | 是什么 |
| --- | --- |
| `refact-fable.md` | `../refact-fable.md` 在 `e08d89cd` 时刻的**逐字节副本** |
| `runtime-architecture.md` | `../runtime-architecture.md` 在 `e08d89cd` 时刻的**逐字节副本** |

由来是所有者 2026-09-07 的指示：**「copy 一份原稿备份待转，而现在的不变」**。

⚠ **这两份与 `../` 下的同名文件不是重复件。** `../` 下的两份在 `R10` 之后
加了「历史档案，不再有规范效力」的页眉（各多 7 行），本目录的副本**没有加**——
加了就不再是原稿备份。两者的差异仅此页眉与随之更新的一条链接。

**`../` 下的两份仍是活文档，参与重构；本目录的两份不参与。**

### 乙、已被取代的旧稿（2 份）

| 文件 | 被谁取代 |
| --- | --- |
| `development-lifecycle-agent.md`（1604 行） | `../agent-dev-guide.md`（`runtime-refact` 轮定稿） |
| `development-lifecycle-human.md`（823 行） | 同上 |

2026-09-07 由 `working/` 移入（提交 `08df41df`）。移入时改写了 8 处外部链接。

## R9 / R10 已裁定

本文件早先版本写着「源稿的规范效力尚待裁定，在裁定之前不得断言源稿已作废」。
**该问题已经关闭**：`rounds/runtime-refact/rulings.md` `R9` 定源稿为历史档案，
`R10` 据此在 `../` 的两份上加了降级页眉。此处保留这段记录，是为了说明
本目录的副本为何没有那个页眉——它冻结在裁定之前。

## 本目录的存续条件

副本的用途是所有者说的**「待转」**：需要一个可以直接改的工作底本，而不是只读的
git 历史。**如果哪天不再需要改造，本目录的甲类应当删除**，回到「历史在 git 里」。
乙类是另一回事——旧稿有 40 份文档的历史引用，删除前须过迁移闸。
