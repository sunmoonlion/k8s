apiVersion: apps/v1
kind: Deployment
metadata:
  name: tools-admin-backend
  namespace: app-platform-dev
  labels:
    app: tools-admin-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tools-admin-backend
  template:
    metadata:
      labels:
        app: tools-admin-backend
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: tools-admin-backend
        image: harbor.sunmoonai.com:30443/app-images/tools-admin-backend:1.0.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: tools-admin-backend-config
        - secretRef:
            name: tools-admin-backend-secret
        - secretRef:
            name: tools-admin-backend-postgresql-conn
        - secretRef:
            name: tools-admin-backend-redis-conn
        - secretRef:
            name: tools-admin-backend-mongodb-conn
        - configMapRef:
            name: tools-admin-backend-s3
        - secretRef:
            name: tools-admin-backend-s3
        - configMapRef:
            name: tools-admin-backend-elasticsearch
        - secretRef:
            name: tools-admin-backend-elasticsearch
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: /app/data
        - name: elasticsearch-ca
          mountPath: /var/run/secrets/sunmoonai/elasticsearch
          readOnly: true
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: tools-admin-backend-pvc
      - name: elasticsearch-ca
        secret:
          secretName: tools-admin-backend-elasticsearch
          items:
          - key: ca.crt
            path: ca.crt
---
apiVersion: v1
kind: Service
metadata:
  name: tools-admin-backend
  namespace: app-platform-dev
  labels:
    app: tools-admin-backend
spec:
  selector:
    app: tools-admin-backend
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
