# `development-lifecycle-agent-new.md` 整合记录

> 整合日期：2026-09-02 · 整合方：**opus** · 分支：`integrate/2026-09-02`
>
> 基座：**luna** commit `530a5a8de335f9fd73ec15a3c0900334c5da35f5`，blob `13347913`，
> sha256 `6cdcfb85dc0d21f8…`（内容寻址核对通过，与 luna 自评所报一致）。
> **内容取自该 commit，不取自任何 worktree 的工作区文件。**
>
> 裁决依据：[`development-lifecycle-agent-new-decision.md`](development-lifecycle-agent-new-decision.md)。
>
> **利益冲突声明：整合方 opus 同时是裁决方和 M/F 改进清单的作者。**opus 未参加本轮候选，
> 因此不违反「候选方不得作为自己基线的最终整合方」；但「决策方兼整合方」意味着我在按
> 自己写的清单打分，这是本次整合的主要残余风险。缓解办法只有一个：下面每条都写明来源、
> 理由和可证伪点，供基座作者异议、供人终审。
>
> **状态：`INTEGRATED`，未 `VERIFIED`，未 `PUBLISHED`。**现行
> [`development-lifecycle-agent.md`](development-lifecycle-agent.md) **未被改动**——替换它是
> 发布动作，须有权主体批准（§7.7 的 publication target 与 §9 权力表）。
>
> **异议轮已跑（见 §7）**：F2 与 F5 判阻断、M3 部分异议，三条全部采纳并已修订。
> 异议方另发现本记录 §5 验收项 5 的行数过期（27→33），已修正。

## 1. 提交序列

一条主张一个提交，可逐条 revert 或复核：

| 提交 | 主张 | 处置 |
| --- | --- | --- |
| `7b9fd027` | 取 luna `530a5a8d` 为基座，逐字节相同 | 基线 |
| — | F1 标题去候选作者名 | 接受 |
| — | M1 载体表补未提交工作区文件 | 接受（源 cursor §5.2） |
| — | M2 + N3 写入前一票否决门禁 | 接受（源 cursor §3.1/§5 + qwen §5.3） |
| — | M4 场景表补恢复列 + 事故四步前门 | 接受（源 cursor §5.4 + kimi §7.3） |
| — | F2 拆开产品侧租约与开发侧冻结 commit | **部分接受**，见 §3.1 |
| — | M3a 补回 Investment App 范围门禁与四级词典 | 接受（源 cursor §4） |
| — | M3b + F5 新增 §14 项目实例 | 接受，写法按 §3.2 收窄 |
| — | N1 本仓具体落点 | 接受（源 cursor §5.3） |
| — | N2 + F4 反模式补五行 / 否决文件名律 | 接受 + **拒绝** qwen 律 3 |
| — | N4 handoff 单写者 | 接受（源 cursor §1.5/S20） |
| — | N5 有权主体权力表 | 接受（源 kimi §4） |
| — | N7 补 6 个高频场景 | 接受（源 cursor S2/S6/S7/S12/S22/S27） |
| — | N6 §11.1 生效边界 | 接受（源 kimi §12 末段） |
| — | F7 链接改可解析并跑存在性检查 | 接受 |
| — | 修 §7.6 重复列 | 缺陷修复 |
| — | **F2 修订**：异议轮后修三处自相矛盾 | 采纳异议 |
| — | **M3 修订**：项目门禁压成三条稳定路由 | 采纳异议 |
| — | **F5 修订**：§14 证据分三级并逐行重定级 | 采纳异议 + 连带自查 |
| — | 整合记录更新（本文件） | — |

（提交哈希以 `git log integrate/2026-09-02` 为准；本表按主张排序，便于对位。）

规模：908 → **1107** 行（异议轮后 M3 压缩抵掉一部分增量）。增量集中在 §5.1 写入前门禁、
§7.6 恢复列、§9 权力表、§11.1 生效边界、§14 项目实例。

## 2. 拒绝并入的内容（可复核理由）

| 来源 | 内容 | 拒绝理由 |
| --- | --- | --- |
| cursor §5.4 S1 | 「移走自己的稿后**删掉共享路径上的那份**」 | 共享路径已被覆盖时，当前内容未必属于本执行者，删除会造成**第二次数据损失**。统一走 §7.8「停写→保全→分流→溯源」，事故处理中不得破坏他人或用户的未提交内容。N7 并入 S12 时也据此改写为「撤出、改从 commit 取」，并加「撤出前先确认没有覆盖他人内容」 |
| qwen §5.2 律 3 | 「带助手标识的文件名」作为归属三律之一 | 已被现场证伪：三份不带后缀的同名文件在三棵 worktree 中共存无冲突（后缀非必要）；同一工作树内两人写同一带后缀的名照样覆盖（后缀非充分）。它会制造虚假安全感。§13 反模式已写反。qwen 自己的评审也已作同样的自我纠正 |
| cursor §0/§10、qwen §0.2/§10 | 引用 `working/collaboration.md`、`working/doc-conventions.md` | **两文件均已核对不存在**。前者被当作 P5 权威，后者被当作四条「项目实例」的证据出处。不并入死链，也不凭空补造证据；相关四条按 §14 降级为设计风险 |
| cursor §10 | 「写进主 checkout 的两路哈希相同」 | 共享路径现场已被清走，不可复核。§14 只记可证事实 |
| qwen 头部、§5.1、§10 | 具名归因「luna 覆盖 cursor」 | 同上。按 §7.8 第 4 步，mtime 与「最后一版」只作线索，不足以证明写入顺序或责任人 |
| kimi §1/§2 | 「复杂任务由 agent 在 sandbox 中构建 git 仓库」 | 建仓主体写反，会使 §4.4 物化门禁失去承载主体（供给方与 supervisor 合一）。见裁决书 §2.3 |

## 3. 对基座作者主张的改动（luna 可提异议）

下面三条**改的是 luna 自己的设计主张**，不是补缺口。按 §6.5「整合方不得默认接受优胜作者
自评」，我做了改动；按 §6.7，luna 可以解释设计但不审批自己的基线。异议请附理由，我逐条
记录接受/部分接受/拒绝。

> **§3 记录的是异议轮**之前**的状态，保留原样以便对照——异议轮的三条意见正是针对下面
> 这三节提出的。修订后的实际内容见 §7。**

### 3.1 F2 · 租约：部分接受 cursor 的主张

- **基座原文**：§8「迟到依据状态、租约/fencing、commit 和冻结时间判断」；「过期执行者的
  写入由 fencing 拒绝，不能只靠自律」。
- **改成**：两层并列——产品侧用租约/fencing（语义只引用 `request-lifecycle.md`），开发侧
  用冻结 commit + 候选状态 + 整合方显式核对，并明写「开发侧没有租约机制」。
- **理由**：分支、worktree 和文件路径上没有任何东西会自动拒绝过期执行者的写入。按原文
  执行会产生虚假安全感，进而省掉整合方的显式核对。
- **未全盘接受 cursor 的部分**：§9 权限公式保留 `∩ 有效租约`，只标注机制归属。cursor 主张
  本文不应出现租约，我判过头——删掉它会让有效权限的计算不完整，那确是产品侧的真实约束。
- **可证伪点**：若能指出开发侧存在一个会拒绝迟到写入的运行时机制，本条应回退。

### 3.2 M3 · 项目实例：接受，但这是路线改动

- **基座事实**：全文 0 处 `2026-08-27`、0 处 `investment`、0 处 `parallel-proposals`、
  0 处 `dormant`（已核对）。这是「写通用合同、不写项目实例」的路线选择，不是疏漏。
- **改成**：§5.2 补本项目范围门禁与四级能力词典，§6.4 补 `parallel-proposals.py` 验证边界，
  新增 §14 项目实例与设计风险两级表。
- **理由**：本文当前的读者是本仓执行者，而 human 那份将来要继承这些门禁。一旦两份文档
  同时不写，门禁就没有着落——而这正是本轮重写要解决的问题（§11.1）。反模式说明「这样
  会失败」，项目实例说明「这里已经失败过」，后者是纪律的效力来源。
- **luna 最可能的反驳**：通用合同不该绑单一项目。我的回应是分层——门禁写在 §5.2 通用路由
  之后并标明「本项目」，实例集中在 §14 且标注证据等级，两者都可随项目变化独立更新。

### 3.3 F5 · 事故写法：收窄

- 基座**没有**写 2026-09-02 实例，§14 是新增。我按可证事实写：多路并行重写同一
  `*-new.md`、共享 checkout 上后写覆盖先写且无冲突无历史、写在自己 worktree 的幸存、
  三份同名文件在三棵 worktree 共存。**不写谁覆盖了谁。**
- 这一条同时否决了 cursor 与 qwen 稿里更强的具名叙述。

## 4. 未做与部分做的（不掩盖）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| **F6 压缩 §7 与 §13 重复** | **部分做** | 逐行看过后判定不做大幅压缩：§7 是场景处置，§13 是一句话反模式清单，两者是同一事实的两个检索入口，不是冗余。本次反而给 §13 增了 5 行（N2）。全文因此比基座长 175 行。若你认为篇幅需要压，这是可动的地方，我不当作已完成 |
| **异议轮** | **已做** | 见 §7。三条主张全部被提出异议，全部采纳；另修正本记录一处过期数字 |
| **M3 内容的最终归宿** | **假设，未确认** | 我问过「补回的项目门禁最终该放 agent 那份还是 human 那份」，未得答复。**采用的假设**：先写进本文（本文必须自足），同时在 §11.1 写明人那份必须自足、这些内容将来要搬。若你决定直接写进 human 那份，§5.2 的门禁表和 §14 可整块移出，不影响本文其余部分 |
| **human 那份的 7 处悬空引用** | **未做，刻意** | 已核对 `development-lifecycle-human.md` 有 7 处引用 agent 那份（第 6、90、118、133、152、157、162 行）。解除它们是本轮之外单独的一件工作；本次未改人那份，五个 worktree 的 human.md 哈希仍全部相同（`138ac0b5…`） |
| **替换现行 `development-lifecycle-agent.md`** | **未做，待批准** | 见文首。`AGENTS.md` 指向的是现行那份，替换会立即改变所有执行者的入口，属发布动作 |
| **整合稿本身的独立验收** | **未做** | 我是整合方，不能给自己的整合做终审（§6.5）。下面 §5 给可判定的验收标准 |

## 5. 建议验收标准（供未参与方或人逐条判定）

| # | 标准 | 判定方式 |
| --- | --- | --- |
| 1 | 基座取自 commit 而非工作区文件 | `git show 7b9fd027:…` 与 `git show 530a5a8d:…` 逐字节相同 |
| 2 | 全部链接可解析 | 已跑：7 个目标全部存在；不含 `collaboration.md`、`doc-conventions.md` |
| 3 | 隔离主表述是 worktree + 命名分支，文件名后缀至多为二级防护 | §7.5 开头、§13 反模式 |
| 4 | 含「独立 worktree 改同一相对路径=安全、共用工作树=必须 owner namespace」的精确条件 | §0.4（基座原有，未改动） |
| 5 | 存在「已经覆盖时怎么恢复」的规程，且场景表带恢复列 | §7.6 恢复列 **33** 行（M4 补 27 行，N7 又增 6 行）+ §7.8 四步/八步 |
| 6 | 存在发布期并发竞争处置 | §7.7 CAS（基座原有，未改动） |
| 7 | 冲突优先序含 `constraints.md` | §0.1 权威表（基座原有） |
| 8 | 事故记录只含可证事实，无不可复核的具名归因 | §14 末段 |
| 9 | 无危险恢复动作（删除来源不明文件） | 全文搜「删」，§7.8 与 §7.6 恢复列 |
| 10 | `development-lifecycle-human.md` 字节未变 | 五处哈希仍为 `138ac0b5…` |
| 11 | 全文表格列数一致 | 已跑校验脚本，通过 |

## 6. 交有权主体裁决

1. **是否用本稿替换现行 `development-lifecycle-agent.md`**（发布动作，需批准）；
2. **M3 内容归宿**：留在本文，还是直接落到 human 那份（见 §4）；
3. ~~是否跑异议轮~~ —— **已跑，见 §7**，三条全部采纳；
4. **F6 篇幅**：1107 行是否需要压，压哪里（§4 已说明我判定 §7/§13 不是冗余）；
5. **本整合的独立验收方**：不应是 opus（整合方兼裁决方），也不应是 luna（基座作者）。

## 7. 异议轮结果与处置

异议方对 §3 的三条逐条提出意见：**F2 阻断、F5 阻断、M3 部分异议**。三条全部成立，
全部采纳。整合方（本人）的原判在三处都不够严，逐条记录：

| 主张 | 异议 | 处置 | 提交 |
| --- | --- | --- | --- |
| **F2** | ① 开发侧核对写成「commit 仍在**有效分支**上」，把有效性绑回可移动的 branch，与 §7.5「分支是运输通道、不是稳定评审对象」直接冲突；② `∩ 有效租约` 无条件成立，会让 Human 路径和不隶属任何 Attempt 的开发操作在公式上天然无权限；③ 产品侧 fencing 写成既成机制 | **全部接受。**①改为核对四项：候选状态仍有效、commit 仍可达、提出方未撤回、基线未失效，并写明「commit 还在某分支上」既不必要也不充分；②拆成两条权限公式，租约降为条件项；③标为 `request-lifecycle.md` 的**目标合同**，是否接线由代码证明 | `F2 修订` |
| **F5** | §14 定义「项目实例」为「留下可核对痕迹」，却断言共享 checkout 上确实发生了「后写覆盖先写」，同一段又承认现场已清走、写入顺序不可复核——自相矛盾 | **接受。**证据改为三级（已核对事实 / 事故报告 / 设计风险），2026-09-02 拆成三行：两行可复核事实、一行现场已灭失的事故报告，不再断言发生过哪次覆盖 | `F5 修订` |
| **M3** | 保留项目门禁合理（现行文档本就含这些规则，全删会造成替换回归），但通用路由表后又铺一张高度重叠的 Investment App 表，与本节「不复制项目实现」的声明矛盾；易变事实应只放 project-guide | **接受。**压成三条稳定路由（项目入口 / 四级词典 / dormant 回跑），删掉与通用表重叠的 5 行；异议方明确接受的能力词典与验证入口全部保留 | `M3 修订` |
| 本记录 §5 验收项 5 | 「恢复列 27 行」过期，HEAD 已是 33 行 | **接受。**已改为 33，并注明 M4 补 27、N7 增 6 | 本次 |

### 7.1 异议轮的连带发现（超出异议范围，主动补做）

异议方对 F5 的标准同样适用于我未被质疑的 2026-08-27 三行——我当时标了「项目实例」却
没有真去核对。补做后发现 `merge-review/` 虽已被 `eb38868b` 删除，**内容可从 `eb38868b^`
取回**，于是逐条重查并重定级：

| 原写法 | 复核结果 | 改成 |
| --- | --- | --- |
| 评审给出的**八条**验收标准长期无人执行 | `disposition.md` 末节自记：cursor 那份「未回跑」、kimi 那份「待评审方执行」、luna 那份仅第 3 条通过 | **已核对事实**，但删去未经证实的「八条」 |
| **优胜作者独自拒绝改进** | 记录只支持「处置边界由被审方单方划定」，及一条门禁因「三台机器分别报 0 / 4 / 95 条失败」被判误报删除。更强的说法无记录支持 | 改写并降低强度，**不采用**原说法 |
| 评审引用旧提交、**主方已领先三个提交** | 原始记录中**未找到**对应条目 | 降为**事故报告** |

这三处是我自己犯的、异议方没点名的同类错误。记在这里，是因为「只改被点名的」会让证据
标准变成抽查合格制。

### 7.2 仍然开着的

- **独立验收未做**：整合方不能自审。§5 的 11 条标准中机械项已由异议方复核通过（基座
  SHA-256 一致、7 个链接存在、五份 human.md 哈希一致、表格列数一致、现行 agent 文未改）；
  判断项仍需未参与方或人逐条判定。
- **未发布**：现行 `development-lifecycle-agent.md` 仍未改动。
- **human 那份的 7 处悬空引用**：本轮之外的单独工作，未动。
