apiVersion: apps/v1
kind: Deployment
metadata:
  name: incubator-app-ssr
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: incubator-app-ssr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: incubator-app-ssr
  template:
    metadata:
      labels:
        app: incubator-app-ssr
    spec:
      imagePullSecrets:
      - name: ${INCUBATOR_SSR_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: incubator-app-ssr
        image: ${INCUBATOR_SSR_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-IfNotPresent}
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: incubator-app-ssr-config
        - secretRef:
            name: incubator-app-ssr-secret
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data-storage
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
          subPath: ${PVC_SUB_PATH:-}
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-incubator-app-ssr-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: incubator-app-ssr
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: incubator-app-ssr
spec:
  selector:
    app: incubator-app-ssr
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP

