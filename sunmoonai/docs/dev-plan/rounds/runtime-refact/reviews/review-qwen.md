# 评审：qwen ｜ `runtime-refact` 轮 ② 互评

> 作者：qwen。身份由 worktree 目录名判定（`~/worktrees/qwen/k8s`）。
> 对象：① 的四份候选，按 [call-②.md](../call-②.md) 表格取件，哈希已逐一复核（B 表）。
> **R2 遵守声明**：写本评审期间未读 `agent-dev-refact.md`（未打开、未 `git show`、未 grep），
> 亦不引用它。① 期间的遵守情况（含一名规划子代理可能见过该文件的不确定性）已在
> 候选 §7 覆盖声明里如实登记，此处不重复也不撤回。
> 评审自身也是产出，自陈盲区见文末「评审自身的覆盖声明」。

## A. 自述（关于我自己的候选）

我交的是 187 行的压缩稿（`runtime-refact/qwen:sunmoonai/docs/dev-plan/agent-dev-guide.md`，
commit `7d31265a`）。结构 8 节：权威来源与读法、dev.change 落法与角色/工作区、
原生中断恢复、证据与判定纪律、验收与成本边界、迁移删除与未决、覆盖声明、64 行落点表。

**写了什么**：单一权威面映射表；`dev.change` 作为 Task Profile 而非第二状态机；router /
orchestrator（确定性组件）与 arbiter / acceptor（角色）的分离；工作区独占与只读取件纪律；
基于 investment-app 代码锚（`pilot_graph.py:53-66`、`graph_runtime_service.py:13-46`、
`test_runtime_selection_spike.py:33-46`）的原生中断/恢复事实；证据四层表（自述/可重算/
进程观察/外部权威）；宿主权限事实的诚实降级（含本机 `id && sudo -n -l` 复跑）；
git 载体的限度；验收三层与「开发便宜、财务昂贵不可外推」边界；删除条件与未决清单。

**⚠ 与未验证**（候选 §7 已声明）：端到端恢复未落地（`resume()` 仍是 `NotImplementedError`）；
外部身份域未建立；本地强制点对 agent 无效的结论仅限本宿主本身份；财务 Task 验收未解决；
隔离无法事后证明。

**与其他三家的分歧，及我现在的判断**（这是本次互评对我最有价值的部分）：

1. **被证伪六条要不要专章保留（Q4）**：我选「不保留」，理由篇幅。cursor 用 §12 专章保留
   （理由：防止再犯），kimi 用 §1.1 核查表，luna 用 §8.1 K1–K6。**我改判：保留是对的。**
   任务书 §1.3 说「漏了就没了」——源稿删除后，六条的证伪命令若只留在 git 历史，
   新读者不会去翻；防止再犯需要证据基座在正文里。
2. **执行者登记表给不给取值**：我只保留「各执行者须单独取证」的要求，不列形状与取值，
   理由「推断不得进登记表」。kimi §5.2 / cursor §4.1 给了字段形状、把未核值留空或标 ⚠、
   并写明「取值以 agents.toml 为准」。**我改判：他们的对**——形状本身是规范，取值标 ⚠
   即可；我的做法把形状也丢了。
3. **trace 读数迁不迁**：我判为轮次档案不迁。kimi §6.6 / cursor §8.1 保留了
   「23 条中 attested 仅 2」的读数并升格出两条引用纪律。**我改判：该迁**——读数是
   「先在这一层跑通」的账本版证据，且从它长出的引用纪律（防张冠李戴、防凭空构造）
   有独立规范价值。
4. **工单要不要一般化**：我担心重写协议（J6），只写了一句「工单至少钉定……」。
   三家都把 `round.md` 一般化为按 tier 取表的工单 schema。**我改判：一般化不是重写**——
   协议管「轮次怎么走」，工单 schema 管「任何档位的任务契约形状」，真源不同；
   我的保守损失了 T0/T1 落地的抓手。
5. **H1–H8 权力表**：我没有迁，理由「权力与状态定义归内核/协议」。三家的做法是迁
   开发侧落法（谁批准什么、强制点在哪），并声明具体状态定义仍归内核。**我改判：这是
   过度让渡**——源稿的权力表本来就是开发场景投影，不是内核重定义。半漏。

**故意没写与漏写的区分**：五家取值表、T0 复合上界数字（理由：未经验证的数字不写——
cursor 的做法更好：保留数字并标「未经实测的设计值」）属取舍；独立性折算没写
（kimi §5.6 / cursor §4.4 有，或源自源稿而我未迁）属漏，不是取舍。

**对照任务书 §8 的自评**：

- **J3：4/6。** 第 1–2 条有我自己的代码锚；第 3 条有本机 `id && sudo -n -l` 复跑；
  第 6 条由第 3 条推导。第 4、5 条（agent 不 push / 发布是本地 merge）只援引
  `forensics.md:20-31/58-74`，未自跑 `git branch -vv` / `git ls-remote`，也未像 kimi 那样
  逐条标明「未独立复核」——按 F6 的严格读法，这两条计「未处置」。
- **J2：三处落点偏薄**（自查）：R2.8 trace 样例 → §4.1 只有方法没有读数；
  R4.2 五家取值 → §4.1/§7 只有要求没有取值表；R5.3 T0 上界 → §2.2/§5 只有原则没有
  复合向量。三处行文都写了「保留……要求/方法」，不算凭空声称，但按「该节确有对应内容」
  的标准只算部分满足。
- 机械条：M1（64 行）、M2、M7 通过；M4/M5/M6 词汇扫描零命中（见 C 块命令）；
  M3 两门禁在 ① 提交前自跑过，本轮未复跑。
- 一票否决：不触发。

## B. 候选集冻结

四份齐，无缺。以下数值均为本会话逐一复核（`git show <ref>:<路径> | sha256sum` 与
`| wc -lc`），与 [call-②.md](../call-②.md) 的表格一致。

| 家 | worktree 路径（各自仓内） | 行 | 字节 | SHA-256 | commit |
| --- | --- | --- | --- | --- | --- |
| luna | `~/worktrees/luna/k8s` 的 `sunmoonai/docs/dev-plan/agent-dev-guide.md` | 648 | 43921 | `cc47d068c39c9bcd68b5f120a9794b4eecb3aacf60a2c1d8c72502afdb8dcfea` | `39889605` |
| kimi | `~/worktrees/kimi/k8s` 的同路径 | 917 | 71858 | `913ccb082320b8e6470dc3d8566590c157aad6488fd15cb373b773e7f4a84774` | `dc8efd59` |
| cursor | `~/worktrees/cursor/k8s` 的同路径 | 921 | 70095 | `926a974e02df238f3a180dc1afa20aa89333a2ba3fdebd2d24a18269971d5138` | `d8fdb32a` |
| qwen | `~/worktrees/qwen/k8s` 的同路径 | 187 | 22316 | `e783ce080489c83783e99ee576cdaacfd508c80d47decc73437562b8d2213198` | `7d31265a` |

取件 ref 统一为 `runtime-refact/<家>:sunmoonai/docs/dev-plan/agent-dev-guide.md`。

## C. 评优

**利益冲突声明**：我是四份候选的作者之一，本评审不是独立终审。我把自己排在末位，
依据是下面逐条给出的可复核差距（J3 4/6 对 6/6、J2 薄落点对无），不是姿态；
同理，把别家排前面须给出与评我家同样标准的依据，下文逐条给。

### C.1 机械条核验（声明覆盖：查了什么、没查什么）

查了（命令与结果均可复跑）：

- **哈希与行数字节**：见 B 表，四份逐一复核，与 call-② 一致。
- **M4/M6 词汇扫描**：对四份候选分别跑
  `git show <ref>:<路径> | grep -nE 'amend_schema|amend\.mode|回执仓|候选仓|三道边界|开发 Profile|产品 Profile|kind ?= ?"human"'`：
  - **luna：零命中**（其 D2 表用 HTML 实体转写源标题中的敏感词，并在 §10 表头声明
    「HTML 实体只用于让已撤销设计的字面不被机械门误判为正文复活，渲染后的标题不变」）。
  - **qwen：零命中**（同法，D2 用 `&#32;` 等实体转写源标题）。
  - **kimi：6 行命中**（43/45/46/48/835/843），逐行核过语境：全部在 §1.1 核查表
    （词旁带「**已证伪**」粗体标注）或 §12.2「**故意不要（已证伪）**」行内。
  - **cursor：6 行命中**（752–757/821/854），逐行核过语境：752–757 在 §12
    「已被证伪、不得复活的设计」专章（章首有「本节是『被证伪』语境」声明），821/854 在
    D2「故意不要」行或带「不沿用」注记的源标题引用行。
  - 结论：四家 M4 均通过（词只出现在被证伪/拒绝语境）。M6 的 `kind = "human"` 四家零命中，
    且四家正文均明写「人不是执行者」、登记表无人条目。
- **M1 行数**：数了四家的 D2 表行——luna §10、kimi §12.1+§12.2、cursor §14（F 31 行 +
  R 33 行）、qwen §8，均为 31+33=64，且源节号/标题与两份源稿实际标题逐一对得上
  （源稿标题清单我独立枚举过，见覆盖声明）。
- **M2**：四家每行要么给落点、要么「故意不要」+ 具体理由（无一家用「已过时」三字了事）。

没查（判不了，显式标出）：

- **M3**：`doc-gate.py --all` 与 `anchor-gate.py` 未在四家分支上复跑——门禁按当前分支的
  git 索引判，我没有逐一检出四家分支。此项留给 ⑤ 机械验收；本评审对 M3 一律标「未核」。
- **M5 的「两个 Profile」**：补扫 grep 被本会话权限拒绝。判定依据是四份全文通读：
  四家该词均只出现在 D2 表引用源稿标题「3.1 一套状态机，两个 Profile」处——D2 的形状
  要求「节号与标题以源稿实际为准」，引用源题不等于主张分层；四家正文均无
  双 Profile 并列架构。kimi 在该行注明「源题原名，其结论已推翻」，cursor 注明
  「不沿用」，luna/qwen 用实体转写。人判：四家均通过。

### C.2 判断条逐条对照

| 判据 | luna | kimi | cursor | qwen |
| --- | --- | --- | --- | --- |
| J1 新读者可读 | 强：§0 结论链 + §0.1 真源表 + §0.2「为什么分成这些章」 | 强：§2 权威地图，名词在首现处定义 | **最强**：§0 五步读法，通篇按新读者组织 | 中：§1 权威表够用，但术语密度高、更依赖读者已熟悉协议 |
| J2 落点属实 | 抽 3 条全属实 | 抽 3 条全属实 | 抽 3 条全属实 | 抽 3 条均偏薄（A 块已列） |
| J3 六条处置 | 6/6：§8.1 K1–K6，宿主复跑（含 sandbox/宿主两组结果不混写的声明） | 4 自跑 + 1 旁证 + 1 降级：第 3 条 sudo 沙箱内被 no-new-privileges 拦、援引 forensics.md F1（独立取证记录，非任务书结论行）；第 4 条 `branch -vv` 自跑、`ls-remote` 断网不可跑、如实声明 | **6/6**：§12 逐条宿主复跑，且带 `uid_map` 恒等映射的环境证明——这正是沙箱会踩空的点 | 4/6：第 4–5 条未自跑（A 块） |
| J4 立得住/站不住分清 | 强：§8.2「保留/撤销」两份显式清单 | 强：§1.1 + D2 逐行注记 | 强：§12 处置列 + §2–9 承载保留内容 | 弱：散文式，无显式清单 |
| J5 取证质量 | **最规范**：全文锚点钉 commit（`request-lifecycle.md @ ed0b5136:247-277` 式） | 强：锚点密；两处依赖前轮已核锚（`cli.rs:60`）并声明未自跑 | 强：自带宿主取证块（hostname/id/uid_map）；代码引文与任务书照录原文一致 | 中：锚点少但实（代码锚 + `id`/`sudo` 复跑） |
| J6 不重写权威定义 | 通过 | 通过 | 通过 | 通过 |
| J7 §4.3 边界不淡化 | 通过（§1.4 专节） | 通过（§0 第 6 条 + §2 末段，措辞最强硬） | 通过（§1.3 专节 + 淡化后果说明） | 通过（§5，未淡化但位置不显要） |
| J8 自增内容质量 | 3 条自增、各有取证（§9.3） | 多条自增、各有出处（§5.5/§5.6/§10.4 等） | 4 条自增、逐条带取证（§15 末） | 少而实（§4.1 四层表） |
| 一票否决 | 不触发 | 不触发 | 不触发 | 不触发 |
| F1–F6 | 无 | F6 第 3/4 条部分，均已按「如实声明」处置 | 无 | F6 第 4/5 条（未声明地未自跑） |
| F5 篇幅 | 648 行，密度最高 | 917 行，重复少 | 921 行，重复少 | 187 行，最短但短因缺覆盖，不构成优点 |

D2 抽查明细（call-② 要求重点核落点属实，每家抽 3 条）：

- **luna**：fable 3.7 → §3.2（provision 函数、独占/干净/基线三判据、N 写者 N worktree——
  属实）；R 5.3 → §6.2（复合上界 ≤(2,4,2,0) 与「人的必需动作 ≤ 手工模式」——属实）；
  R 2.8 → §5.3（23 条仅 2 条 attested 的读数与限定——属实）。
- **kimi**：fable 3.13 → §6.4 + §1.1 第 3–5 条 + §9.2（取证纪律、forensics 事实、信任域
  两个有效方向——属实）；R 2.8 → §6.6（23/2/15/6 读数 + 引用纪律两条——属实）；
  fable 5.2 → §10.2（枚举全部标题、脚本查零缺口、行数不进判据——属实）。
- **cursor**：fable 3.13 → §5.2 末段 / §5.3 / §6.2 / §12 第 3–6 条（§12 有逐条复跑——
  属实）；R 2.8 → §8.1 末两段（23 条 2 attested 与 refact 轮 28 条对照——属实）；
  R 6（OP 裁定）→ §8.1（OP-1 等效）/ §7（OP-2 接库）/ §8.2–8.3（OP-3 开销）——属实。
- **qwen**（自查，见 A 块）：R2.8/R4.2/R5.3 三处偏薄。

锚点抽核（三家共同依赖的内核锚，我在基线版本上核了三处）：
`request-lifecycle.md:247-277` 确为「WAITING 与 Interaction」节与十个绑定字段（luna/kimi
引用属实）；`:270` 确有「Attempt 若从 checkpoint 原地恢复，可在自己的状态机内
`WAITING → RUNNING`」原句（cursor 引用属实）；`:627` 确为修订纪律（三家引用属实）。

### C.3 完整排序与基座建议

**排序：cursor ＞ kimi ＞ luna ＞ qwen。第 1/2 名差距小于判据分辨率（见覆盖声明），
翻转前二我都能接受；第 3/4 名与第 2 名的差距是实的。**

**建议基座：cursor。** 理由按判据给：

1. **J3 唯一 6/6 且带环境证明**。第 3 条（免密 sudo）是六条里唯一「沙箱会给出相反观测值」
   的（kimi 在沙箱内被 no-new-privileges 拦下、只能援引 forensics.md；cursor 在宿主
   跑通并附 `uid_map` 恒等映射的证明）。本轮题目就是「纳入核查结论」，六条的处置完整性
   是第一判据。
2. **Q4 的选择对**（§12 被证伪专章，逐条带复跑命令与处置）：源稿将删，「防止再犯」的
   证据基座必须在正文里。
3. **§7 的 H8 五步接线**是四家中唯一把「内核 Interaction 绑定字段 ↔ 库原语」逐字对上的
   落法（interrupt 出向 → principal channel → `Command(resume=)` → 同 thread 原地续跑 →
   四条件才新建 Task），且逐条有代码引文。
4. **权力表多一列 `enforcement_point`**（§5.2）——把「强制点必须落在动作实际经过的路径上」
   从原则变成表结构，直接回应了六条被证伪设计的共同成因。
5. **§15 覆盖声明形状最完整**（查了/没查/未读 R2/隔离边界/未验证/故意没写/新增条款带取证），
   D3 的最好样板。

**kimi 屈居第二的原因**只有两条：J3 第 3 条未自跑（已按任务书的「如实声明」路径降级处置，
援引的是 forensics.md 而非任务书结论行，我认为这不是 F6 的照抄，但完备性上输给 cursor 的
宿主复跑）；无被证伪专章（§1.1 核查表功能等价）。**kimi 在投影机器上比 cursor 更系统**
（工单 schema、guard 表、触点数 0/1/2、收件箱字段分级、独立性折算、17 项未决登记），
若裁决方以 kimi 为基座，D 块 cursor 侧的吸收清单同样成立。

**luna 第三**：单位行数信息密度四家最高（648 行覆盖了 ~920 行稿的绝大部分机器）；
引用纪律最好（全文锚点钉 commit，抽核属实）；G0–G5 演进表带每步退出条件。差距：
独立性折算与收件箱字段分级两块缺失（kimi/cursor 有）；被证伪记录用 K 表承担、无处置
语境的显式声明（功能等价，语境纪律略逊于 cursor 的专章声明）；D2 行注记较简
（M2 合法但可审计性低于 kimi）。**luna 的引用样式与 G0–G5 表无论如何都该进最终稿**（D 块）。

**qwen 第四**（我自己）：J3 4/6、J2 三处薄落点、覆盖机器最薄、无权力表投影。
纪律性（证据四层表、宿主事实诚实降级、git 限度）是它的长处，但这些长处 kimi/cursor
都有等价物。最短不构成优点——判据是「该覆盖的有没有覆盖」，我缺的是覆盖。

## D. 值得吸收的点

不管基座是谁，以下逐条值得并进最终稿。格式：主张｜出自谁·哪一节｜为什么。

### 出自 kimi

1. **工单 TOML schema 与 `intake_author` 同票禁任的机械拦**（§4.3）——把 `round.md`
   一般化的现成自洽形状；「起草意图/验收者不得兼任 proposer/arbiter/acceptor」落成
   分发前的机械检查，是角色分离从纪律到机制的最早机会。
2. **开工前触点数按档位 0/1/2，且 H2 一次完成四件事**（§4.3）——防多次前置确认退化成
   盖章；对 T0「零触点开工」的可达性是关键增量。
3. **收件箱字段分级：无默认字段（验收条、executors、tier）不得预填**（§5.5）——把
   「默认值属于省事方向」落到字段级；预填等于把盖章做成阻力最小路径。
4. **独立性折算首版**（§5.6）——分组键 harness、组内带取证权重 ×2、引用关系视同同组、
   先比独立信号数再比取证组数。「多家一致不等于独立信号强」第一次有了可算形状。
5. **git 载体验不了的三样明列**（§4.5：事务性、租约、fencing）与「先在这一层跑通」的
   准确表述——防把脚手架说成已验证。
6. **trace 读数与引用纪律两条**（§6.6）——23 条中 attested 仅 2 的读数；「张冠李戴」与
   「凭空构造」两种引用失败形态分开防，防法不同不可互替。
7. **十七项未决登记**（§11）——含 H5-final/H5-round 拆分、retroactive 与 I1 的关系；
   最终稿的未决清单可以直接以此为底。
8. **内核按 commit 引用 + 改后重跑状态词对称差**（§10.4）——防内核演化反向冲击开发
   文档与脚本。
9. **别名登记**（§4.2：`DEV`/`DEV_ROUND`/`DEVELOPMENT` → `dev.change`）——防多真源的
   最便宜手段。

### 出自 luna

1. **全文锚点钉 commit 的引用样式**（通篇 `@ ed0b5136:行号`）——四家中唯一把「按 commit
   取件」贯彻到每一条内部引用的；最终稿采用它，「锚点随主线漂移」一类问题一次解决。
   抽核属实（见 C.2）。
2. **「为什么分成这些章」表**（§0.2）——Q2（每多一章须说明为何不能并入相邻章）的直接
   答卷，最终稿保留章结构时照此自证。
3. **G0–G5 演进表**（§7.1）——比源稿 R0–R5 优在 G1 改为先做原语 spike（本轮核查结论的
   正确顺序化），且每步有机械可判的退出条件。
4. **T0 两道门与「`diff ⊆ paths` 只能绑完工门」**（§3.1）——分发时 diff 为空集、
   空集恒真，所以路径检查只能绑完工门；这个观察是对的，两道门的分工因此成立。
5. **`route_proposal / route_effective / route_delta` 三字段分立**（§2.4）——「建议与
   决定分开、人改了什么留痕」的最小形状，三者不可合成一段自然语言。
6. **输入边界偏差的如实登记**（§9.2：按 AGENTS.md 强制入口读了未列入只读输入的
   `development-lifecycle-agent.md`，声明不作为锚点、未据其起稿）——覆盖声明的诚实样板，
   值得作为纪律写进最终稿的覆盖声明节。

### 出自 cursor（若基座翻转为 kimi/luna，以下全部为吸收项；cursor 为基座则已内含）

1. **§12 被证伪专章 + 逐条宿主复跑（含 `uid_map`）**——Q4 的「保留」选择，源稿将删的
   背景下价值最高；「本节是『被证伪』语境」的显式声明同时满足 M4 的人核语境要求。
2. **§7 H8 五步接线**——四家中唯一逐字对上「内核绑定字段 ↔ 库原语」的落法。
3. **权力表 `enforcement_point` 列**（§5.2）——把强制点落位变成表结构。
4. **§15 覆盖声明七段式形状**——D3 的最完整形状（含「隔离面见到了什么、没见到什么」的
   精确边界：跑 `branch -vv` 见到分支名与 HEAD、未读内容）。
5. **§10.1 执行层架构另迁 `executor-architecture.md`（走 T1）**——把源稿诊断 1.5
   （执行架构与开发流程混装）真正收口，且不把这次搬家塞进本轮。
6. **取证纪律附 `hostname; id; cat /proc/self/uid_map`**（§6.2）——防沙箱伪 root 误判，
   cursor 自己的宿主取证块就是按此执行的。

### 出自 qwen

无。我的候选里值得进最终稿的内容（证据四层表、宿主事实降级、git 限度）在 kimi/cursor
的稿里都有等价或更强的版本（E0–E4 表、§12 取证块、§6.4 三样验不了）。如实交代，
不为凑数硬列。

## 评审自身的覆盖声明

**查了**：四份候选全文逐行（共 2673 行，含各自 D2 表逐行）；两份源稿的全部 `##`/`###`
标题独立枚举（31 + 33 = 64，与各家 D2 的源节号逐一比对）；三份权威文档（refact-fable /
runtime-architecture / request-lifecycle）经 `git diff ed0b5136 HEAD` 确认与基线一致，
故工作区读到的内核内容即基线内容；四份候选的 SHA-256 与行数字节逐一复核；M4/M6 词汇
grep（命令与命中行号记于 C.1）；内核锚点三处抽核（`request-lifecycle.md:247-277` /
`:270` / `:627`）；本轮 `round.md`、`runtime-refact-task.md`、`rulings.md`、`call-②.md`。

**没查**：M3 两门禁未在任何一家分支复跑（C.1 已标「未核」）；M5「两个 Profile」grep
补扫被权限拒绝，判定基于全文通读；investment-app 代码文件本会话未重新打开（权限拒绝）——
代码锚点的采信依据是任务书 §3.2 照录的两段原文、我本人在 ① 的复核记录（候选 §3 的
行号锚）、以及三家引用的交叉一致；各家候选引用的 request-lifecycle 行号锚只抽核三处，
未逐条回查；未复核各家对 `rounds/` 历史轮次产物的引用（kimi/cursor 引用的 23 条轨迹
读数、refact 轮 28 条提交等，采信其自述与源稿一致）。

**未验证 ⚠**：J1「新读者可读」是我的判断不是实验（没有真找新读者测试）；cursor 与
kimi 的排序差距小于本评审判据的分辨率——两者都以「内容等价、机器互有独占」的方式
达标，翻转前二我都能接受，裁决方以处置表逐条吸收时以内容为准，不必拘泥我的次序。
