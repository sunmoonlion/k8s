# SESSION_HANDOFF（k8s / docs-cursor）

> 用途：新对话接续、上下文压缩后快速恢复。  
> 使用方式：新会话先 `@docs-cursor/SESSION_HANDOFF.md`，再补充本次目标。

---

## 1) 当前状态（截至 2026-04-01）

- `docs-cursor/整体架构.md` 已作为主架构文档维护。
- 已新增并生效：
  - `docs-cursor/环境事实表.md`（可变环境事实）
  - `docs-cursor/联调速查.md`（安全版命令模板）
- `整体架构.md` 已补充「实战补充（无敏感信息）」章节：
  - Remote 基建阶段顺序摘要
  - Traefik 与 DNS 联动约束
  - RabbitMQ 对外策略
  - 文档治理规则

---

## 2) 文档分工（必须遵守）

- `docs-cursor/整体架构.md`
  - 放稳定结构、分层关系、部署约束、治理规则。
- `docs-cursor/环境事实表.md`
  - 放易变信息：IP、端口、节点角色、DNS、命名空间。
- `docs-cursor/联调速查.md`
  - 放命令模板与排障路径，不放真实凭据。

---

## 3) 安全红线

- 禁止把真实账号/密码/Token/私钥写进仓库文档。
- 连接串、账号字段只写占位：`<from-secret>` / `<from-env>`。
- 涉及联调凭据，统一从 Kubernetes Secret 或私有配置系统读取。

---

## 4) 下次会话建议起手

1. 先确认本次目标是：
   - 架构补充文档，还是
   - 实际改 `deploy-*` / `values` / `secrets` 流程。
2. 若涉及环境变化，先更新 `环境事实表.md`。
3. 若涉及操作步骤，优先复用 `联调速查.md` 的模板并补充验证结果。

---

## 5) 待办（可选）

- 增加 `docs-cursor/目录索引.md`，做所有核心文档的跳转入口。
- 为 `docs-cursor` 增加“变更记录”区块（记录每次文档治理变更）。
- 根据后续实操，补 `Remote` / `Kind` 各自的最小回归清单。

---

最后更新：2026-04-01  
状态：生效中（接续入口）

