apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodebullworker-info-web-backend
  namespace: app-platform-dev
  labels:
    app: nodebullworker-info-web-backend
    component: node-bull-worker
    backend: info-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodebullworker-info-web-backend
  template:
    metadata:
      labels:
        app: nodebullworker-info-web-backend
        component: node-bull-worker
        backend: info-web-backend
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: nodebullworker
        image: harbor.sunmoonai.com:30443/app-images/info-web-backend:1.0.0
        imagePullPolicy: Always
        command:
        - sh
        - -c
        - |
          set -eu
          export NODE_ENV="${NODE_ENV:-production}"
          export QUEUE_ON="${QUEUE_ON:-true}"
          exec node dist/worker
        envFrom:
        - configMapRef:
            name: info-web-backend-config
        - secretRef:
            name: info-web-backend-secret
        - configMapRef:
            name: nodebullworker-info-web-backend-config
        - secretRef:
            name: nodebullworker-info-web-backend-secret
        - secretRef:
            name: info-web-backend-nodebull-redis-conn
        env:
        - name: QUEUE_WORKER_SERVICE
          value: "info-web-backend"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
