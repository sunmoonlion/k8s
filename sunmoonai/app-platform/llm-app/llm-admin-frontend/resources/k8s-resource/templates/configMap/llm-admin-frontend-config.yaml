apiVersion: v1
kind: ConfigMap
metadata:
  name: llm-admin-frontend-config
  namespace: ${NAMESPACE}
  labels:
    app: llm-admin-frontend
data:
  # VITE_* 变量在 Docker 构建时通过 --build-arg 注入，已静态嵌入 bundle。
  # 运行阶段是纯 nginx 容器，不读取任何环境变量。
  # 本 ConfigMap 预留用于将来可能的 nginx 运行时配置（当前为空）。
