apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: logstash-stripprefix
  namespace: data-platform-dev
  labels:
    app: data-platform-ingress
    component: logstash-stripprefix
    project: sunmoonai
spec:
  stripPrefix:
    prefixes:
      - "/logstash"

