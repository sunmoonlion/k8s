apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: neo4j-ratelimit
  namespace: data-platform-dev
  labels:
    app: data-platform-ingress
    component: neo4j-ratelimit
    project: sunmoonai
spec:
  rateLimit:
    average: 50
    burst: 100

