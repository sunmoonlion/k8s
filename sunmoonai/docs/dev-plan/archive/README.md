# `archive/` —— 原稿的冻结备份

> 建于 2026-09-07，所有者指示：**「copy 一份原稿备份待转，而现在的不变」**。

| 文件 | 是什么 |
| --- | --- |
| `refact-fable.md` | `../refact-fable.md` 在 `e08d89cd` 时刻的**逐字节副本** |
| `runtime-architecture.md` | `../runtime-architecture.md` 在 `e08d89cd` 时刻的**逐字节副本** |

## 三条状态声明

1. **副本不加任何页眉或标注。**加了就不再是原稿备份。
   它们的状态写在本文件里，不写进文件本身。
2. **副本无规范效力，也不是第二真源。**它们是「待转」的工作底本——
   将来要改造的是**副本**，`../` 下的原稿保持不动。
3. **本目录不参与任何轮次。**不得作为候选、评审或验收的取件对象。

## 为什么不直接用 git 历史

git 里当然有每一个版本，取原文一条 `git show` 就够。建这份副本是因为
所有者的用途是**「待转」**——需要一个可以直接改的工作底本，而不是只读的历史。
**如果哪天不再需要改造，本目录应当删除**，回到「历史在 git 里」这一条。

## ⚠ 一个尚未裁定的问题

原稿保留之后，`refact-fable.md`、`runtime-architecture.md` 与
`agent-dev-guide.md` 三份并存，而 P1 要求「同一事实只有一个权威写入面」。
三者的**规范效力**尚待所有者裁定（见 `rounds/runtime-refact/rulings.md` `R9`）：
源稿降为历史档案，还是仍为规范。**在裁定之前，不得在任何文档里断言源稿已作废。**
