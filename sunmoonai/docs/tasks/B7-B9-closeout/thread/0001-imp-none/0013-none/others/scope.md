# 批准四个历史缺失文件补回并继续本机部署

本轮「允许」对应上一轮最终问询：仅补回 Info 四个缺失文件，再继续部署；
不覆盖已有文件、不改数据库记录、不重放历史任务。对象精确目录、大小和摘要以
[上一轮检查结果](../../0012-none/others/preparation.md) 为准；批准记为 reported。
原本机 KIND 维护及三 App 独立身份切换授权继续有效，不涉及云端部署或扩大数据修复范围。

起点 k8s/luna `10653a7a`，工作区干净。先重新读当前引用、构造候选并核原摘要，
再对四 key 全部检查无历史版本/删除标记，以 IfNoneMatch=* 条件 PUT，GET 返回版本校验。
出现漂移、部分成功或未知回执立即停止，不自动删除已恢复对象或重复覆盖。
成功后完整对象导出/隔离恢复，继续既定发布前置与串行切换；不把文件恢复称为部署完成。

规则 D1–D5/D9：Info 自有凭据和桶、精确旧引用、数据库只读、不拿 RAG 派生数据冒充原文；
I3/I8：不轮换或输出既有凭据；T3–T5/R1–R7：固定候选不重新构建、独立迁移，
不改历史 release，不绕过备份/身份门禁，不向共享运行环境套用测试供给器。
