apiVersion: apps/v1
kind: Deployment
metadata:
  name: investment-web-frontend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: investment-web-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: investment-web-frontend
  template:
    metadata:
      labels:
        app: investment-web-frontend
    spec:
      imagePullSecrets:
      - name: ${INVESTMENT_WEB_FRONTEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: investment-web-frontend
        # Harbor 镜像路径：仓库/项目/镜像名:标签
        # 由 deploy-investment-web-frontend.sh 计算并导出 INVESTMENT_WEB_FRONTEND_FULL_IMAGE_NAME
        image: ${INVESTMENT_WEB_FRONTEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: ${INVESTMENT_WEB_FRONTEND_CONFIGMAP_NAME:-investment-web-frontend-config}
        - secretRef:
            name: ${INVESTMENT_WEB_FRONTEND_SECRET_NAME:-investment-web-frontend-secret}
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data-storage
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
          subPath: ${PVC_SUB_PATH:-}
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-investment-web-frontend-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: investment-web-frontend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: investment-web-frontend
spec:
  selector:
    app: investment-web-frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
