apiVersion: apps/v1
kind: Deployment
metadata:
  name: investment-admin-frontend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: investment-admin-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: investment-admin-frontend
  template:
    metadata:
      labels:
        app: investment-admin-frontend
    spec:
      imagePullSecrets:
      - name: ${INVESTMENT_ADMIN_FRONTEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: investment-admin-frontend
        # Harbor 镜像路径：仓库/项目/镜像名:标签
        # 由 deploy-investment-admin-frontend.sh 计算并导出 INVESTMENT_ADMIN_FRONTEND_FULL_IMAGE_NAME
        image: ${INVESTMENT_ADMIN_FRONTEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: ${INVESTMENT_ADMIN_FRONTEND_CONFIGMAP_NAME:-investment-admin-frontend-config}
        - secretRef:
            name: ${INVESTMENT_ADMIN_FRONTEND_SECRET_NAME:-investment-admin-frontend-secret}
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data-storage
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
          subPath: ${PVC_SUB_PATH:-}
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-investment-admin-frontend-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: investment-admin-frontend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: investment-admin-frontend
spec:
  selector:
    app: investment-admin-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
