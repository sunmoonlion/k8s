# Document Converter Web 路由配置
# 通过 Traefik HTTP 路由暴露 Document Converter 服务（8000）
#
# 使用路径前缀路由，避免需要在 /etc/hosts 中配置多个域名
# 访问方式：
# - https://<节点IP>:30443/document-converter
# - 或 https://www.sunmoonai.com/document-converter (如果配置了 DNS)

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: document-converter-route
  namespace: {{NAMESPACE}}
  labels:
    app: app-platform-ingress
    component: document-converter
spec:
  entryPoints:
    - websecure
    - web
  routes:
    # 使用路径前缀路由，支持 IP 或统一域名
    # 方案1: 使用 IP 地址（无需 DNS 配置）
    # 访问: https://<节点IP>:30443/document-converter
    # 方案2: 使用统一域名（只需在 /etc/hosts 中添加一个条目）
    # 访问: https://www.sunmoonai.com/document-converter
    - match: Host(`115.190.153.150`) && PathPrefix(`/document-converter`)
      kind: Rule
      services:
        - name: {{SERVICE_NAME}}
          port: {{SERVICE_PORT}}
      middlewares:
        - name: document-converter-stripprefix
    - match: Host(`{{UNIFIED_HOST}}`) && PathPrefix(`/document-converter`)
      kind: Rule
      services:
        - name: {{SERVICE_NAME}}
          port: {{SERVICE_PORT}}
      middlewares:
        - name: document-converter-stripprefix
  tls:
    secretName: traefik-tls-secret

