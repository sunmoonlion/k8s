# ============================================================================
# Investment Admin Backend YAML 生成配置
# 职责：配置 YAML 生成相关的参数和数据内容
# ============================================================================

# 模板文件路径（相对于 resources/k8s-resource/）
TEMPLATE_FILE="templates/app/investment-admin-backend.yaml"

# 输出文件名（生成在当前目录）
OUTPUT_FILE="investment-admin-backend-generated.yaml"

# 是否启用生成
ENABLED="true"

# 资源类型
RESOURCE_TYPE="app"

# ============================================================================
# 基础配置（用于生成 YAML）
# ============================================================================
NAMESPACE="${NAMESPACE:-app-platform-dev}"
ENVIRONMENT="${ENVIRONMENT:-development}"
ENV="${ENV:-dev}"

# ============================================================================
# App 数据内容（用于生成 YAML）
# ============================================================================
# 镜像配置
INVESTMENT_ADMIN_BACKEND_IMAGE_REGISTRY="${INVESTMENT_ADMIN_BACKEND_IMAGE_REGISTRY:-harbor.sunmoonai.com:30443}"
INVESTMENT_ADMIN_BACKEND_IMAGE_PROJECT="${INVESTMENT_ADMIN_BACKEND_IMAGE_PROJECT:-app-images}"
INVESTMENT_ADMIN_BACKEND_IMAGE="${INVESTMENT_ADMIN_BACKEND_IMAGE:-investment-admin-backend}"
INVESTMENT_ADMIN_BACKEND_TAG="${INVESTMENT_ADMIN_BACKEND_TAG:-1.0.0}"
IMAGE_PULL_POLICY="${IMAGE_PULL_POLICY:-Always}"
INVESTMENT_ADMIN_BACKEND_IMAGE_PULL_SECRET_NAME="${INVESTMENT_ADMIN_BACKEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}"

# Secret / ConfigMap 名称（用于生成 App YAML）
INVESTMENT_ADMIN_BACKEND_SECRET_NAME="${INVESTMENT_ADMIN_BACKEND_SECRET_NAME:-investment-admin-backend-secret}"
INVESTMENT_ADMIN_BACKEND_CONFIGMAP_NAME="${INVESTMENT_ADMIN_BACKEND_CONFIGMAP_NAME:-investment-admin-backend-config}"

# PVC 配置（用于生成 App YAML 中的 volumeMounts）
PVC_NAME="${PVC_NAME:-investment-admin-backend-pvc}"
PVC_MOUNT_PATH="${PVC_MOUNT_PATH:-/app/data}"
PVC_SUB_PATH="${PVC_SUB_PATH:-}"
