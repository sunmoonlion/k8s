# 当前数据持久化方案设计

> **用途**：重构前的完整现状记录，作为迁移到 local-path-provisioner 的基线参考。  
> **覆盖范围**：data-platform（6组件）、messaging-platform、cicd-platform、ops-platform、app-platform  
> **更新时间**：2026-04-14

---

## 一、三层存储架构

项目按运行环境分三层，存储方案完全独立：

```
┌─────────────────────────────────────────────────────┐
│  Layer 1：Local（Kind / WSL）                        │
│    模式 A init：dev-values.yaml → 动态 nfs-2 → WSL NFS │
│    模式 B reuse：静态 PV/PVC → WSL NFS（172.28.46.235）│
├─────────────────────────────────────────────────────┤
│  Layer 2：Remote（C1 / C2，真实集群）                 │
│    唯一方式：dev-values.yaml → 动态 nfs-2 → 云 NFS     │
│    保护：脚本自动阻断 CLUSTER!=kind + reuse 的组合      │
├─────────────────────────────────────────────────────┤
│  Layer 3：Production                                 │
│    唯一方式：prod-values.yaml → fast-ssd（本地 SSD）   │
│    无需改动，重构不涉及                                 │
└─────────────────────────────────────────────────────┘
```

---

## 二、持久化模式决策机制

核心数据组件（Redis / PostgreSQL / MongoDB / Neo4j / Elasticsearch）的部署脚本，统一实现了一套两级持久化控制逻辑：

### 2.1 决策优先级链

```
优先级（高→低）：
1. SUNMOONAI_GLOBAL_PERSIST_MODE   全局强制，覆盖所有组件配置
2. <COMPONENT>_PERSIST_MODE        组件级配置（如 POSTGRESQL_PERSIST_MODE）
3. 远程集群检测                    CLUSTER != "kind" → 强制改为 init
4. 默认值                          init
```

### 2.2 决策树（以 PostgreSQL 为例）

```bash
# Step 1：读取全局或组件模式
if [[ -n "$SUNMOONAI_GLOBAL_PERSIST_MODE" ]]; then
    persist_mode="$SUNMOONAI_GLOBAL_PERSIST_MODE"
else
    persist_mode="${POSTGRESQL_PERSIST_MODE:-init}"
fi

# Step 2：远程集群保护（防止 Kind 专用 PV 误用到云集群）
if [[ CLUSTER != "kind" && persist_mode == "reuse" ]]; then
    persist_mode="init"   # 强制动态模式
fi

# Step 3：选择 values 文件
case "$environment" in
  "development")
    if [[ "$persist_mode" == "reuse" ]]; then
        kubectl apply -f postgresql-dev-pv-pvc.yaml   # 先建静态 PV/PVC
        values_file="dev-values-persist.yaml"          # 再引用 existingClaim
    else
        values_file="dev-values.yaml"                  # 动态创建
    fi
    ;;
  "production")
    values_file="prod-values.yaml"                     # 始终用生产配置
    ;;
esac
```

### 2.3 两种 dev 模式对比

| | init 模式 | reuse 模式 |
|---|---|---|
| **触发方式** | 默认 / `PERSIST_MODE=init` | `PERSIST_MODE=reuse` |
| **适用环境** | Remote（强制）/ Kind 新建 | **仅 Kind**，集群重置后保留数据 |
| **values 文件** | `dev-values.yaml` | `dev-values-persist.yaml` |
| **PVC 来源** | Helm 动态创建 | 预先 apply 静态 PV/PVC YAML |
| **数据结果** | 每次全新空盘 | 挂回旧数据目录，数据保留 |

---

## 三、配置文件结构

每个核心数据组件的 `resources/custom-values/` 下有 3 类文件：

```
resources/custom-values/
├── dev-values.yaml              ← Remote + Kind init，动态 nfs-2
├── dev-values-persist.yaml      ← Kind reuse 专用，existingClaim
├── <component>-dev-pv-pvc.yaml  ← Kind reuse 专用，静态 PV/PVC（含 WSL NFS IP + UUID）
└── prod-values.yaml             ← 生产，fast-ssd
```

Harbor / Jenkins / Casdoor / pgAdmin：**只有 `dev-values.yaml` 和 `prod-values.yaml`**，无 persist 版本。

RabbitMQ：有静态 PV/PVC YAML 和 `dev-values-persist.yaml`，支持 reuse 模式（密码随 PVC 持久化，`updatePassword: false`）。

---

## 四、静态 PV/PVC 文件详情（Kind reuse 专用）

所有静态 PV/PVC 文件共同特征：
- NFS server 硬编码为 WSL IP：`172.28.46.235`
- NFS path 包含实际的 PVC UUID（从首次 Kind 部署时记录）
- `storageClassName: ""`（静态 PV，不走 StorageClass）
- `persistentVolumeReclaimPolicy: Retain`

| 组件 | PVC 名称 | 容量 |
|------|---------|------|
| Redis | `redis-sunmoonai-dev-pvc` | 5 Gi |
| PostgreSQL | `postgresql-sunmoonai-dev-pvc` | 8 Gi |
| MongoDB | `mongodb-sunmoonai-dev-pvc` | 8 Gi |
| Neo4j | `neo4j-sunmoonai-dev-pvc` | 20 Gi |
| Elasticsearch | `elasticsearch-sunmoonai-dev-pvc` | 8 Gi |
| RabbitMQ | `rabbitmq-sunmoonai-dev-pvc` | 10 Gi |

> ⚠️ **重要**：NFS path 中的 UUID 是 Kind 集群首次部署时生成的。若集群完全重建，PVC UUID 会改变，静态 PV/PVC YAML 需手动更新路径。这是当前方案的主要维护痛点之一。

---

## 五、各组件当前配置详情

### 5.1 核心数据组件（data-platform）

| 组件 | dev StorageClass | dev 容量 | 副本数（dev） | 有 persist 版 | 备注 |
|------|----------------|---------|-------------|--------------|------|
| Redis | nfs-2 | 5 Gi | master×1，replica×0 | ✅ | Sentinel persistence disabled |
| PostgreSQL | nfs-2 | 8 Gi | primary×1，readReplicas×0 | ✅ | |
| MongoDB | nfs-2 | 8 Gi | 1（standalone）| ✅ | |
| Neo4j | nfs-2 | 20 Gi | 1 | ✅ | persist 版同时清空 global.storageClass |
| Elasticsearch | nfs-2 | 8 Gi（master）| master×1，data×0 | ✅ | data 节点 dev 禁用 |

### 5.2 其他组件（无 persist 版）

| 组件 | StorageClass | 容量 | 副本数 | 特殊注意 |
|------|-------------|------|-------|---------|
| RabbitMQ | nfs-2 | 10 Gi | 1 | `updatePassword: false`，密码随 PVC 持久化 |
| Harbor（Trivy）| nfs-2 | 5 Gi | 1 | 内置 PG+Redis 由子 Chart 管理 |
| Jenkins | nfs-2 | 8 Gi | 1 | 删 PVC 后 NFS 目录残留，需手动清理 |
| Casdoor | nfs-2 | 1 Gi | - | 挂载 /conf，存 app.conf，缺 `copyrequestbody=true` 登录失败 |
| pgAdmin | nfs-2 | 10 Gi | 1 | contextPath: /pgadmin |

### 5.3 生产配置（prod-values.yaml，不需重构）

| 组件 | prod StorageClass | prod 容量 | 副本数（prod）|
|------|-----------------|---------|------------|
| Redis | fast-ssd | 20 Gi | master×1，replica×2 |
| PostgreSQL | fast-ssd | 100 Gi | primary×1，readReplicas×2 |
| MongoDB | fast-ssd | 100 Gi | 3（ReplicaSet）|
| Neo4j | fast-ssd | 100 Gi | 3 |
| Elasticsearch | fast-ssd | 100 Gi | master×3 |
| RabbitMQ | fast-ssd | 50 Gi | 3 |

---

## 六、dev 存储总量

| 分类 | 组件 | 容量 |
|------|------|------|
| data-platform | Redis + PostgreSQL + MongoDB + Neo4j + Elasticsearch | 49 Gi |
| messaging | RabbitMQ | 10 Gi |
| cicd | Harbor + Jenkins | 13 Gi |
| ops | pgAdmin | 10 Gi |
| app | Casdoor | 1 Gi |
| **合计** | | **≈ 83 Gi** |

---

## 七、重构对应关系

### 7.1 文件层面

| 现状 | 重构后 |
|------|--------|
| `dev-values.yaml`（nfs-2）| `dev-values.yaml`（**local-path**）|
| `dev-values-persist.yaml`（Kind reuse 专用）| **保留，不改**（existingClaim 机制不变）|
| `*-dev-pv-pvc.yaml`（NFS 类型，含 WSL IP + UUID）| **替换**（改为 hostPath 类型，固定路径）|
| `*-dev-pv-pvc.template.yaml`（PV/PVC 占位符模板）| **废弃**（固定路径无需模板）|
| `prod-values.yaml`（fast-ssd）| **不动** |

### 7.2 utils 脚本层面

`utils/重建持久化数据/` 下的三个脚本与静态 PV/PVC 机制强绑定，重构后同步退休：

| 脚本 | 功能 | 重构后 |
|------|------|--------|
| `update-static-pv-path-from-cluster.sh` | 从集群读取动态 PV 的 NFS server/path，写入静态 YAML | **退休**（静态 YAML 不再存在）|
| `update-all-static-pv-paths.sh` | 批量调用上面的脚本，覆盖 6 个组件 | **退休** |
| `cleanup-b-level-nfs-dirs.sh` | 扫描 `/data/kind-nfs` 下无 PV 使用的历史 NFS 目录，交互式删除 | **退休**（数据目录改为 local-path，不再是 NFS）|

> 这三个脚本连同对应的 `.template.yaml` 文件，可在重构完成并验证稳定后统一删除。

> **SC 名称**：两端统一使用 **`local-path`**。远程集群由 step09 脚本创建（`STEP09_LOCAL_STORAGE_CLASS_NAME="local-path"`），Kind 内置即为 `local-path`，无需额外操作，dev-values.yaml 天然一致。

### 7.3 Kind 集群重建后数据恢复：extraMounts + 固定路径静态 PV

> **背景**：Kind 集群重建（`kind delete cluster` + `kind create cluster`）是常见操作，数据库数据必须能恢复。这与当前 NFS reuse 机制要解决的是同一个问题。

#### 当前 NFS reuse 的机制

```
首次部署 → NFS 动态创建 UUID 路径
         → update-all-static-pv-paths.sh 记录 UUID 到静态 PV YAML
集群重建 → kubectl apply 静态 PV YAML → PVC 绑定到原 UUID 路径 → 数据恢复
```

痛点：UUID 每次都不同，每次集群重建前必须先运行更新脚本，且 NFS server IP 硬编码。

#### 重构后的等价方案：extraMounts + 固定路径静态 hostPath PV

**核心思路**：用固定的、有意义的目录名代替 UUID，路径永远不变，静态 PV YAML 写一次永久有效。

**Step 1：Kind 集群配置 extraMounts**

将 WSL 宿主机目录挂进 Kind 节点（数据存在 Docker 之外）：

```yaml
# kind-cluster-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraMounts:
  - hostPath: /data/kind-local-storage   # WSL 宿主机，Kind 删除后数据依然存在
    containerPath: /data/kind-local-storage
```

```bash
kind create cluster --config kind-cluster-config.yaml
```

**Step 2：WSL 上预建固定数据目录**

```bash
sudo mkdir -p /data/kind-local-storage/{postgresql,redis,mongodb,neo4j,elasticsearch,rabbitmq}
sudo chmod -R 777 /data/kind-local-storage
```

**Step 3：静态 hostPath PV（取代原 NFS 静态 PV，六个组件）**

```yaml
# postgresql-dev-pv-pvc.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgresql-sunmoonai-dev-pv
spec:
  capacity:
    storage: 8Gi
  accessModes: [ReadWriteOnce]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /data/kind-local-storage/postgresql
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values: [kind-control-plane]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-sunmoonai-dev-pvc
  namespace: data-platform-dev
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 8Gi
  storageClassName: ""
  volumeName: postgresql-sunmoonai-dev-pv
```

```yaml
# redis-dev-pv-pvc.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-sunmoonai-dev-pv
spec:
  capacity:
    storage: 5Gi
  accessModes: [ReadWriteOnce]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /data/kind-local-storage/redis
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values: [kind-control-plane]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-sunmoonai-dev-pvc
  namespace: data-platform-dev
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
  storageClassName: ""
  volumeName: redis-sunmoonai-dev-pv
```

```yaml
# mongodb-dev-pv-pvc.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-sunmoonai-dev-pv
spec:
  capacity:
    storage: 8Gi
  accessModes: [ReadWriteOnce]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /data/kind-local-storage/mongodb
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values: [kind-control-plane]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-sunmoonai-dev-pvc
  namespace: data-platform-dev
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 8Gi
  storageClassName: ""
  volumeName: mongodb-sunmoonai-dev-pv
```

```yaml
# neo4j-dev-pv-pvc.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: neo4j-sunmoonai-dev-pv
spec:
  capacity:
    storage: 20Gi
  accessModes: [ReadWriteOnce]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /data/kind-local-storage/neo4j
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values: [kind-control-plane]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neo4j-sunmoonai-dev-pvc
  namespace: data-platform-dev
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 20Gi
  storageClassName: ""
  volumeName: neo4j-sunmoonai-dev-pv
```

```yaml
# elasticsearch-dev-pv-pvc.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: elasticsearch-sunmoonai-dev-pv
spec:
  capacity:
    storage: 8Gi
  accessModes: [ReadWriteOnce]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /data/kind-local-storage/elasticsearch
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values: [kind-control-plane]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elasticsearch-sunmoonai-dev-pvc
  namespace: data-platform-dev
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 8Gi
  storageClassName: ""
  volumeName: elasticsearch-sunmoonai-dev-pv
```

```yaml
# rabbitmq-dev-pv-pvc.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: rabbitmq-sunmoonai-dev-pv
spec:
  capacity:
    storage: 10Gi
  accessModes: [ReadWriteOnce]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /data/kind-local-storage/rabbitmq
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values: [kind-control-plane]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rabbitmq-sunmoonai-dev-pvc
  namespace: messaging-platform-dev
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  volumeName: rabbitmq-sunmoonai-dev-pv
```

**Step 4：集群重建流程**

```bash
# 1. 重建集群
kind delete cluster && kind create cluster --config kind-cluster-config.yaml

# 2. apply 静态 PV/PVC（路径固定，YAML 无需更新）
kubectl apply -f postgresql-dev-pv-pvc.yaml
kubectl apply -f redis-dev-pv-pvc.yaml
# ... 其他组件

# 3. 正常部署（reuse 模式，dev-values-persist.yaml 使用 existingClaim）
PERSIST_MODE=reuse ./deploy.sh
```

#### 与当前 NFS reuse 的对比

| 对比项 | NFS reuse（当前）| extraMounts + hostPath（重构后）|
|--------|----------------|-------------------------------|
| 数据存储位置 | WSL NFS 导出目录 | WSL 宿主机直接目录 |
| 路径含 UUID | ✅ 是（每次集群不同）| ❌ 否（固定路径）|
| 集群重建后需更新 YAML | ✅ 需要运行 update 脚本 | ❌ 不需要，YAML 永久有效 |
| 依赖 NFS 服务 | ✅ 是 | ❌ 否 |
| `dev-values-persist.yaml` | 保留 | **保留**（existingClaim 机制不变）|
| 静态 PV/PVC YAML | NFS 类型，含 UUID | hostPath 类型，固定路径 |
| update-all-static-pv-paths.sh | 需要 | **退休**（路径固定，无需更新）|

### 7.4 重构后文件保留与废弃汇总

| 文件 | 重构后 | 原因 |
|------|--------|------|
| `dev-values.yaml` | **保留，修改 SC 名** | nfs-2 → local-path |
| `dev-values-persist.yaml` | **保留，不改** | existingClaim 机制不变，仍用于 Kind reuse |
| `*-dev-pv-pvc.yaml`（NFS 类型，含 UUID）| **替换** | 改为 hostPath 类型，固定路径，无 UUID |
| `*-dev-pv-pvc.template.yaml` | **废弃** | 新静态 PV 路径固定，无需模板生成 |
| `update-static-pv-path-from-cluster.sh` | **废弃** | 路径固定，无需从集群读取 UUID |
| `update-all-static-pv-paths.sh` | **废弃** | 同上 |
| `cleanup-b-level-nfs-dirs.sh` | **废弃** | 数据目录从 NFS 改为 hostPath，清理逻辑不再适用 |
| `prod-values.yaml` | **不动** | fast-ssd 保持原样 |

### 7.5 部署脚本是否需要改动

**不需要改动**。脚本中的 init/reuse 模式判断、远程集群保护逻辑完全保留。重构后：
- `dev-values.yaml` 里 `nfs-2` 改为 `local-path`，脚本读取同一文件
- reuse 模式依然可以用（如果日后重新引入静态 PV），脚本逻辑不变
- 环境变量控制体系不变

### 7.6 批量替换命令（重构实施时）

```bash
# Linux/macOS（注意：-E 和 + 必须有，防止误匹配 nfs-common 等字符串）
find . -name "dev-values*.yaml" -exec sed -i -E 's/nfs-[0-9]+/local-path/g' {} +

# Windows PowerShell（\d+ 已正确匹配一个或多个数字）
Get-ChildItem -Recurse -Filter "dev-values*.yaml" |
  ForEach-Object {
    (Get-Content $_.FullName) -replace 'nfs-\d+', 'local-path' |
    Set-Content $_.FullName
  }
```

---

## 八、重构前检查清单

- [ ] 远程集群运行 step09 脚本，验证 StorageClass `local-path` 存在且为默认（`kubectl get sc`）
- [ ] 云服务器数据盘已挂载到 `/data`，local-path-provisioner 数据目录为 `/data/local-storage`（已由脚本配置）
- [ ] WSL 上预建固定数据目录：`/data/kind-local-storage/{postgresql,redis,mongodb,neo4j,elasticsearch,rabbitmq}`
- [ ] Kind 集群配置文件加入 `extraMounts`：WSL `/data/kind-local-storage` → 容器 `/data/kind-local-storage`
- [ ] 将各组件静态 PV/PVC YAML 从 NFS 类型改为 hostPath 类型，路径改为固定的 `/data/kind-local-storage/<component>/`（无 UUID）
- [ ] 验证集群重建后 apply 静态 PV YAML → 以 reuse 模式部署 → 数据可恢复
- [ ] 批量替换 dev-values*.yaml 中 `nfs-[0-9]+` → `local-path`，验证无遗漏
- [ ] 确认 prod-values.yaml 未被修改（fast-ssd 保持原样）
- [ ] Casdoor /conf/app.conf 手动迁移到新 PVC 后验证登录正常

> 详细实施步骤见：`数据库持久化存储重构方案.md`
