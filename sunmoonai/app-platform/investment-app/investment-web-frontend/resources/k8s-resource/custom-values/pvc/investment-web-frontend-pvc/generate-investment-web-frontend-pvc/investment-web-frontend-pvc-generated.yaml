apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: investment-web-frontend-pvc
  namespace: app-platform-dev
  labels:
    app: investment-web-frontend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
