# Redis 中间件总控部署配置
# 注意：Redis 使用 TCP 路由，仅支持 policy（rateLimit），不支持 stripprefix

# 基础配置
REDIS_PROJECT_ID="sunmoonai"
REDIS_NAMESPACE="data-platform-dev"
ENVIRONMENT="development"

# ============================================================================
# 中间件组件启用配置
# ============================================================================
redis_policy_enabled="true"        # Policy 中间件（限流）

# ============================================================================
# 部署优先级控制（数值越大越先部署）
# 建议范围：1-1000
# ============================================================================
redis_policy_priority=100       # Policy 优先级
