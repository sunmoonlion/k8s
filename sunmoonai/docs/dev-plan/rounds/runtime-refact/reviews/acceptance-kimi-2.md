# ⑤b 重验 ｜ `runtime-refact` 轮 ｜ 验收方 `kimi`

> **验收对象**：`sunmoonai/docs/dev-plan/agent-dev-guide.md` @ `a7cfb3f8`（③ 回到后的新 HEAD）。
> **重验范围（自己划定，按通知要求写明）**：
> 1. **只重判 J1**——M1–M7、J2–J8 沿用 ⑤（`acceptance-kimi.md`）的结论，不重复取证；
> 2. **连带复核本次改动是否伤到别处**——`git diff 420c1f06 a7cfb3f8` 全文核对；
> 3. **门禁快核**——在新 HEAD 的克隆上重跑两道门禁，确认修补没有碰坏锚点。
> 除此之外一概不看：不重读候选、不重跑宿主命令、不重算处置表。

---

## 一、结论：**J1 改判为通过**

⑤ 判 J1 未通过的唯一缺口是 `TraceEnvelope` 有名无指路。修补后：

| 复核点 | 结果 |
| --- | --- |
| 定义可得性 | ✅ §5.3（`:483`）现写明「**比较上下文**（源稿称 `TraceEnvelope`…）另含 Task Profile 版本、验收摘要、策略版本、principal 身份域、输入摘要、Side Effect 摘要和证据权威摘要」——**名与内容对上**；§8.2（`:661`）保留清单里的那一处，读者按名检索可到 `:483` |
| 不沿用该名的理由 | ✅ 文中明写（「它只是这一组字段的包装，另起一个名字会让读者以为多了一个对象」）——是编辑判断，不是静默改名，读者知道去哪找权威定义 |
| 源稿删除后的可达性 | ✅ 名与字段组的对应已进本文正文，不依赖源稿存世 |

**fresh 抽验两词**（防「只修被点名的那一个」）：

| 词 | 结果 |
| --- | --- |
| `provenance` | ✅ §5.3 以钉 commit 锚（`runtime-architecture.md @ ed0b5136:363-425`）把轨迹结构整体指向权威定义，三值（`attested/reported/inferred`）的定义正在该范围内；钉 commit 的锚在源稿删除后仍可解析 |
| `Delivery` | ✅ §0 列为内核七对象之一，§0.1 指明对象定义以 `request-lifecycle.md @ ed0b5136:92-343` 为准 |

## 二、连带复核：修补只动了该动的地方

`git diff 420c1f06 a7cfb3f8 -- sunmoonai/docs/dev-plan/agent-dev-guide.md`：**仅一处 hunk、
净增两行**（§5.3 比较上下文句），其余 805 行逐字节相同。修补没有碰 §8.2 的清单原文、
没有碰任何锚点行、没有顺带改其他内容——与通知「只修 J1 那一处」的声明一致。

## 三、门禁快核（新 HEAD 克隆上实跑）

- `doc-gate.py --all` → **198 份通过**（较 ⑤ 时 +2，为本环节新增的通知与验收稿）；
- `anchor-gate.py` → 钉 commit 锚 30 + 裸路径行号锚 187 全通过；
  唯一的硬判失败仍是 ⑤ 已登记的 **N-1**（源稿 `runtime-architecture.md` 的 `task.md:214`，
  环境依赖型假失败，随 §7.3 删除门消失），**不是本次修补引入**。

## 四、沿用声明

M1–M7、J2–J8 沿用 ⑤（`acceptance-kimi.md` @ `0354638e`）的逐条结论：判据冻结、对象改动仅一处、
该处不触及任何 M/J 条的取证基础。⑤ 登记的三件事状态：N-1 未变（见上）；N-2（四家分支最终路径
仍是各家候选）未变，⑦ 仍须按 commit 取件；N-3（`23/2 attested` 指路不精确）未修，仍在修订单元里，
与 `O-4a`/`O-4b` 正文残留一起随 ⑦ 处理（⑤ 四.4 的两条件继续有效）。

## 五、声明

- **R2**：遵守。本次重验未打开、未 `git show`、未 `grep`/`rg` `agent-dev-refact.md`。
- **没查什么**：除上述三项范围外的一切；⑤ 已列的残留边界（落点表 52 行未逐字核等）原样保留，
  不因本次重验缩小或扩大。
- **交付后不再改。**
