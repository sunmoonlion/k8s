# ============================================================================
# Investment Admin Frontend Rate Limit Middleware YAML 生成配置
# 职责：配置 YAML 生成相关的参数和数据内容
# ============================================================================

# 模板文件路径（相对于 resources/k8s-resource/）
TEMPLATE_FILE="templates/middleware/investment-admin-frontend-rate-limit.yaml"

# 输出文件名（生成在当前目录）
OUTPUT_FILE="investment-admin-frontend-rate-limit-generated.yaml"

# 是否启用生成
ENABLED="true"

# 资源类型
RESOURCE_TYPE="middleware"

# ============================================================================
# 基础配置（用于生成 YAML）
# ============================================================================
NAMESPACE="${NAMESPACE:-app-platform-dev}"
ENVIRONMENT="${ENVIRONMENT:-development}"
ENV="${ENV:-dev}"

# ============================================================================
# Rate Limit Middleware 数据内容（用于生成 YAML）
# ============================================================================
RATE_LIMIT_AVERAGE="${RATE_LIMIT_AVERAGE:-100}"
RATE_LIMIT_BURST="${RATE_LIMIT_BURST:-200}"
RATE_LIMIT_PERIOD="${RATE_LIMIT_PERIOD:-1}"
