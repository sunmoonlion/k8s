apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: investment-admin-backend-pvc
  namespace: app-platform-dev
  labels:
    app: investment-admin-backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
