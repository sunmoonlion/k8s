# Llm Admin Backend 命名空间配置
# 用途：创建应用专用的 Kubernetes 命名空间
#
apiVersion: v1
kind: Namespace
metadata:
  name: ${NAMESPACE}
  labels:
    app: app-platform
    component: llm-admin-backend
    environment: ${ENVIRONMENT}
    managed-by: deploy-script
