# 结构示例，请勿直接 apply。各集群复制为 harbor-registry-secret.yaml 后填写 .dockerconfigjson 的 base64。
# 其他组件的 harbor-registry-secret 目录可参考此结构。
apiVersion: v1
kind: Secret
metadata:
  name: harbor-registry-secret
  namespace: ops-platform-dev
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: ""
