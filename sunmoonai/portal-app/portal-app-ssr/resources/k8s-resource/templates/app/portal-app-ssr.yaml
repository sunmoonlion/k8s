apiVersion: apps/v1
kind: Deployment
metadata:
  name: portal-app-ssr
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: portal-app-ssr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portal-app-ssr
  template:
    metadata:
      labels:
        app: portal-app-ssr
    spec:
      imagePullSecrets:
      - name: ${PORTAL_SSR_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: portal-app-ssr
        image: ${PORTAL_SSR_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-IfNotPresent}
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: portal-app-ssr-config
        - secretRef:
            name: portal-app-ssr-secret
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data-storage
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
          subPath: ${PVC_SUB_PATH:-}
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-portal-app-ssr-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: portal-app-ssr
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: portal-app-ssr
spec:
  selector:
    app: portal-app-ssr
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP

