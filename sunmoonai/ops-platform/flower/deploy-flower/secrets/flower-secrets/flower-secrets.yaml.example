# 结构示例，请勿直接 apply。各集群/环境请复制为 flower-secrets.yaml 并填写 base64 编码后的密码。
apiVersion: v1
kind: Secret
metadata:
  name: flower-secrets
  namespace: ops-platform-dev
type: Opaque
data:
  basic-auth-user: ""   # echo -n "admin" | base64
  flower-password: ""   # 实际密码的 base64
