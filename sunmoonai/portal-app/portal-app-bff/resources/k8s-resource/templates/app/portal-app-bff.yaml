apiVersion: apps/v1
kind: Deployment
metadata:
  name: portal-app-bff
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: portal-app-bff
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portal-app-bff
  template:
    metadata:
      labels:
        app: portal-app-bff
    spec:
      imagePullSecrets:
      - name: ${PORTAL_BFF_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: portal-app-bff
        # Harbor 镜像路径：仓库/项目/镜像名:标签
        # 由 deploy-portal-bff.sh 计算并导出 PORTAL_BFF_FULL_IMAGE_NAME
        image: ${PORTAL_BFF_FULL_IMAGE_NAME}
        # Always: 总是从 Harbor 拉取最新版本，避免使用节点本地的旧镜像
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: portal-app-bff-config
        - secretRef:
            name: portal-app-bff-secret
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data-storage
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
          subPath: ${PVC_SUB_PATH:-}
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-portal-app-bff-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: portal-app-bff
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: portal-app-bff
spec:
  selector:
    app: portal-app-bff
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP

