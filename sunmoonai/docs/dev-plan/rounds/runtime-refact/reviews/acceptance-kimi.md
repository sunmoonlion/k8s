# ⑤ 验收 ｜ `runtime-refact` 轮 ｜ 验收方 `kimi`

> **验收对象**：`sunmoonai/docs/dev-plan/agent-dev-guide.md` @ `420c1f06`
> （806 行 / 56370 字节 / sha256[:16] `949bb650b1bb843e`；取件 `git show 420c1f06:<路径>`，不看工作区）。
> **验收方身份**：`kimi`，由处置表算出（计算过程我独立复核，见四.2）。
> 身份确认：`git rev-parse --show-toplevel` → `…/worktrees/kimi/k8s` → `kimi`。
> **判据**：任务书 §8（`runtime-refact-task.md` @ `420c1f06`），逐条给结论，不改判据。

---

## 一、结论

| 条 | 结果 |
| --- | --- |
| M1–M7 | **全部通过**（M3 附一条环境相关发现 N-1；M4/M5 人核语境通过） |
| **J1** | **未通过**——抽三词，一词（`TraceEnvelope`）有名无指路。缺口一行可修，修法见三.J1 |
| J2–J8 | **全部通过** |
| 四处重点 | 三条改判**全部复核成立、未过头**；验收方计算**复核无误**；落点表机械保真 **64/64**；`O-4b`（及 `O-4a` 残留）留到 ⑦ **判定为可接受**，附两条件（四.4） |

**综合评估**：806 行的稿子里，判据命中的缺口只有 J1 一处，且与 ④ 已登记的 `O-4a`/`O-4b`
正文残留同属「遗漏级」（该写的没写全），不是「错误级」（写了假话）。我建议把 J1 缺口与
`O-4a`/`O-4b` 后果并入**同一个修订单元**随 ⑦ 处理；是「随修订单元通过」还是「打回新 Attempt」，
由 ⑥ 裁定——我按冻结判据给出的逐条结论不变，不为了整稿通过而把 J1 改写成通过。

---

## 二、机械条 M1–M7

复用裁决方修过四次的判据脚本（`check-mechanical.py`），结果逐条人核：

| # | 结果 | 我的取证（全部实跑，非抄声明） |
| --- | --- | --- |
| M1 | ✅ | 脚本：64 行（F 31 + R 33）。**我另独立数了源稿**：`refact-fable.md @ ed0b5136` 有 39 个 `##` 级标题 − 8 个 `####` = **31**；`runtime-architecture.md @ ed0b5136` 有 46 − 13 = **33**。前提「31+33=64」本身属实 |
| M2 | ✅ | 脚本判全满足；人核 5 行「故意不要」（R3.1/3.2/3.3/3.5、R4.4）：理由均写明**被什么取代**（库的 `Command(resume=)`、真实动作路径不设旁路等），无一行是「已过时」式空理由（F4 不命中） |
| M3 | ✅（本稿范围内） | 在 `420c1f06` 的干净克隆上实跑：`doc-gate.py --all` → **197 份通过**；`anchor-gate.py` → **钉 commit 锚 30 + 裸路径行号锚 184 全通过**。⚠ 门禁全仓另有 1 处硬判失败，在**源稿** `runtime-architecture.md`，不在本稿——见五.N-1 |
| M4 | ✅ 人核 | 五词中 `amend.mode` 零命中；`amend_schema`／回执仓／候选仓／三道边界**各恰 1 命中，全部在 §8 第 634 行**——隔离区自己的规则句（「被证伪的专名只在本节出现」）。§10 表中三处源题原名以 HTML 实体转义，渲染语境均标「已推翻／故意不要」。**引用一个被推翻的名字以说明它被推翻，不是复活它** |
| M5 | ✅ 人核 | 字面零命中；§10 两处实体转义标题（F 3.1「两个 Profile」、F 3.10「开发 Profile」）语境为「撤销部署形态分层」「不造部署分层」——证伪语境，不是架构分层并列 |
| M6 | ✅ | `kind = "human"` 零命中；§2.3 明写「人登记在 principal 表…没有 harness、执行粒度或执行角色」 |
| M7 | ✅ | §9 覆盖声明在；§9.2「没查什么」非「无」：三类分列（宿主恒等／远端授权／执行者内部）加「不能排除」档，含 6 条具体没查项 |

## 三、判断条 J1–J8

### J1 新读者可读——❌ **未通过（一处）**

随机抽三词：

| 词 | 结果 |
| --- | --- |
| `fencing` | ✅ §5.4 指向产品 `I14`；内核 `request-lifecycle.md:461` 有其不变量定义 |
| `RouteDecision` | ✅（偏弱）§2.1 给出产生者与值域（router 由持久化字段产生 `decide/ask/refuse` 和 RouteDecision），属及格的内联刻画 |
| **`TraceEnvelope`** | ❌ **全文仅 §8.2 保留清单出现一次，无定义、无指路**。其内容（契约版本／验收摘要／策略版本／身份域／输入与副作用摘要）已被 §5.3 以「比较上下文」**无名吸收**；源稿删除后，这个词在所有存世文档里将没有任何定义 |

**修法一行**：§5.3「比较上下文」处注明「即源稿的 `TraceEnvelope`」。
⚠ 附言：裁决方自检抽的另三词（`principal`／`tool.enforced`／`dispatch_event`）我复核均合格——
但抽查点仍落在自己刚补过的区域，`TraceEnvelope` 正是漏网的那一类：**覆盖不全的第七个实例，形态相同**。

### J2 落点表属实——✅

抽 **9 行打开正文**（刻意与裁决方已核的 3 行不重叠），全部命中、无「声称而无内容」：

| 落点表行 | 核到的内容 |
| --- | --- |
| F 3.5 路由 → §2.1、§2.5 | §2.1 三值 `decide/ask/refuse` 加模型只建议；§2.5 只读可判字段表 |
| F 3.7 工作区供给 → §3.2 | `provision(...)` 六参签名加独占/干净/基线三判据 |
| F 3.12 验证分层 → §5.5 | L0–L3 四层表加「顺序不可倒」 |
| F 5.3 删除条件 → §3.5、§7.3 | §7.3 六条删除门；§3.5 清理纪律 |
| F 2 设计原则 → §1、§5.2 | P0→§1.1/§3.4；P1→§1.2；P2→§3.4；P3→§3.1；P4→§5.2；P5→§5.5，六条逐一可寻 |
| R 2.7 等效判据 → §5.3 | S/R 两层加轨迹条目字段加比较规则六条 |
| R 4.1 粒度三字段 → §5.1 | 三字段表加四档定义 |
| R 5.3 T0 开销上界 → §6.2 | 复合向量 `(2,4,2,0)` 加升档条件 |
| R 4.5 独占是约定 → §3.2 | 「CLI/GUI 腿通常只是约定」加 `enforced/convention` 登记要求 |

### J3 §1.1 六条处置有据——✅（我独立复跑，非照抄，F6 不命中）

§8.1 六条「独立观察 → 结论与落点」相邻。我逐条复跑：

| # | 复跑结果 |
| --- | --- |
| K1/K2 | `pilot_graph.py:59-66` `interrupt({...})` 收任意 dict ✅；`langgraph_runtime.py:13-21` `Command(resume=user_input)` ✅；`graph_runtime_service.py:14-23` 同 `session_id→thread_id` ✅；`agent_graph.py:106-128` 同 thread 配置加 PostgreSQL checkpointer ✅（investment-backend @ `18d88c7`） |
| K3 | 宿主非沙箱 `sudo -n -l` → **`(ALL) NOPASSWD: ALL`** ✅（沙箱内因 no-new-privileges 失败的形态也与 §8.1 记载一致） |
| K4 | `git branch -vv`：仅 `master` 有 upstream；`git ls-remote --heads origin`：`cursor/kimi/luna/master/opus/qwen` **六头同 SHA** ✅ |
| K5 | `git ls-remote --tags origin`：仅 `2.0.0` 与 `pre-architecture-v2-final-20260813` ✅ |
| K6 | 由 K3–K5 推出，逻辑闭合 ✅ |

### J4 「立得住」与「站不住」分开——✅

立得住的五件全部保留完整：对象模型（§0/§1.1，内核七对象齐）、粒度（§5.1，含四档逐档定义）、
证据等级（§5.2 E0–E4 加 min 采信）、权力表（§4.2 H1–H8，动作加 principal 加强制点形状）、
等效判据（§5.3 S/R 层加比较上下文）。站不住的一支只在 §8 隔离区与 §10 表注出现（M4/M5 已核）。

### J5 取证质量——✅（一处指路不精确，记 N-3）

抽 12 处断言核锚，**锚点全部存在且支持断言**：

- 代码锚 5 处（K1/K2 行，见 J3 表）逐行打开核过；
- `graph_runtime_service.py:26-31` 的 `NotImplementedError` 原文逐字相符；
- `forensics.md @ ed0b5136:18-45`：免密 sudo（F1）、双向 push dry-run 未被拒（F5/F6）、deploy key 有效、VM 门禁无效，四项均在该范围；
- `request-lifecycle.md @ ed0b5136:106-119`（Task 不等于 Attempt）、`:247-277`（Interaction 七字段逐名在）、`:444-483`（I4 在 +8、I13 在 +17）、`:398-440`（持久化先于 Delivery）；
- `constraints.md @ ed0b5136:149-159`（A1–A5 五行齐）、`:95-103`（T4/T5 在范围内）；
- `runtime-architecture.md @ ed0b5136:363-425`：等效结构（S/R、provenance 三值、TraceEnvelope 定义、最低等级宣称）确在该范围。

⚠ 一处加重档的近失：**§4.3 的 ⚠ 段**（「不证明本项目已把中断恢复跑通」「留给实现方的空位」）——
锚点支持字面断言，但断言的**结论**已被同仓 e2e 测试推翻（见四.1 `O-4a`）。
这发生在 ④ 改判之前，按「产物冻结」登记，不按 J5 新罪计。

### J6 不重写权威定义——✅

§3.3 投影表出现的状态词全部在内核合法转换表内（`CREATED/RUNNING/WAITING/COMPLETED/BUDGET_EXCEEDED`
核对 `request-lifecycle.md:313-343`）；§0.1 声明的「引用什么/不做什么」与正文实际一致；
未见对内核对象的重定义（B3 不命中）。

### J7 §4.3 那条边界——✅

§1.4 写入且未淡化：「`dev.change/1` 跑通只证明开发场景…**没有解决判断且昂贵的那一半**」，
并要求财务 Task Profile 以真实验收用例重证。任务书 §4.3 的要求逐字落实。

### J8 自增内容质量——✅

七处自增逐条有论证或取证：§9.3 自述的三条（最小绑定／真实路径强制点／G0–G5 重排）各有 §4.3、§8.1
代码与命令证据；§3.3「第 3 条最容易被跳过」（顺看/倒看的论证）；§9.2「没查」三类（分类理由写明）；
§2.3「多家一致≠多路独立信号」（**忠实执行了 K3 的部分接受**——正文有「只作观察值，不作硬规则」⚠ 注）；
§4.2 H7 失败安全方向（伪造取消只停不放行的论证）。无 F1 形态的散文。

## 四、四处重点复核

### 1. 三条改判是否又过头——**没有，三条全部成立**

按冻结 commit（`d8fdb32a` cursor / `dc8efd59` kimi，sha256 与 ① 冻结表相符）核行号：

- **`A-3`（推翻→成立）**：`cursor:373` 与 `kimi:342` **逐字相同**（「人的 Attempt 无 checkpoint 义务」），
  免责声明等价（`cursor:300` ↔ `kimi:23`、`:319`）。`A-2` 既判成立，`A-3` 同标准必须成立——
  `qwen` 的「判决未闭合」指控属实。**保留的区分（`cursor:229` 不构成违反）是对的**：
  该行执行者列是 `publisher COMPLETED`，注文「Side Effect 由人执行本地合并」描述手工期的物理动作——
  按裁决稿 §4.1 自己的口径，这类动作是 `dispatch_event` **欠账**而非执行者建模；
  它没有给人分配 Attempt 或 kind。而 `:373` 把 Attempt（执行者概念）安到人头上，
  即是否定义句也采纳了「人是一种执行者」的旧框架。**改判范围止于这一句，后果与 K4 对称
  （C10 不采纳），未扩大。**
- **`O-4a`（resume 未接线→已接线）**：实跑 `uv run pytest tests/test_graph_runtime_service.py`
  → **2 passed**；适配器 `LangGraphRuntimeService.resume` 实现逐行核过。改判正确。
  ⚠ **残留**：裁决稿 §4.3 的 ⚠ 段未随改判更新（④ 自记「原文表述要改」，但正文已冻结）——
  与 `O-4b` 同属已知未改进正文项，并入四.4 处置。
- **`O-4b`（并发语义「验不了」→服务态已验）**：实跑全量 `uv run pytest` →
  **156 passed, 2 skipped in 5.20s**，与 ④ 引用一致。改判正确。

### 2. 验收方计算——**复核无误；偏向排除方法如下**

独立重数冻结处置表（`disposition.md` @ `420c1f06` 三、`disposition-objections.md` 三）：
`cursor` 接受 8（C1–C8）、不采纳 2（C9、C10）；`qwen` 接受 4（Q1–Q4）；
`kimi` 接受 2、部分接受 1、不采纳 1 → 计入 **3**。排除 `opus`（裁决方）、`luna`（基座作者）；
`fable` ① 期间退赛（R3）。最小者 = `kimi` ✅。**稳健性**：部分接受按 1 / 0.5 / 0 三种口径计，
结果不变（3<4 或 2<4），没有留给裁量的缝。

**我怎么排除自身偏向**（我是被算出来的那一家）：

1. 数字全部出自冻结产物，任何人可对同一 commit 重数；我若虚报，**立即可被查**；
2. 我先把三家逐条裁定重新过一遍再算合计，不是抄结论行；
3. 我的利益方向与表面相反：当选意味着承担验收工作，要避选须把自己计多——
   那需要篡改冻结表，不可行；
4. 本验收稿本身接受复核，虚报的代价高于当选的代价。

### 3. 落点表 64 行——**机械保真 64/64，正文抽核 9+3 行，残留 52 行如实登记**

- **全部 64 行**逐行与 `ed0b5136` 源稿 `##`/`###` 标题比对：**0 处真实不符**
  （7 处表面不符全是行内代码或 HTML 实体转义所致，还原后逐字相同）；**无重复行**——
  「骨架机械生成」「一个标题恰有一处落点」两条规矩成立，这比抽查更能排除漏行（漏行不会有人发现）；
- 打开正文抽核 9 行（J2，与裁决方的 3 行不重叠），无伪落点；
- **未逐字核的 52 行是本轮覆盖的残留边界**，显式标出，不默认通过（B5）。

### 4. `O-4b`（及 `O-4a` 残留）留到 ⑦——**判定：可接受，附两条件**

理由：

1. 冻结文本**没有一句是假话**：§5.4 的主语是 Git 载体，「Git 不能证明事务/租约/fencing」为真；
   缺的是「服务态判据已有测试在跑」这半截现状——**属遗漏，不属错误**；
2. 误导的代价有界：按 §7.1 执行 G1 的人第一步就会撞上那 156 个测试（5.2 秒跑完），
   最坏结果是重复发现一次；
3. 若因此打回，806 行的稿子要重走发布与验收，而修法只是两三句加测试名引用——成本倒挂；
4. 这与本稿自己的标准一致（§5.2：零命中要区分「没查到」与「没有」）。

条件：

- **修订单元现在就以本验收稿登记**：§4.3 ⚠ 段、§5.4/§7.1 各一处；修订必须**引用测试名**
  （`test_graph_runtime_service.py` 的 e2e 恢复测试；`test_runtime_selection_spike.py` 的
  `test_interrupt_resume_executes_side_effect_once` / `test_retry_after_crash_after_commit_does_not_repeat_effect`；
  `test_execution_identity_spike.py` 的 fencing 测试等），不接受无锚改写；
- ⑦ 或修订单元落地前，任何人引用裁决稿论证「中断恢复未接线／并发语义未验证」时**须注明本条**。

## 五、验收中新发现的三件事（登记，不改判据、不改冻结物）

- **N-1 ｜ anchor-gate 的判定依赖执行环境。**在 `420c1f06` 克隆上 anchor-gate 报 1 处硬判失败：
  源稿 `runtime-architecture.md` 的 `task.md:214`——索引里有两份 `task.md`
  （`rounds/runtime/` 与 `rounds/executor-adapter/`），基名消歧失败后退到外部仓，
  命中 `deepseek-harness` 里一个同名 ISSUE_TEMPLATE（20 行）。**该失败在自检 commit `6228ed18`
  上同样存在**，裁决方自检「M3 全过」的声明在本机不可复现；同一门禁在组织者机器与验收方机器
  结论不同——「判不了的显式标出」在这里又多了一个实例。被指的锚在冻结源稿内，随 §7.3 删除门
  一并消失；建议 ⑦ 给 anchor-gate 加「基名歧义时硬判不唯一，不退外部仓」。
- **N-2 ｜ 四家工作分支的共享最终路径仍是各家 ① 候选。**实测：`kimi` 分支 917 行、`cursor` 921、
  `luna` 648、`qwen` 187，**均与裁决稿不同**（各家 merge master 时的冲突解决留了自家版本）；
  `master`（`b5fe40be`）与 `420c1f06` 的裁决稿**逐字节相同**。**⑦ 整合与任何引用必须按
  commit（`420c1f06`）取件，不得按分支工作区。**这也说明为什么我把 M3 挪到 `420c1f06` 的克隆上跑：
  门禁按索引判，在 kimi 工作区跑测到的是我的候选，不是裁决稿。
- **N-3 ｜ §5.3「23 条里只有 2 条 `attested`」指路不精确。**读数属实（源稿 `:45` 及 §2.8 样例），
  但不在所钉范围 `ed0b5136:363-425` 之内；锚点支持主断言（等效结构有效），读数指路越界。轻微，
  随修订单元一并处理即可。

## 六、覆盖声明

**查了什么**（除上文逐条所列命令外）：任务书 §8 全文；`disposition.md`、`disposition-objections.md`、
`mechanical-results.md`、`arbiter-selfcheck.md`、`observations-opus.md`（均 @ `420c1f06`）；
裁决稿全文两遍；cursor/kimi 两份候选的 A-3 相关行（按冻结 commit 加 sha256 校验取件）；
源稿两份在 `ed0b5136` 的全部标题与抽核节正文；内核、constraints、forensics、handoff 的被锚范围；
investment-backend @ `18d88c7` 的五处代码锚与两轮 pytest（2 passed；156 passed, 2 skipped）；
宿主 `sudo -n -l`、`git ls-remote --heads/--tags origin`、`git branch -vv`（均非沙箱实跑）。

**没查什么**：

- 落点表 52/64 行未打开正文逐字核（机械保真已全覆盖，见四.3）；
- 189 处 C 类章节号引用（门禁软判）未逐条人核；
- cursor/kimi/qwen 三份候选**未全文重读**——只按 `A-3` 所需行号取读；③④ 已处置的部分我不重做裁决；
- 其他四台宿主的 sudo/远端事实未核（本机已核）；远端**写**授权未核——`ls-remote` 是读，不是 push dry-run；
- `rounds/` 历史轮次全文未穷读。

**不能排除**：

- 我在 ② 是互评方、④ 是异议方，读过四份候选与全部处置记录，**不能假装没有先验**。
  降低方法：本稿每条结论只引可复跑命令与冻结行号，不引记忆。

**R2 声明**：遵守。未打开、未 `git show`、未 `grep`/`rg` `agent-dev-refact.md`，未以它为起点。

**交付后不再改。**
