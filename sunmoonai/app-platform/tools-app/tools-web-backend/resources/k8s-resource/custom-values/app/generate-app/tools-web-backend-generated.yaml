apiVersion: apps/v1
kind: Deployment
metadata:
  name: tools-web-backend
  namespace: app-platform-dev
  labels:
    app: tools-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tools-web-backend
  template:
    metadata:
      labels:
        app: tools-web-backend
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: tools-web-backend
        image: harbor.sunmoonai.com:30443/app-images/tools-web-backend:1.0.0
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: tools-web-backend-config
        - secretRef:
            name: tools-web-backend-secret
        - secretRef:
            name: tools-web-backend-postgresql-conn
        - secretRef:
            name: tools-web-backend-redis-conn
        - secretRef:
            name: tools-web-backend-mongodb-conn
        - configMapRef:
            name: tools-web-backend-s3
        - secretRef:
            name: tools-web-backend-s3
        - configMapRef:
            name: tools-web-backend-elasticsearch
        - secretRef:
            name: tools-web-backend-elasticsearch
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: /app/data
        - name: elasticsearch-ca
          mountPath: /var/run/secrets/sunmoonai/elasticsearch
          readOnly: true
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: tools-web-backend-pvc
      - name: elasticsearch-ca
        secret:
          secretName: tools-web-backend-elasticsearch
          items:
          - key: ca.crt
            path: ca.crt
---
apiVersion: v1
kind: Service
metadata:
  name: tools-web-backend
  namespace: app-platform-dev
  labels:
    app: tools-web-backend
spec:
  selector:
    app: tools-web-backend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
