# REQ-XXX：<短名>

> 状态：PROPOSED ｜ 提出日期：YYYY-MM-DD ｜ 评审日期：—

<!-- 命名：文件夹 REQ-<三位序号>-<短名>，序号全局递增不复用、短名不含日期/状态，
     详见 AGENTS.md §5 命名规范。
     序号无自动递增机制：创建时由创建者（通常是接请求的助手；手动建同规）
     看本目录现有最大序号 +1 填写；提出日期填创建当天，评审日期评审后补
     （之前保持 —）。
     一个 REQ 文件夹 = 一次开发任务请求的闭环（提议→审核→定任务→实施→评估→按需更新 baseline，
     不必每步都走）：request.md（本文件）+ 按需派生 baseline.md / plan-*.md / handoff.md。 -->

## ① 原始需求

<!-- 用户原话，一字不改。这是意图证据，评审过程中不得改写。 -->

## ② 架构评审

<!-- 结论四选一：采纳 / 修改后采纳 / 不采纳 / 待澄清。
     附理由：依据（或冲突）的架构原则，引用 baseline/overall/ 两份文档的 §编号；
     若与现有架构冲突，必须摆出"按 request 做的代价 vs 维持现状的代价"两个选项，由用户拍板。 -->

- 结论：
- 理由：
- 冲突与代价对比（如有）：

## ③ 落地去向

<!-- 采纳后写进了哪：本文件夹 baseline.md §X / 某 App 任务 ID / 新 ADR / baseline/ §X。
     目标态事实写进基线或任务文件，本文件只留引用，不复述（维护约定 1）。
     不采纳的也记录去向：无，保留本记录以备条件变化后重新评审。 -->

## ④ 状态流转

<!-- PROPOSED → REVIEWED → ADOPTED/REJECTED → IMPLEMENTED -->

| 日期 | 状态 | 说明 |
| --- | --- | --- |
| YYYY-MM-DD | PROPOSED | 用户提出 |
