# ============================================================================
# Casdoor 部署后初始化配置
# 供 post-deploy-setup.sh 读取
# ============================================================================

# ----------------------------------------------------------------------------
# PostgreSQL 连接（默认从 db-access-bootstrap/config/postgresql.external.env 读取）
# 如需覆盖，在此取消注释并填值
# ----------------------------------------------------------------------------
# DB_HOST=www.sunmoonai.com
# DB_PORT=30444
# DB_USER=casdoor
# DB_PASSWORD=casdoor_dev_pass
# DB_NAME=casdoor

# 业务 Organizations/Applications/Admin 密码不再放在仓库配置中。
# 统一由 operator-only 的 post-deploy-setup.local.conf（或 P0-005 的
# 声明式 identity provision Job）注入；没有本地配置时本脚本只做数据库
# 不变量与运行时检查，不会创建带 placeholder credential 的业务应用。
