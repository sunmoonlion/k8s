# 从远程集群到 Kind 的迁移指南

## 1. 迁移目标与策略

本指南说明如何在现有项目上支持 Kind，并保证**切换 Kind/远程时行为一致**：同一套部署流程，先可在 Kind 上跑，再切到远程同样跑。统一策略见 **《策略.md》**。

- **远程集群（现有）**：使用 `deploy-infrastructure-all`（Step00～Step12）在多台机器上完成从 OS 到 K8s、CNI、存储等的全流程，得到「已有集群」。
- **Kind 集群（新增）**：用 `kind-up.sh` 创建集群并做平台初始化（命名空间、NFS）；之后与远程共用**同一套应用/平台部署命令**，仅通过 k8s-admin.conf 和 KUBECONFIG 切换目标。

因此：**迁移 = 多支持一个目标（Kind），并约定：当前目标由配置决定，部署命令一致，KUBECONFIG 统一。**

---

## 2. 配置与步骤对照

### 2.1 在 Kind 中不再使用的部分

| 类别 | 说明 | 原因 |
|------|------|------|
| **服务器节点配置** | `C1_SERVER_n_*` / `C2_SERVER_n_*`（IP、SSH、USER、DIR、HOSTNAME 等） | Kind 节点是 Docker 容器，无 SSH、无独立机器 IP |
| **Step00** | 集群重置、恢复 hostname/swap、卸载 K8s/运行时等 | 由 `kind delete cluster` + 重建替代 |
| **Step01** | OS 基线（swap、sysctl、内核模块、hostname、时间同步） | 由 Kind 节点镜像内建 |
| **Step02** | 容器运行时（nerdctl-full/containerd）安装与 registry 配置 | Kind 自带 containerd |
| **Step03** | kubeadm/kubelet/kubectl 安装 | Kind 节点镜像已包含 |
| **Step04** | kubeadm init（控制面初始化） | `kind create cluster` 时自动完成 |
| **Step06** | 节点加入（kubeadm join） | Kind 通过配置文件定义节点并自动加入 |

**结论**：在 Kind 模式下，上述配置与步骤应**跳过**，不应被 deploy 脚本执行。

---

### 2.2 可复用思路、需简化实现的部分

| 步骤 | 在原配置中的作用 | 在 Kind 上的建议 |
|------|------------------|------------------|
| **Step07** | 命名空间平台管理（dev/prod、各 platform 命名空间及策略） | 保留“创建哪些命名空间、应用哪些策略”的配置思路；改为对本机 kubeconfig 执行 `kubectl`/Helm，不依赖 SSH 与远程 admin.conf |
| **Step09** | 存储（本地存储、NFS、云存储、StorageClass） | **Kind 使用 WSL 上的 NFS**（见《kind使用指南.md》5.7）：WSL 跑 `wsl-setup-nfs-server.sh`，集群内跑 `apply-nfs-existing-cluster.sh`，创建 StorageClass **nfs-2**。应用 values（如 dev-values）中指定 `storageClassName: nfs-2` 即可，Kind 与远程一致，无需部署脚本内做 Kind 专用覆盖。 |
| **Step10** | 节点污点（按 SERVER_n 设置 taint） | 仍可给节点打污点，但节点名为 kind 默认（如 `kind-control-plane`、`kind-worker`）；配置需从“按 SERVER_n”改为“按节点名或 label” |
| **Step11** | 镜像预加载、Harbor DNS（/etc/hosts） | 镜像改为在宿主机 `docker pull` + `kind load docker-image`；Harbor 访问可在宿主机 /etc/hosts 配置，或通过 Ingress/NodePort 在集群内访问 |
| **Step12** | 统一根 CA / 证书管理 | 逻辑不变，在 Kind 集群内通过 Kubernetes API/Secret 执行即可，无需 SSH 到节点 |

---

## 3. 推荐迁移节奏（分阶段）

### 阶段一：验证“现有清单能在 Kind 上跑”

- 使用 **`kind-up.sh`** 一键完成「创建集群 + 命名空间 + NFS」（前置：WSL 已跑 `wsl-setup-nfs-server.sh`）。
- 在 `k8s-admin.conf` 中设置 `cluster_mode=kind`、`default_cluster=KIND`，用连接管理器或 `KUBECONFIG` 连接到 Kind。
- 从现有仓库中选取部分 YAML/Helm（业务或平台），用 `kubectl apply` / `helm install` 在 Kind 上手动执行，确认无硬依赖“多机或特定节点 IP”。

**目标**：确认业务与平台清单对 Kind 兼容，无需改业务代码。

### 阶段二：抽取“对现成集群”的脚本

- 编写 1～2 个独立脚本（如 `apply-namespaces-existing-cluster.sh`、`apply-nfs-existing-cluster.sh`），输入为当前 kubeconfig 或 context。
- 脚本内容：创建 Step07 中的命名空间与策略、创建 1 个简单的 StorageClass（local-path），**配置与远程同源**（读 deploy-infrastructure-all.conf），行为与远程一致、仅执行方式改为本机 kubectl。
- 不修改原有 `deploy-infrastructure-all` 主流程，仅作为“Kind 或任意现成集群”的补充工具。

**目标**：用最少脚本在 Kind 上复现“平台层”核心能力。**详细设计、配置项见《kind使用指南.md》第 5.7 节。**

### 阶段三（可选）：与现有入口集成

- 在 README 或 Makefile 中区分：
  - `deploy-remote` / 现有脚本：仅当 `cluster_mode=remote` 时跑 Step00～Step12。
  - Kind 使用 **`kind-up.sh`** 一条脚本完成创建与平台初始化。
- 保持 `deploy-infrastructure-all.conf` 为远程部署的单一权威配置，Kind 使用独立配置或少量环境变量即可。

---

## 4. 需要改动的范围（尽量小）

| 改动点 | 说明 |
|--------|------|
| **连接与模式** | `k8s-admin.conf` 中已有 `cluster_mode` 与 `[KIND]`；deploy-infrastructure-all 在目标为 Kind 时明确提示并退出/跳过，不执行 Step00～06。 |
| **KUBECONFIG 统一** | 应用/平台部署脚本统一依赖环境变量 `KUBECONFIG`（由连接管理器或从 k8s-admin 解析），**不写死** kubeconfig 路径，以保证切换 Kind/远程时同一套命令生效。 |
| **新增脚本** | 仅新增“对现成集群”的 1～2 个脚本（命名空间、简单 StorageClass），对当前 KUBECONFIG 指向的集群执行，供 Kind 或任意已有集群使用。 |
| **业务与 Chart** | Helm Chart、业务 YAML 尽量不改；若有硬编码 IP/主机名，再按需改为可配置或兼容 Kind。**存储**：Kind 使用 WSL NFS，StorageClass 为 nfs-2；dev/prod values 中指定 `storageClassName: nfs-2` 即可，Kind 与远程一致，无需部署脚本内做 Kind 专用覆盖。 |

---

## 5. 小结

- **不必“迁移整个项目”**：远程部署体系保留，Kind 作为额外目标；**切换 = 改 k8s-admin.conf + 跑连接管理器**，部署命令不变。
- **节点与系统层（Step00～06）在 Kind 上全部跳过**；Kind 的平台层由独立小脚本对「当前集群」执行，与 deploy-infrastructure-all 解耦。
- **一致性**：先在 Kind 跑、再切远程同样跑——同一套应用/平台部署流程，仅目标由配置与 KUBECONFIG 决定。详见 **《策略.md》**。

更多 Kind 的具体操作见 **《kind使用指南.md》**。
