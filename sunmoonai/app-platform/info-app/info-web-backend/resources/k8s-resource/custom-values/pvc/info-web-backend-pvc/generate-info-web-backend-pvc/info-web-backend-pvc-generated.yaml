apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: info-web-backend-pvc
  namespace: app-platform-dev
  labels:
    app: info-web-backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
