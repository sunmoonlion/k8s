# Kind 与远程集群统一策略

## 1. 目标

- **在 Kind 上跑一套**：本地用 Kind 开发、验证。
- **切换到远程同样跑**：改配置、切连接后，用**同一套命令和流程**在远程集群上部署。
- **行为一致**：不因目标是 Kind 还是远程而换脚本或换用法，仅通过「当前目标」切换。

---

## 2. 统一入口：当前目标 = 当前生效的 KUBECONFIG

**统一三者的根本基础**：无论远程直连、远程经连接管理器（隧道）、还是本地 Kind，都是**本机 kubectl 客户端**根据 config 里的 API 地址发 HTTPS 请求；区别仅在于 config 里填的地址指向哪里——远程 IP、本机隧道端口、或本机 Kind 暴露的端口。因此只要「当前生效的 kubeconfig」指向目标集群的 API，同一套部署命令即可复用。

- **配置**：`k8s/utils/k8s-admin.conf` 中 `cluster_mode`、`default_cluster` 等，用于**选当前要操作的集群**；切换目标时改配置，并让「当前生效的 kubeconfig」指向该集群即可。
- **何时需要连接管理器**：  
  - 远程集群 API **不可直连**（需跳板机或 SSH 隧道）时：用 `k8s-connection-manager.sh` 建隧道，并把 kubeconfig 里的 server 改成 `localhost:端口`，再设置 `KUBECONFIG`。  
  - 远程集群 API **可直连**时：部署好后把远程的 admin.conf 拷到本地（或 scp 下载），其内 server 已是可访问地址；直接 `export KUBECONFIG=~/.kube/cluster-c1-admin.conf`（或合并进 `~/.kube/config` 并切换 context）即可在本地跑 kubectl，**不必**用连接管理器。  
  - Kind：可直接用 `kind get kubeconfig` 得到的文件，或通过连接管理器统一设置 `KUBECONFIG`（二者皆可）。
- **统一约定**：无论用不用连接管理器，部署脚本只认「当前环境的 KUBECONFIG 或默认 config 的 current-context」；不写死 kubeconfig 路径。这样「下载好 config 直接 kubectl」与「通过连接管理器设置 KUBECONFIG」两种方式都符合策略。

### 2.1 连接管理器做的事

连接管理器只做一件事：**通过 SSH 把远程的 config 下载到本地，并改写其中的 server 地址**（同时会先建 SSH 隧道，再把 server 改成 `127.0.0.1:隧道端口`，这样 kubectl 的流量经隧道到远程 API）。  
结果就是：本地拿到一份「已经指向可访问地址」的 config，kubectl 直接用这份 config 即可。

因此：**kubectl 客户端不用区分是直连还是连接管理器**，它只要认 config——当前 `KUBECONFIG` 指向哪份 config，就按那份 config 里的 server 和认证去连。直连时 config 里是远程 IP，经连接管理器时 config 里已是本机隧道端口，**如何连接由 config 决定**，kubectl 不关心。

**为何还要运行连接管理器**：kubectl 只认「当前环境的 KUBECONFIG」；不运行连接管理器的话，没有任何东西帮你把该变量设成指向远程（或隧道）用的那份 config。所以要在本机执行 kubectl 操作远程，要么 (1) 运行连接管理器（脚本会下载/改写 config 并**在当前 shell 设置 KUBECONFIG**），要么 (2) 自己先拿到 config 文件后**手动** `export KUBECONFIG=...`。连接管理器 = 拿 config + 设好环境变量，一步到位；直连时你可以手动设一次 KUBECONFIG，Kind 同理。

**Kind**：config 里的 server 是本机 Kind 暴露的地址，同样由 config 决定连接方式；用前也需让 KUBECONFIG 指向该 config（运行连接管理器或手动 export）。

### 2.2 现状与问题：部署脚本通过统一模板“先连再部署”，退出时会清理连接

- **现状**：k8s 应用部署脚本（如 deploy-sunmoonai-all、各平台 deploy-*-all）在开头会 **source `k8s/utils/unified-deployment-template.sh`**，在需要执行 kubectl/Helm 前会调用模板提供的 **setup_kubectl_environment()**。该函数会：若已有连接状态则复用，否则调用 **start_k8s_connection()** 建立连接——逻辑与连接管理器一致（读 k8s-admin.conf、建 SSH 隧道、下载并改写远程 config、设置 KUBECONFIG 并写入状态文件）。因此实际上是**先连再部署**，只是连接逻辑在统一部署模板里，而不是单独运行 `k8s-connection-manager.sh`。
- **连接管理器（独立运行）退出时的行为**：`k8s-connection-manager.sh` 在退出时（trap cleanup）会 **unset KUBECONFIG** 并关闭隧道。若用户是「先在一个终端跑连接管理器、再在另一终端跑部署」，则部署脚本所在进程并未继承该 KUBECONFIG，部署脚本会通过 source 的模板再次建立连接（setup_kubectl_environment）。
- **统一模板在部署脚本退出时**：trap 会调用 cleanup → cleanup_k8s_connection（关闭隧道、清状态文件），一般不会在该进程内显式 unset KUBECONFIG（进程退出后环境自然消失）。即：部署脚本运行期间由模板建连并设置 KUBECONFIG，脚本结束时模板会清理隧道等资源。
- **改进方向（与策略一致）**：统一模板需完整支持 **cluster_mode=kind**（当前 read_k8s_config 仅处理 C1/C2，start_k8s_connection 仅有 bastion/direct/local）。应在模板中增加 kind 分支：当 cluster_mode=kind 时，直接根据 [KIND] 段设置 KUBECONFIG，无需隧道。同时去掉各子脚本中写死的 kubeconfig 路径（如 `KUBECONFIG=~/.kube/cluster-admin.conf`），统一依赖模板设置的 KUBECONFIG。

---

## 3. 两条路径准备集群，同一套命令部署应用

### 3.1 集群就绪（目标不同、只做一次）

| 目标   | 如何得到「已有集群」 | 说明 |
|--------|----------------------|------|
| **Kind** | `kind-setup.sh create`（或 `kind create cluster`） | 可选：再跑「对现成集群的平台初始化」（命名空间、简单 StorageClass），与远程布局对齐。 |
| **远程** | `deploy-infrastructure-all`（Step00～Step12） | 在 C1/C2 机器上从零装系统、K8s、CNI、存储等，得到与 Kind 等价的「已有集群 + 平台基础」。 |

两边做完后，都视为「集群已就绪」，后续用同一套部署流程。

### 3.2 应用与平台部署：同一套命令

- **入口**：`deploy-sunmoonai-all`、各平台 `deploy-*-all`、各组件部署脚本等。
- **约定**：这些脚本**只依赖当前环境的 KUBECONFIG**（或默认 `~/.kube/config` 的 current-context），不写死 kubeconfig 路径。
- **结果**：
  - 在 Kind 上：让当前 KUBECONFIG 指向 Kind（例如 `kind get kubeconfig` 导出后 `export KUBECONFIG=...`，或用连接管理器）→ 跑 `deploy-sunmoonai-all`（或子平台）→ 部署到 Kind。
  - 在远程上：让当前 KUBECONFIG 指向远程——若 API 可直连，用已下载的 config 即可（`export KUBECONFIG=~/.kube/cluster-c1-admin.conf`）；若需隧道，先跑连接管理器再跑**同样的**部署命令。
- **切换**：把「当前生效的 kubeconfig」指到目标集群（改 KUBECONFIG 或改 default_cluster + 跑连接管理器），再执行相同部署命令，无需换脚本。

---

## 4. 基础设施脚本的边界（保证一致、不混用）

- **deploy-infrastructure-all**：仅用于**远程集群**（C1/C2）。  
  - 当 `default_cluster=KIND` 或 `cluster_mode=kind` 时：**不执行**，并给出明确提示（例如：「基础设施部署仅用于远程集群，当前目标为 Kind，请使用 kind-setup.sh 与《kind使用指南》」），然后退出或跳过。  
  - 不在该脚本内为 Kind 实现「部分 Step」或 SSH，避免行为分叉和复杂判断。
- **Kind 的“平台层”**：如需命名空间、默认 StorageClass 等，由**独立的小脚本**（对「当前 KUBECONFIG 指向的集群」执行 kubectl/Helm）完成，与 deploy-infrastructure-all 解耦。  
  - 这样：远程 = 一次 deploy-infrastructure-all；Kind = kind-setup + 可选平台初始化；之后两边都走同一套应用/平台部署。

---

## 5. KUBECONFIG 统一（切换 kind/远程行为一致）

- **谁决定当前集群**：当前 shell 的 `KUBECONFIG`，或未设置时 `~/.kube/config` 的 current-context。可用方式包括：① 连接管理器（需隧道时）；② 直接 `export KUBECONFIG=...` 指向已下载的远程 config 或 Kind 的 config；③ 多集群合并进 `~/.kube/config` 后 `kubectl config use-context ...`。
- **非交互 / 脚本 / CI**：所有执行 `kubectl` 或 Helm 的脚本，通过**环境变量 KUBECONFIG**（或从 k8s-admin 解析出的路径）获知当前集群，再调同一套部署命令。
- **禁止**：在业务或平台部署脚本中写死某一条 kubeconfig 路径；统一依赖「当前 KUBECONFIG 或约定解析方式」，以保证切换 Kind/远程时同一套命令生效。

---

## 6. 小结

| 项     | 约定 |
|--------|------|
| **目标与切换** | 当前目标 = 当前生效的 KUBECONFIG；切换 = 让该变量（或 default config 的 context）指向目标集群。需隧道时用连接管理器，API 可直连时用已下载的 config 即可。 |
| **集群就绪**   | Kind = kind-setup + 可选平台初始化；远程 = deploy-infrastructure-all。之后都视为「已有集群」。 |
| **应用部署**   | 同一套命令（deploy-sunmoonai-all 等），统一依赖 KUBECONFIG，不写死路径。 |
| **基础设施**   | deploy-infrastructure-all 仅用于远程；遇 Kind 时明确提示并退出/跳过。 |
| **一致性**     | 「先在 Kind 跑，再切远程同样跑」= 同一套部署流程，仅通过 k8s-admin.conf 和 KUBECONFIG 切换目标。 |

实施细节与分阶段步骤见 **《迁移指南.md》**；Kind 的具体操作见 **《kind使用指南.md》**。

---

## 7. 实施要点总结

1. **配置文件尚未支持 Kind，需要改以支持 Kind**  
   k8s-admin.conf 已有 [KIND] 段，但连接管理器和统一模板里读配置、建连的逻辑目前只处理 C1/C2（或 bastion/direct/local），没有识别 cluster_mode=kind 或 default_cluster=KIND。需要在**配置解析与建连流程**中增加对 Kind 的支持（读 [KIND]，按配置路径写 config、设置 KUBECONFIG）。  
   **✅ 已实施**：统一部署模板（unified-deployment-template.sh）已增加 Kind 分支；k8s-admin.conf 无需改动。

2. **支持 Kind 后，连接管理器与模板脚本与规范一致，无需再为“规范”而改**  
   规范为：多文件（每集群一份 config，路径由 k8s-admin 指定）+ 建连时写入该路径并 export KUBECONFIG，断开时 unset KUBECONFIG 并关隧道。连接管理器和模板脚本已按此思路实现；补齐 Kind 后，二者在行为上与规范一致，不需要为“符合规范”再做额外修改。  
   **✅ 已落实**：连接管理器本身已支持 Kind；模板补上 Kind 分支后，二者均符合规范，无需再改。

3. **应用脚本：调用模板脚本的不改；只有自己设置 config 且写死 config 路径的才要改**  
   - 应用脚本若已 source 统一模板并在用 kubectl 前调用 setup_kubectl_environment()，则依赖模板设置的 KUBECONFIG，**不需要改**。  
   - 只有**在脚本内自己设置 KUBECONFIG（或 kubectl 命令前写死路径）且写死了路径**（如 `KUBECONFIG=~/.kube/cluster-admin.conf kubectl ...`）的，才需要改为使用当前环境的 KUBECONFIG（例如直接 `kubectl` 或 `KUBECONFIG="${KUBECONFIG:-}" kubectl`），不再写死路径。  
   **✅ 已排查并修改**：全库检索后，仅 2 个脚本存在写死路径，已改为使用 `KUBECONFIG="${KUBECONFIG:-}"`：`sunmoonai/messaging-platform/rabbitmq/deploy-rabbitmq/deploy-rabbitmq.sh`（4 处）、`sunmoonai/ingress-platform/traefik/deploy-traefik/deploy-traefik.sh`（2 处）。  
   **排查说明（不需改的）**：utils 下模板/连接管理器/kind-setup 为读配置或建连逻辑，用变量；k8s-admin.conf 为配置项非脚本写死；rabbitmq/mongodb 的 deploy-secrets-all 及 deploy-mongodb 已用 `KUBECONFIG="${KUBECONFIG:-}"`；各 deploy-llmops/incubator/auth/document-converter 等仅含 `echo "export KUBECONFIG=..."` 提示语；infrastructure/steps 中 REMOTE_KUBECONFIG、/etc/kubernetes/admin.conf 为**远程节点**上 kubeconfig，属建集群/节点管理场景，非本机多集群 config 规范范畴。
