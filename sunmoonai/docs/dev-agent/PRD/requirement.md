# 需求：要满足什么

> 依据：[产品合同](../../product/product-contract.md)。本文只写**要什么**，怎么做见 [设计层](../SDD/architecture/README.md)。

同目录另有 [`functions.md`](functions.md)（七阶段产品功能，全部 `F-*`）。
**分期不在这一层**：三期与人周以 [产品合同附录 C](../../product/product-contract.md) 为准，本目录不另立。

## 目标与范围

把产品合同要求的东西做出来：用户在桌面应用里提交 Task，后端受理、路由、编排并验收，Agent 在用户电脑上执行，结果端到端加密后可靠取回。

- **包含**：合同的全部 `F-*` 功能义务与 `I1` 至 `I20` 不变量，以及 `AT-*` 的验收；
- **不包含**：合同里标为后置的能力（私有部署、企业网关、团队协作、统一分发、Linux 与信创），以及附录 A 未定的决策事项；
- **不包含部分的归属**：后置能力在相应决策拍板后另建模块。

## 约束

- [代码必须遵守的规则](../rules/constraints.md)；
- 合同的定位约束 `F-POS-01` 至 `F-POS-05`：自有数据只装事实、方法库只写方法、输出是研究底稿、系统不替用户下判断、留存定性证据；
- 合同的不变量 `I1` 至 `I20`。

## 未决

以合同附录 A 为准，本文不另立一份清单。第一层怎么分已经定了（三块，见 [组成部分与本轮范围](../SDD/architecture/lifecycle.md)）；
还影响范围的是 D17（定位的法律边界、备案与数据授权）。
