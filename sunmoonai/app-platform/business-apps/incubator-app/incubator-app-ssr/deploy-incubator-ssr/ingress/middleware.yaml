# 如需中间件可在此添加（示例占位，当前未使用）
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: incubator-app-ssr-placeholder
  namespace: {{NAMESPACE}}
  labels:
    app: app-platform-ingress
    component: incubator-app-ssr
spec:
  headers:
    customRequestHeaders:
      X-Incubator-SSR: "true"

