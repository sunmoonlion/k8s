# ============================================================================
# ONLYOFFICE Docs App BFF YAML 生成配置
# ============================================================================
# 注意：主应用使用 Helm 部署，这里只生成 Ingress 和 Middleware 的 YAML

# 需要生成的资源列表
# 格式：资源类型:模板路径:输出文件名:是否启用
GENERATE_RESOURCES=(
    "pvc:templates/pvc/onlyoffice-docs-pvc.yaml:onlyoffice-docs-pvc-generated.yaml:true"
    "secret:templates/secret/harbor-registry-secret.yaml:harbor-registry-secret-generated.yaml:true"
    "secret:templates/secret/onlyoffice-jwt-secret.yaml:onlyoffice-jwt-secret-generated.yaml:true"
    "secret:templates/secret/onlyoffice-postgresql-secret.yaml:onlyoffice-postgresql-secret-generated.yaml:true"
    "secret:templates/secret/onlyoffice-rabbitmq-secret.yaml:onlyoffice-rabbitmq-secret-generated.yaml:true"
    "secret:templates/secret/onlyoffice-redis-secret.yaml:onlyoffice-redis-secret-generated.yaml:true"
    "middleware:templates/middleware/onlyoffice-docs-stripprefix.yaml:onlyoffice-docs-stripprefix-generated.yaml:true"
    "ingress:templates/ingress/ingress.yaml:onlyoffice-docs-ingress-generated.yaml:true"
)

# 生成顺序
GENERATE_ORDER=(
    "middleware:ingress"
)

# 部署配置路径
DEPLOY_CONFIG="../../deploy-onlyoffice-docs/deploy-onlyoffice-docs.conf"
