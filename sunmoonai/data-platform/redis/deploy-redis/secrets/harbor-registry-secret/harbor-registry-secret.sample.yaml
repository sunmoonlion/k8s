apiVersion: v1
kind: Secret
metadata:
  name: harbor-registry-secret
  namespace: data-platform-dev
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "REPLACE_WITH_BASE64_JSON"
---
# 仅归档用途：此 Secret 通常由统一密管或基础设施创建；此文件用于占位/对齐结构

