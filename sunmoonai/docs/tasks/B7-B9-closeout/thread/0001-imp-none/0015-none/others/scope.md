# 在线身份供给接入

基线：k8s/luna `b3882966d2df08ef1de13e781f5d3003a549680e`，
tpl-app/luna `6e42d9e9603e42af84008169ad8890434a125a55`，开工干净；五仓上一轮已完整同步。
承接 0014 固定候选 `kind-b7-20260919`，不修改业务组件，不重建镜像，不启动 Opus 助手。

目标：把已隔离验证的模板 SQL/broker 策略接到本机 KIND 受控执行步骤，保留原账号直到
新身份认证/权限验证成功；未知状态、目录漂移、名字冲突或查询失败一律停止，不清理后重试。
先实现有测试的目录前置核验、私有材料与窄写入口，再接迁移后授权；不得先停服务再临时编写。
只有新流程实际验证通过才进入既有批准的维护窗口。集群操作必须显式目标和精确 payload，
共享 definitions 的更新保留非目标条目并做并发版本检查，不重启共享服务、不删除队列。

范围：模板 k8s-deployment 公共身份策略与测试；k8s 三 App 部署接入/脚本/本 turn 记录。
不动产品合同或新架构，不操作云端部署。通用能力仍模板先行，三个领域 overlay 不分叉。
验证包括纯函数负例、实际隔离 PG/broker、新旧登录及权限、失败中止；保留真实备份与私有日志。
回滚材料不可只靠 Git；上线前绑定停写备份/恢复、旧身份/Secret/ACL/镜像/schema。出现部分
写入不自动删角色或回滚共享设施，保留检查点并先查明状态。

规则 D1–D9、I3/I8、T3–T5、R1–R7：单库/单迁移链不变，独立 Migration Job，
授权与实际运行验证分开记账，准备测试不能冒充发布或业务验收。
