# 0001-intake：受理与路由

**承担**：身份与幂等、建单、契约固定、路由决定。合同 `F-ADMIT-*` 那一段。

**不承担**：不执行任务，不派发——契约固定后进队列，交给 `0002-agent-execution`。

**边界要点**：身份与幂等在**同一个事务边界**内完成；契约固定**之后**才进队列。
身份从认证上下文确定，不信任客户端自报。

**要满足什么**见子任务 [`submodules/0001-intake/`](../submodules/0001-intake/PRD/requirement.md)。
