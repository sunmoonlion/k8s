# 0001-intake：受理与路由（已取消）

**本版起取消。**责任并入 [`0006-router`](0006-router.md)（受理、类别判定、路由决定）
与 [`0005-kernel`](0005-kernel.md)（建单与幂等账的写入）。

**为什么**：原来这四块按合同七阶段切——阶段是时间，不是模块。按阶段切时，状态机与四本账
横穿四块却不归任何一块，「同一事实只有一个权威写入面」只能靠纪律守。新划分把内核提出来，
其余按「朝哪一侧」切（客户端、runtime、人），一个变更基本只落一块。

**目录不删**：[`../submodules/0001-intake/`](../submodules/0001-intake/) 下的 turn 已交回并冻结，
是当时发出去的原话与交回的原物，留着；该子任务不再开新的 turn。
其中 `rules/agent-dev-guide.md` 的内容已按新划分分到各块。
