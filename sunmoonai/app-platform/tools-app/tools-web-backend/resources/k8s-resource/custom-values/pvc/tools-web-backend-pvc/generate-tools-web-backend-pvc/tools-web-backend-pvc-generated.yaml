apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tools-web-backend-pvc
  namespace: app-platform-dev
  labels:
    app: tools-web-backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
