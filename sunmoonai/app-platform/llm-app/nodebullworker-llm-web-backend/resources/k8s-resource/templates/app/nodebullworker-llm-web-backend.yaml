apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodebullworker-llm-web-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: nodebullworker-llm-web-backend
    component: node-bull-worker
    backend: llm-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodebullworker-llm-web-backend
  template:
    metadata:
      labels:
        app: nodebullworker-llm-web-backend
        component: node-bull-worker
        backend: llm-web-backend
    spec:
      imagePullSecrets:
      - name: ${NODEBULLWORKER_LLM_WEB_BACKEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: nodebullworker
        image: ${NODEBULLWORKER_LLM_WEB_BACKEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        command:
        - sh
        - -c
        - |
          set -eu
          export NODE_ENV="${NODE_ENV:-production}"
          export QUEUE_ON="${QUEUE_ON:-true}"
          exec node dist/worker
        envFrom:
        - configMapRef:
            name: ${LLM_WEB_BACKEND_CONFIGMAP_NAME:-llm-web-backend-config}
        - secretRef:
            name: ${LLM_WEB_BACKEND_SECRET_NAME:-llm-web-backend-secret}
        - configMapRef:
            name: ${NODEBULLWORKER_LLM_WEB_BACKEND_CONFIGMAP_NAME:-nodebullworker-llm-web-backend-config}
        - secretRef:
            name: ${NODEBULLWORKER_LLM_WEB_BACKEND_SECRET_NAME:-nodebullworker-llm-web-backend-secret}
        - secretRef:
            name: ${NODEBULLWORKER_LLM_WEB_BACKEND_QUEUE_REDIS_SECRET_NAME:-llm-web-backend-nodebull-redis-conn}
        env:
        - name: QUEUE_WORKER_SERVICE
          value: "llm-web-backend"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
