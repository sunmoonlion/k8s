apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-web-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: knowledge-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: knowledge-web-backend
  template:
    metadata:
      labels:
        app: knowledge-web-backend
    spec:
      imagePullSecrets:
      - name: ${KNOWLEDGE_WEB_BACKEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: knowledge-web-backend
        image: ${KNOWLEDGE_WEB_BACKEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: ${KNOWLEDGE_WEB_BACKEND_CONFIGMAP_NAME:-knowledge-web-backend-config}
        - secretRef:
            name: ${KNOWLEDGE_WEB_BACKEND_SECRET_NAME:-knowledge-web-backend-secret}
${KNOWLEDGE_WEB_BACKEND_DATABASE_ENV_FROM}
${KNOWLEDGE_WEB_BACKEND_OBJECT_STORAGE_ENV_FROM}
${KNOWLEDGE_WEB_BACKEND_ELASTICSEARCH_ENV_FROM}
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
${KNOWLEDGE_WEB_BACKEND_ELASTICSEARCH_VOLUME_MOUNT}
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-knowledge-web-backend-pvc}
${KNOWLEDGE_WEB_BACKEND_ELASTICSEARCH_VOLUME}
---
apiVersion: v1
kind: Service
metadata:
  name: knowledge-web-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: knowledge-web-backend
spec:
  selector:
    app: knowledge-web-backend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
