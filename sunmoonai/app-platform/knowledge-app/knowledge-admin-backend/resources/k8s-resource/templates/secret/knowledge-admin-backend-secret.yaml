apiVersion: v1
kind: Secret
metadata:
  name: knowledge-admin-backend-secret
  namespace: ${NAMESPACE}
  labels:
    app: knowledge-admin-backend
type: Opaque
stringData:
  RAGFLOW_API_KEY: "${RAGFLOW_API_KEY}"
