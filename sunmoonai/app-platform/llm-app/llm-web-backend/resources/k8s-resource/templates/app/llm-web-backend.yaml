apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-web-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: llm-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-web-backend
  template:
    metadata:
      labels:
        app: llm-web-backend
    spec:
      imagePullSecrets:
      - name: ${LLM_WEB_BACKEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: llm-web-backend
        image: ${LLM_WEB_BACKEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: ${LLM_WEB_BACKEND_CONFIGMAP_NAME:-llm-web-backend-config}
        - secretRef:
            name: ${LLM_WEB_BACKEND_SECRET_NAME:-llm-web-backend-secret}
        - secretRef:
            name: ${LLM_WEB_BACKEND_QUEUE_REDIS_SECRET_NAME:-llm-web-backend-nodebull-redis-conn}
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-llm-web-backend-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: llm-web-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: llm-web-backend
spec:
  selector:
    app: llm-web-backend
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
