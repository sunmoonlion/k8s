# 跨模块的功能义务

> 这里只装**一条义务由两个以上模块共同满足**的那些。单模块的义务在各自模块里：
> [桌面应用](../SDD/submodules/0002-desktop/PRD/functions.md)、
> [本地 runtime](../SDD/submodules/0003-runtime/PRD/functions.md)、
> 后端七块各在 [`SDD/modules/`](../SDD/submodules/0001-backend/SDD/modules/) 下自己那一份。
>
> **`F-*` 的 ID 全局唯一、跨文件不复用、不因搬动而重排。**合同的验收矩阵 `AT-*` 按这些 ID 勾。
> 每一站发生在哪见 [请求生命周期](../SDD/architecture/lifecycle.md)，谁负责哪一条见合同「责任投影」。

## 提交（桌面应用）

- **F-INTAKE-07**：提交前在界面上给出任务类别，按本机确定性规则预填，由用户确认或改正后随提交上报；预填只读取本机文件，不上传内容、文件名或派生特征。

### 工具级与 Task 级审批

> 两层的划分、审查窗口要表达什么、以及「被攻破的后端」这个威胁模型，
> 见 [`approval.md`](../SDD/architecture/approval.md)。

### 后端被攻破时仍要成立的

- **F-GUARD-04**：任务类别与 Profile 不授予数据权限：自有数据、领域工具与知识服务的访问由授权与订阅独立判定，自报的类别不能扩大可取数据的范围；

## 结果加密

> 「问题侧明文，资料侧加密」这条分界与它的不变量，见
> [`invariants.md`](../SDD/architecture/invariants.md)。

- **F-CRYPTO-01**：结果密钥在用户设备上生成，存系统钥匙串；加密在执行端完成，发生在本地内容检查之后；
- **F-CRYPTO-07**：问题侧的明文字段只容纳来自任务文本的内容；把本地资料内容、文件名或其派生特征写入任何明文字段都是违规，字段 schema 必须能拦住。

## 验收与完成提交

- **F-ACCEPT-02**：**本地结构与内容检查**：凡是需要读正文的检查——输出 schema 与未声明字段、必填证据字段、F-POS-04 的判断拦截、验收条目中需要读正文的部分——都在执行端、加密之前完成，合成一张签名回执；后端核对回执与密文摘要一致；

## 交付、失败与重试

结果交付采用「**持久化后通知**」：

- **F-DELIVERY-01**：客户端可按 `task_id` 获取当前状态与稳定版本的结果密文，在本机解密；
- **F-DELIVERY-02**：每个事件有稳定 `event_id`、Task 内单调 `sequence_no`、`payload_schema_version`、时间、主体与可见级别；
- **F-DELIVERY-03**：先持久化事件再推送；
- **F-DELIVERY-04**：事件流使用 cursor 续传，重复事件对客户端幂等；
- **F-DELIVERY-05**：客户端先建立订阅，再读取快照与历史并按 sequence 去重，或采用可证明无丢失窗口的等价握手；
- **F-DELIVERY-06**：事件推送只是低延迟通知，不是结果的唯一载体；流式片段与未验收草稿不能单独构成 `SUCCEEDED`；
- **F-DELIVERY-07**：终态与失败原因可以机械判定，不靠自然语言暗示；
- **F-DELIVERY-08**：用户可见的失败带可行动分类；
- **F-DELIVERY-09**：每次读取状态、事件和结果都重新校验用户与授权；
- **F-DELIVERY-10**：通知失败进入 Delivery 记录或重试，不修改 Task 终态；
- **F-DELIVERY-11**：结果正文只以密文离开执行端；本地资料内容与文件名不离开执行端；管理后台与事件流不含正文。

失败至少分类为：

| 类别 | 例子 | 默认处置 |
| --- | --- | --- |
| `ADMISSION` | 建单后发现业务或政策不可受理 | `REJECTED`，安全说明 |
| `DISPATCH` | 投递失败、无可用设备 | 可重试或等待设备；策略耗尽后 `FAILED` |
| `DEVICE` | 执行端离线、失去租约 | 暂停并对账；按策略恢复或开新 Attempt |
| `EXECUTION` | 模型、工具或外部系统失败 | 先查副作用，再决定重试 |
| `VALIDATION` | 输出 schema、验收、证据或内容检查不通过 | 新 Attempt；不得原样盲重试 |
| `BUDGET` | 预算耗尽 | 追加批准、换策略或失败 |
| `CANCEL` | 用户或系统取消 | 安全收敛后 `CANCELLED` |
