# 工程落点与共同约束

## 仓与技术栈

| 模块 | 仓 | 技术栈 | 新旧 |
| --- | --- | --- | --- |
| 工作台后端 | `investment-app/investment-backend` | Python、FastAPI、SQLAlchemy、PostgreSQL、Outbox；app-server 客户端用 `websockets` | 改造 |
| 网页 | `investment-app/investment-web-frontend` | Next.js 16、同源 `/api/`、事件流 | 改造 |
| 沙箱池 | `k8s` 仓新部署单元 + 一个小镜像（Codex CLI 钉版 + 出站桥） | 容器；出站桥 Python 或 Node，与代理共用协议实现 | 新 |
| 会合点 | 新仓 `relay`（或先放 `k8s/platforms/`，`D16` 待定） | Python asyncio + websockets 起步；延迟不达标再换 Go | 新 |
| 本地代理 | `runtime` 仓 | TypeScript/Node（Codex CLI 是 npm 包，同一运行时）；随包带钉版 Codex；签名分发 | 新（仓已有，内容换） |
| 知识服务 | `knowledge-app` | 现状 + MCP 服务端 + 用户资料入库 | 改造 |
| 评测 | 先落 `investment-backend` 的 `eval/` | Python | 新 |

**退役仓**：`desktop-app`（本地 worktree、`~/master` 副本、`repos.conf` 条目一并删；GitHub 仓由所有者删）。

## 共同约束

- 状态机只有一套，场景差异只体现在 Profile 与专家包（`P0`）；
- 同一事实只有一个权威写入面（`P1`）；Codex thread 不是真源；
- 跨进程仍须正确的不变量由 PostgreSQL 承担，不放在沙箱或代理进程里；
- 执行引擎租用不自建，只经 app-server 与 exec-server 的公开协议（`C-A4`、`C-C7`）；
- 接口契约单一真源：网页与代理都从工作台的 OpenAPI 生成客户端；
- 版本成对：Codex 版（app-server 与 exec-server 同版）、会合点协议版、代理版随发布清单钉（`C-R8`）；不匹配拒绝连接（`AT-28`）；
- 领域概念不进 Port 签名（`C-A5`）。

## 第一期切法

| 项 | 第一期 | 之后 |
| --- | --- | --- |
| 委托 | 单委托、串行、只读为主（写只落项目目录） | 并行、写范围扩大 |
| 沙箱池 | 一个演示用户的常驻 pod | 按需拉起、PVC、配额 |
| 会合点 | 单实例、单站点 | 多实例、多站点分片 |
| 本地代理 | Windows（第一个面向用户的平台）、Linux/WSL（开发联调）；macOS 其次 | 信创 |
| 用户资料 | 显式上传 | 目录同步 |
| 评测 | 问数二十题 + 勾稽首批 | 持续沉淀 |
| 离线模式 | 无 | 第二期（`D13`） |
| 计费 | 只记账 | 收费（`D12`） |

## 第一段的探针（动代码前）

按顺序，每个都有可证伪的判据，结果落 `runtime/probe/`：

1. **本地上限**：~~exec-server 侧配置能否拒绝 harness 的要求~~ 已验：不能，代理自己挡（`security.md`）；
2. **BYOK**：~~`OPENAI_API_KEY` 与一个国产厂商 key~~ 已验：Kimi K3 经 `model_provider` 直连，远端环境、`apply_patch`、审批全过（`REPORT-2026-09-23-byok-kimi.md`）；OpenAI key 路径待有 key 再补；
3. **会合点透传**：~~两端各出站、中间一个哑透传~~ 回环已验成立（`REPORT-2026-09-23-relay-passthrough.md`）；每 turn 约 45 个串行来回，公网数字要两台机器测（luna）；
4. **断线恢复**：~~25 秒内重连接回原会话；超窗行为~~ 已验（`REPORT-2026-09-23-reconnect.md`）：窗内无损，超窗或代理重启丢会话但自动重连，新 Attempt 可接续；
5. **Windows exec-server**：~~本地 spike~~ 已验 pass（`REPORT-2026-09-24-windows-exec-server.md`）：原生可跑、沙箱挡得住；要一次性管理员初始化、端口不能写死、npm 垫片不能当进程起。

任一失败都改设计，不改判据。

**第一段已全部完成（2026-09-24）。第二段「代理与沙箱最小对」已跑通**：会合点 v1、本地代理、沙箱镜像三件在一台机与容器形态下联调 pass；剩余项见 `runtime/CHECKPOINT.md`。

**第三段「工作台与网页」已到停点（2026-09-24）**：后端账本、runner、顾问、验收与 `/api/workbench` 路由（457 测试）；网页会话台、求助、审查面（59 测试，build 过）；本机真链：顾问驾驶 `SMOKE` 到 `SUCCEEDED`。实现状态见各模块文档末节；未做项集中在 `0001-workbench.md`「实现状态」。

**第四段「知识 MCP、底稿与设置、评测」已到停点（2026-09-24）**：知识 MCP 三工具 + 沙箱镜像配置；`DATA_QUERY` 专家包经 MCP 真跑通；评测三件（真值、判定、发布门）与二十题冻结集；底稿页与设置页。实测发现：Codex 0.155 调 MCP 工具前发 `mcpServer/elicitation/request`，客户端必须应答。实现状态见各模块文档末节。

**第五段「运营层」进行中（2026-09-25）**：已做——KIND 开发包的 runner 角色与只换镜像的升级门、丢失身份准备的现场重建、沙箱按需拉起（供给器 + 会合点管理通道 + 设置页）、知识 MCP 数据集从对象存储钉版取。本地 KIND 部署由 switch-test inbox 06 至 09 串行推进。2026-09-25 夜里补上：runner 重启恢复与分片（租约、接管、未决审批过期）、D10 令牌签发（ES256 JWT，边缘与知识服务公钥就地验，按 jti 吊销推送，设置页撤换）。未做见各模块「实现状态」：质量信号、CODEX_HOME 备份。

## 走向企业级：先补的三件（2026-09-26 所有者定）

现状：发布纪律、最小权限、密钥处理、身份这几样已按企业级做（钉摘要的镜像、备份与两次隔离恢复的门禁、列级数据库授权、非 root、只放行需要的出站、ES256 令牌与吊销、bcrypt、邀请码与邮箱验证）。整体仍是"企业级底座上的内测版"。下面三件按顺序补，是上线服务真实用户之前的底线；前端设计与 Windows 安装包是另一条线（能不能用），不在这里。

### 一、端到端自动测试

**为什么先做它**：2026-09-25 到 26 在 KIND 里靠人点浏览器找出的问题——事件流带事件名被浏览器丢掉、只写库的事件不实时、key 列表被严格校验拒收、页面 401 无反应、沙箱模板权限、代理互踢——单元测试全绿，都是跨进程、跨仓的契约与部署问题，端到端测试本来能提前挡住。

- **范围（第一刀）**：真浏览器（Playwright，网页仓已有）驱动一套完整栈，跑下面这些路径：登录 → 设置页登记 key（列表出现）→ 拉起沙箱 → 新建会话发一句话（**不刷新**出现回答）→ 问专家 SMOKE（**不刷新**出现委托卡与状态）→ 打开底稿；换代理令牌（旧代理被断开、新命令能接入）；未登录访问设置页（出现"请重新登录"）。
- **栈怎么起**：沿用正式开发包，在一个一次性的 KIND（或本地 KIND 的独立命名空间）里部署；模型调用用一个假的 OpenAI 兼容服务（回放固定回答），不花钱、不依赖外网；本地代理与会合点用真的。
- **契约防漂移**：前端从后端 OpenAPI 生成客户端（`F-WEB-06`，现在是手写 zod），生成物与后端路由在同一次检查里比对；已先加了两处钉字段的测试（凭据列表、事件帧），这是过渡办法。
- **验收**：上面每条路径一个用例；在 CI 里每次合并前跑；故意把 2026-09-26 修过的任何一个问题改回去，至少一个用例变红。

### 二、持续集成与交付

**为什么**：现在每发一次版要本地机器按待办手工跑一小时（建镜像、改锁、渲染、门禁、维护窗口、备份回执、部署），容易漏步骤（06 漏停 runner，07b 靠门禁兜住）。

- **已有**：`k8s/sunmoonai/cicd-platform/`：Harbor 已部署；Jenkins + Kaniko 构建流水线的配置在；Argo CD / Workflows / Events 只有规划（`cicd总体架构及实施详细方案.md`，2026-04）。
- **要做**：
  1. 提交即构建：各仓 `fable` 与 `master` 的推送触发单元测试、类型检查、镜像构建（Kaniko，按摘要推 Harbor）。
  2. 合并前跑端到端测试（上一件）。
  3. 发版流水线：把现有正式开发包的步骤（锁、渲染、门禁、`plan`、维护窗口、备份回执、`deploy`、`drift`）原样编排成流水线，**不绕过任何门禁**；需要人批的地方（维护窗口、生产发布）留审批。
  4. 回滚：上一版本的 bundle 与镜像摘要可一键重新部署。
- **待定**：`D22`。

### 三、多副本与告警

**为什么**：上线后任何一个单点挂掉都是整站不可用；没有告警就只能靠用户报。

- **单点清单**：会合点（单实例，内存里的配对与吊销表；状态文件在 emptyDir）、边缘（Traefik + frps + 会合点同一台）、PostgreSQL、Redis、供给器、知识服务 API、工作台 runner（已支持多实例分片，未在集群多副本验证）。
- **要做**：
  1. 无状态服务多副本 + 反亲和：工作台 api、网页、知识 api、供给器；runner 两副本验证租约接管（`F-LEDGER-02` 已做，未在集群验）。
  2. 会合点多实例：配对要求同一用户的代理与沙箱落在同一实例——边缘按用户哈希分流（或共享配对表）；吊销表与公钥放共享存储，不再靠单实例内存与 emptyDir。
  3. 数据层：PostgreSQL 主从与定时备份（已有备份回执的做法，改成周期性）、Redis 哨兵或集群；恢复演练常态化。
  4. 边缘两台以上（`topology.md`：DNS 轮询，每台一个 frps，frpc 各连一台）。
  5. 可观测：指标（Prometheus 一类）、日志汇集、关键链路追踪；告警规则至少覆盖——服务不可用、会合点在线代理数骤降或配对失败率升高、委托卡在 RUNNING 超时、沙箱 CrashLoop、备份失败、证书将到期、磁盘与连接数。仓里目前没有部署监控栈，只有部分 chart 支持导出指标。
- **待定**：`D23`。
