apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: research-admin-frontend-pvc
  namespace: app-platform-dev
  labels:
    app: research-admin-frontend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
