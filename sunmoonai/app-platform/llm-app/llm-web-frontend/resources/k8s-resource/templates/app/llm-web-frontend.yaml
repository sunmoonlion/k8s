apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-web-frontend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: llm-web-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-web-frontend
  template:
    metadata:
      labels:
        app: llm-web-frontend
    spec:
      imagePullSecrets:
      - name: ${LLM_WEB_FRONTEND_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: llm-web-frontend
        image: ${LLM_WEB_FRONTEND_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: ${LLM_WEB_FRONTEND_CONFIGMAP_NAME:-llm-web-frontend-config}
        - secretRef:
            name: ${LLM_WEB_FRONTEND_SECRET_NAME:-llm-web-frontend-secret}
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-llm-web-frontend-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: llm-web-frontend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: llm-web-frontend
spec:
  selector:
    app: llm-web-frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
