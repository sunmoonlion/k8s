# Vite SPA 环境变量（复制为 .env.local 后填写）
# 注意：VITE_ 前缀的变量会打包进客户端 bundle，不要放任何 secret
# ──────────────────────────────────────────────────────
# SPA 侧只需要知道后端 API 地址，其余全在后端配置（见 vite-spa-backend/）

# 自有后端 API 地址（同源时留空；跨域时填写，如 http://localhost:8000）
VITE_BFF_BASE_URL=
