apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: onlyoffice-docs-stripprefix
  namespace: app-platform-dev
  labels:
    app: app-platform-ingress
    component: onlyoffice-docs-stripprefix
    project: sunmoonai
spec:
  stripPrefix:
    prefixes:
      - "/onlyoffice-docs"

