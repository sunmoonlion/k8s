apiVersion: v1
kind: Namespace
metadata:
  name: ${NAMESPACE}
  labels:
    app: app-platform
    component: celeryworker-investment-admin-backend
    environment: ${ENVIRONMENT}
    managed-by: deploy-script
