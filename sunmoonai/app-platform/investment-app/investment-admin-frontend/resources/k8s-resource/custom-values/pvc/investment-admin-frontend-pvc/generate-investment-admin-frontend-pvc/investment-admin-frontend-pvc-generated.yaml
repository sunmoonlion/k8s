apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: investment-admin-frontend-pvc
  namespace: app-platform-dev
  labels:
    app: investment-admin-frontend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
