apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: research-admin-backend-pvc
  namespace: app-platform-dev
  labels:
    app: research-admin-backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
