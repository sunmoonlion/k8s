apiVersion: v1
kind: Secret
metadata:
  name: portal-app-ssr-secret
  namespace: ${NAMESPACE}
  labels:
    app: portal-app-ssr
type: Opaque
stringData:
  # 如 SSR 需要额外敏感配置，可在此添加（示例占位）
  # API_TOKEN: changeme

