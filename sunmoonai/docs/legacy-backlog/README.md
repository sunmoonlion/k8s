# 旧开发任务待接收清单

本目录集中保存停工后仍需接续的旧任务，独立于正式 PRD/SDD 和模块树。
当前状态：**已单独整理，待新架构讨论后逐项接收；不是本轮必须实现的新产品功能。**

## 本次决定与使用边界

2026-09-19 [用户决定](../tasks/B7-B9-closeout/thread/0001-imp-none/0002-none/user-message.md)：
按桌面客户端、本地 runtime、后端三部分划分，同时仍保留 B-S 结构；先把旧任务单独
保存，等随后讨论并确定新架构再处理。因此：

- 三部分是职责/任务划分，不能据此推导放弃 B-S、删除浏览器入口或只允许桌面入口。
  浏览器、桌面与本地 runtime 的具体配合，以及与现有合同表述的对齐，留给架构讨论。
- 本次不改产品合同、PRD/SDD，不搬迁或新增正式模块，不提前指定每项的执行位置。
- B8 本轮完成条件改为“独立清单保存完整、原任务可追溯、已做与未做分明”。
  **正式迁入新架构计划延期，不再以 D1 等架构未决项阻塞当前旧账整理。**
- 保存不等于实现或验收；下面的“已有基础”只用于避免重复开发，不能替代新产品验收。

历史 handoff、实施计划与 B1～B9 原处置记录已从当前目录退出，精确文件名和固定 Git
版本见 [验证与历史索引](verification-index.md)。原任务编号和未完成目标保留在下表。
此前 [新合同映射快照](../tasks/B7-B9-closeout/thread/0001-imp-none/0001-none/others/backlog-routing.md)
保留为历史分析；其中“先定 D1 才能交回 B8”的限制已由本次决定替换，
其中桌面优先等方案推断也不作为本清单的实施前提。

## 待后续统一接收的六组任务

以下均为“待架构讨论后接收”，不默认派工、不冻结新技术方案。

| 编号与原任务 | 仍需完成的目标 | 已有基础与不能据此销账的部分 |
| --- | --- | --- |
| N1 用户交互闭环；P0-008C.5/.6/.7，M1-401～410、411B/C/D、413；M1-302/303、309～311 的交互侧 | 真实执行对接、审批、取消、状态/断线恢复、多标签或多端一致性、引用与文件交互、可访问性 | 已有局部 Task/Interaction、事务事件、回放与 Pilot；未完成最终 B-S/桌面产品路径验收，不恢复旧独立 BFF 或双前端技术栈 |
| N2 Agent 执行能力；M1-301～312，M1-501/502/504 的新执行器部分 | 身份与 Profile、执行适配、工具/沙箱、预算/deadline、证据、取消/恢复、版本兼容 | 已有公共投递、租约、Inbox 与 Pilot；不把现有 LangGraph Pilot 当成新运行时定稿，也不重启旧专业 Agent 自建 loop 方案；执行位置和接口等架构讨论后确定 |
| N3 知识生命周期；M1-201/205/206，摄入生命周期的剩余产品语义 | 停用/删除/reindex、来源失效传播、旧引用、在途摄入、权限变化及 Provider 迁移 | 已有稳定 ID、Provider binding、摄入回执、检索契约与内部 Provider Port；未完成全链路失效传播，不代表已经接入 WeKnora；本地/云端资料边界待统一设计 |
| N4 产品质量与上线；M1-001～005、401～410/411B/C/D/413、501～504、600～605；B4 全引用回收门禁等 | 治理界面、全链路安全/故障/质量评测、SLO、受控发布与恢复；引用生命周期、保留政策及自动回收门禁 | 已有 v2 发布底座、R6 局部跨应用证据与 B7 隔离测试；不等于完整产品验收；禁止自行设置保留天数或删归档。监控已有独立 N4-OPS-01，只引用、不重复建任务 |
| N5 记忆与协作；M1-701/702，M2-001～005、101～105 | 记忆确认/纠错/删除、父子任务上下文/预算/权限、调用关系与版本兼容 | 现有恢复/上下文机制只作复用候选；不照搬旧单 Runtime、多模型后端或 LangGraph Store 方案；与新任务生命周期统一设计 |
| N6 按需扩展；M3-003～010，M3-001/002 未覆盖的 Provider 部分 | Registry、隔离、多租户、容量/扩缩容、分区、灾备、多 Provider；每项先确定触发条件 | 公共投递及部分 Provider operation journal 已有；不重复建设，也不提前建满；触发指标、RPO/RTO、成本和保留上限待确定 |

已明确不恢复的旧做法：M1-313/314 的未选 Agent Server/Hybrid 分支，及
M1-400/411A/412 中已被技术栈合并、v2 与退役工作替代的旧迁移拓扑。
放弃旧实现不等于放弃等价业务目标；新增浏览器/桌面方案不因旧编号而预先受限。

已有证据入口：[精简验证索引](verification-index.md)、
[Knowledge Provider 内部解耦](../knowledge-provider-decoupling-luna.md)、
[B7v 固定验证](../tasks/B7-B9-closeout/thread/0001-imp-none/0001-none/others/verification.md)。
监控唯一任务入口：[N4-OPS-01](../tasks/N4-OPS-01/thread/0001-imp/0001/user-message.md)，
尚未安装/验收到达，仍按此前决定留待未来开发。

## 不混入“等待新架构”的当前运行欠账

以下列在这里作为边界提醒，**并非借本次整理延期或销账**：

- B7 的现有业务身份供给、旧连接排空/撤权、共享 broker 启动配置一致性、业务备份恢复
  和受控切换仍按 [KIND 切换前置](../tasks/B7-B9-closeout/thread/0001-imp-none/0001-none/others/kind-cutover-preflight.md)
  处理；不需要等待桌面/B-S 架构选择，但实际操作仍需明确对象、回滚及授权。
- 现存投递账本/旧归档的实际处置继续遵守
  [部署清单的保留保护条件](deployment-checklist.md)；产品级统一保留/回收设计归 N4。
- B9 源码集成、推送与跨机器同步独立记账，不以本清单代替实际同步或部署。

## 架构讨论后的接收办法

按 N1～N6 逐项决定保留、合并或放弃旧做法；保留目标应给出新任务路径、完成条件、
依赖与已有证据的适用范围，再填写实际接收位置。未接收的继续留在这里，不删原编号，
不靠修改历史测试结论宣布完成。已完成 B1～B7 子项不重复实现。

## 已补齐的旧账范围，避免重复开发

B1 已核领域身份和契约基础；B2 已补抓取 SSRF/流量限额及来源并发；B3 已补
canonical identity、唯一性及乐观并发；B4 已补 Artifact 本域只读对账（不是自动清理）；
B5 已补摄入准入与绑定快照；B6 已补持久调度与非阻塞轮询；B7 已补共享可观测性、
角色探针、权限候选和隔离恢复验证。各项源码完成不代表现存业务环境已部署，
实际欠账统一见 [部署清单](deployment-checklist.md)。

## 从旧设计保留的输入，不是新架构定稿

N1 保留前后端交互的能力目标：类型化请求/状态/事件、幂等、流式恢复、Citation、
审批与恢复一次性、取消及多标签一致性、安全/性能/可访问性；不保留旧 BFF 拓扑。

N2/N5 保留旧 v4 第 20 节 Profile 的概念输入：稳定 key/不可变版本、业务能力、
提示引用、模型选择、工具白名单、权限、记忆策略、知识绑定与预算；执行创建时固定
已解析的有效配置，不能只记录 Profile 名称却不落实限制。Profile 可切换不等于
多 Agent 协作；私有记忆、任务共享状态、会话摘要和长期记忆的边界应分开设计。
这不批准旧 GraphRegistry、云端 ModelGateway 或 RAGFlow 私有字段成为新接口；
具体结构须在新架构讨论后定稿，旧代码中的同名字段不证明能力已生效。

本目录仅三个活动入口：本清单、部署清单、验证索引。已冻结回执和历史证据不搬迁，
旧散文档可按验证索引的固定 Git 提交取回，不维护第二份备份目录。
