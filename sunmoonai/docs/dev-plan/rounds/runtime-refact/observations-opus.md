# 组织者观察：`runtime-refact` 轮

> opus 写。**观察值不是裁定**——需要处置的，在 ③ 裁决时逐条给结论。

## O-1 ｜ ~~`R2` 两家漏声明~~ → **判错，四家全部声明了**

### 更正

初稿写「`luna` 与 `qwen` 无 R2 遵守声明」。**这是错的，四家都写了：**

| 家 | 声明位置与内容 |
| --- | --- |
| `cursor` | 「本评审未读 `agent-dev-refact.md`」，并在 D 块补出后果：未读故不能判断谁向它收敛 |
| `luna` | 第 8 行：「本次互评没有读取 `agent-dev-refact.md`。② 只解除候选之间的隔离，不解除本轮裁定 R2」 |
| `kimi` | 第 7 行：「未打开、未 `git show`、未 grep/rg，未以它为起点」 |
| `qwen` | 第 5–6 行：「写本评审期间未读…亦不引用它。**① 期间的遵守情况（含一名规划子代理可能见过该文件的不确定性）已在**〔覆盖声明〕」 |

⚠ **`qwen` 那条是四份里最有价值的一条**：它没有简单答「未读」，而是**披露了一处不确定性**
——① 期间某个规划子代理可能经宽泛搜索见到过该文件的匹配内容。
`kimi` 在自己的评审里把它点名为**范本**。**如实说「可能见过」不扣分，这正是任务书 §3.1 第三条要的**。

### 我是怎么判错的：`||` 兜底从未触发

检查命令的形状是

```
git show <refA>:<文件> | grep … | head | cut | sed  ||  git show <refB>:<文件> | …
```

**管道的退出码取自最后一个 `sed`，它永远成功**，于是 `||` 的兜底分支**一次也没跑过**。
而 `luna` / `qwen` 的 ② 评审提交在**裸分支** `luna` / `qwen` 上（组织者代提交时用的是它们的当前分支），
第一个 ref `runtime-refact/<家>` 上没有该文件 → `git show` 失败 → 空输出 → **看起来就像「没声明」**。

**不需要为此立新规矩。**协议 §8.1 第 1 条早已写明：

> 零命中要能区分「**真的没有**」与「**没查到**」。

规则在，是执行者（我）没照做。**方向是危险的那一侧**：它不是漏报，是**冤枉了两个合规的参与方**——
与 ① 那次「判据只认一个 ref，把照指示做的那家判成没交」同一形态、同一天内第二次。

### 仍然成立的那一半

本轮机械条 **M1–M7 里没有一条检查 R2 声明**——要求项写在通知正文里靠自觉，
而不是写进 M 类由脚本判。这是**发起方的缺口**，与四家无关，作为下一轮的改进项登记。

## O-2 ｜ 代提交的分布，影响 ③ 的可追溯性折算

| 家 | 产物 | 提交 | 可追溯性 |
| --- | --- | --- | --- |
| `cursor` | 自己写 | **自己提交**（所有者交互式跑） | 作者归属可从提交推出 |
| `luna` | 自己写 | 组织者代提交（`R7`；⚠ 首次代提交未用 `--author`，两字段同身份） | 归属只能靠 commit message 的文字 |
| `qwen` | 自己写 | 组织者代提交，已用 `--author=qwen (qoder)` | 归属可读 |

⚠ **`author` 字段在本机不构成证据**：无签名、各身份共用同一 git 身份，可随意填。
它只让归属**可读**，不让归属**可信**（`agent-dev-refact.md` §2.6）。
③ 处置记录须写明这一差异，**但不得据此调整评分**——
可追溯性弱是**组织者的执行条件**造成的，不是候选的质量问题。

## O-4 ｜ 裁决方两处判错：`resume` 已接线；「验不了的三样」已在服务态验过

2026-09-07 所有者提出「设计与实现应同步推进，否则设计完还要返工」。
据此去产品仓核，**结果推翻了裁决方自己的两处判断**。

### O-4a ｜「抽象层 `resume` 是 `NotImplementedError`，故端到端未接线」——**错**

| 事实 | 取证 |
| --- | --- |
| `NotImplementedError` 在**抽象基类**里，是抽象方法的正确写法 | `graph_runtime_service.py:26-31` |
| **适配器已实现**：`Command(resume=user_input)` + 同 `thread_id` | `langgraph_runtime.py:13-21` |
| **有端到端测试且通过**：中断 → `resume("continue")` → 原地续跑并产生副作用 | `test_graph_runtime_service.py:18-35`，实跑 **2 passed** |

**怎么错的**：吸收 `C2` 时裁决方**确实打开了文件自验**——但只看了基类那 18 行就停了，
**没搜谁继承它、没搜谁调用它、没跑测试**。`cursor` 原话是
「适配器必须把恢复输入翻译成库的命令类型——这是**留给实现方的空位**」，
说的是**接口契约**，被读成了**功能缺失**。

⚠ **这是「打开文件自验」也会失败的一个实例**：验了，但**验的范围是自己划的**，
而范围划错了。与本轮其他几次同形态——**凭印象选检查点**。

### O-4b ｜「事务性 / 租约 / fencing 验不了」——**句子对，读法错**

原句「**git 载体**验不了这三样」成立。但整套文档只写了这一句，
读起来像「本项目至今没验过这三样」。实际产品仓（PostgreSQL 载体）**全有测试且全过**：

| 项 | 测试 |
| --- | --- |
| fencing | `test_expected_version_prevents_two_workers_claiming_same_run` |
| 租约 / 并发 | `test_relational_schema_enforces_identity_and_concurrency_constraints`、`test_same_thread_rejects_second_non_terminal_run` |
| 副作用恰好一次 | `test_interrupt_resume_executes_side_effect_once`、`test_retry_after_crash_after_commit_does_not_repeat_effect`、`test_tool_side_effect_service_records_once_by_tool_call_id` |
| 陈旧覆盖被拒 | `test_versioned_reducer_rejects_stale_plan_overwrite` |
| 取消先于副作用 | `test_cancelled_run_stops_before_side_effect_and_releases_thread` |
| 恢复用钉定图版本 | `test_waiting_checkpoint_resumes_with_pinned_graph_version` |

全套：**156 passed, 2 skipped, 5.13s**（跳过的两条是跨仓消费者向量，未被请求）。

**后果**：`agent-dev-guide` §6「拆脚手架的条件」把并发语义写成「验不了」，
读者会以为那是**未来才能做的事**。准确说法是：**服务态那一侧的判据已经有测试在跑**，
缺的是**手工态与服务态的等效比对**，不是「并发语义没人验」。

### 由此得到的一条方法教训

产品仓最后一次代码提交是 **08-31**，而这六天里我们在文档上给同一批问题标了 ⚠。
**156 个测试 5 秒跑完，答案一直在那里，没人去问。**

> **设计文档里每写一条「未验证 ⚠」，先问一句：产品仓里有没有测试已经回答了它。**
> 标 ⚠ 的成本是零，所以它会越积越多；而其中一部分其实是**已验证但没人去看**。

⚠ 本条与 `A-3`（`cursor` 提异议推翻裁决方）合并在 ④ 处置里一次改判，
**不在 ④ 进行中零敲碎打地改裁决稿**。

## O-5 ｜ 「组织者」：一个被判定为不需要、却仍在用、且五个词装不下的角色

所有者提出：「supervisor 是代码不是角色，现在看不对吧——否则谁来裁决，人行使权力也要靠他。」
并提醒 **supervisor 的定位变过多次**，要多查。查了，演化史如下。

### 演化史（四个阶段，逐条有据）

| 阶段 | 说法 | 出处 |
| --- | --- | --- |
| 1 | supervisor **一词三义**：调度监督器 / 执行监督 Agent / 产品侧拆子 Task；规定未加前缀者专指执行监督 Agent | `refact-fable.md:105-109` 转述现文《两层监督职责与等位原则》 |
| 2 | 发现**第四套**：`round-protocol` 的「**轮次组织者**」。评审指出它可读作第三套在 T2 里的别名，但**协议从未定义这个角色的职责边界，只借了名字** → 记为未决 **U2** | `refact-fable.md:105-109`、`rounds/refact/round.md:100` |
| 3 | **U2 被判定关闭**：「**不需要给『轮次组织者』定名**，需要的是用五个词替换掉全部 supervisor 及其别名」 | `refact-fable.md:568` |
| 4 | 五个词落地：router / orchestrator 是**代码**，arbiter / acceptor / approver 是**角色** | `agent-dev-refact.md` §2.10 |

### 所有者的记忆有一处偏差，但质疑落在真问题上

**偏差**：文档从未说「supervisor 是代码」。它说的是**解体成五个，其中只有两个是代码**。
「谁来裁决」有明确答案——**arbiter 是 agent 角色**。
「人行使权力靠谁」也有答案，而且**恰恰因为 orchestrator 是代码，人的权力才强制得住**：
代码走到权力表那条边会**停下等**，而角色是可以被说服的。**把 orchestrator 写成角色，等于让守门的人可以商量。**

**真问题**：阶段 3 判定「不需要第六个词」，但**替换没做完**——
协议正文里「组织者」仍出现 **17 次**，`agent-dev-refact` 里 1 次。
而本轮裁决方实际做的一批事，**五个词一个都装不下**：

- 决定把哪些错误记成观察、哪些不记；
- 决定环节通知里写什么、把哪三处列为「重点看」；
- 决定 `qwen` 该等还是该杀；
- 决定文档重构排到 ⑦ 之后。

这些**不是 arbiter 的活**（arbiter 只管定基座、吸收、处置异议），
**也不是 orchestrator 能做的**（那是判断，不是脚本）。

### ⚠ 更严重的一层：它与 arbiter 由同一方承担

§2.10 明写「**arbiter 不兼 orchestrator**——否则整合权与流程控制权合一，④ 异议那道制衡形同虚设」。
本轮这两件事**由同一个 opus 承担**。④ 确实制衡住了裁决方（三条改判全部推翻它），
**但那是因为四家尽责，不是因为机制拦住了它。**

### 待判（不预设，⑦ 之后处理）

两条路，**裁决方倾向前者但不自决**：

- **A**：承认第六个概念（组织者的裁量），给它定义与制衡，把「不需要定名」的判定推翻；
- **B**：坚持五个词够用，**把协议里那 17 处「组织者」逐一替换掉**，并说明上列四类判断各归谁。

⚠ 无论选哪条，**「替换没做完」这个事实本身要先修**——
现在的状态是「判定了 B，却按 A 在运行」，两头不靠。
