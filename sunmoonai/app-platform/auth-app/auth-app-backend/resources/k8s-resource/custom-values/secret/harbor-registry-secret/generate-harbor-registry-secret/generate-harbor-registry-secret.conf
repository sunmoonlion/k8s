# ============================================================================
# Harbor Registry Secret YAML 生成配置
# 职责：配�?YAML 生成相关的参数和数据内容
# ============================================================================

# 模板文件路径（相对于 resources/k8s-resource/�?# 生成脚本�?K8S_RESOURCE_DIR 已经指向 k8s-resource/，所以这里直接使用相对路
TEMPLATE_FILE="templates/secret/harbor-registry-secret.yaml"

# 输出文件名（生成在当前目录）
OUTPUT_FILE="harbor-registry-secret-generated.yaml"

# 是否启用生成
ENABLED="true"

# 资源类型
RESOURCE_TYPE="secret"

# ============================================================================
# 基础配置（用于生�?YAML�?# 注意：这些配置的优先级（从高到低）：
#   1. 环境变量（如果已设置�?#   2. 主应用的 deploy-auth-app-backend.conf（如果存在，由生成脚本自动读取）
#   3. 本文件中的默认值（作为后备�?# ============================================================================
# 命名空间和环境配
NAMESPACE="${NAMESPACE:-app-platform-dev}"          # 优先使用环境变量，其次主配置，最后默认
ENVIRONMENT="${ENVIRONMENT:-development}"           # 优先使用环境变量，其次主配置，最后默认
ENV="${ENV:-dev}"                                   # 优先使用环境变量，其次主配置，最后默认�?
# ============================================================================
# Harbor Secret 数据内容（用于生�?YAML�?# 注意：这些数据会被用于生�?Harbor Registry Secret YAML 文件
# ============================================================================
# Harbor Docker 认证配置
DOCKER_SERVER="${DOCKER_SERVER:-harbor.sunmoonai.com:30443}"  # Docker 服务器地址
DOCKER_USERNAME="${DOCKER_USERNAME:-admin}"                    # Docker 用户
DOCKER_PASSWORD="${DOCKER_PASSWORD:-Ha!b1359admin}"            # Docker 密码
