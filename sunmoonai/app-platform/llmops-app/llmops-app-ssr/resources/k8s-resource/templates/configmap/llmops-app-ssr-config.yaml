apiVersion: v1
kind: ConfigMap
metadata:
  name: ${LLMOPS_SSR_CONFIGMAP_NAME}
  namespace: ${NAMESPACE}
  labels:
    app: llmops-app-ssr
    component: configmap
    environment: ${ENVIRONMENT}
    managed-by: deploy-script
data:
  PROJECT_NAME: "${PROJECT_NAME}"
  SERVER_NAME: "${SERVER_NAME}"
  SERVER_HOST: "${SERVER_HOST}"
  SERVER_BOT: "${SERVER_BOT}"
  BACKEND_CORS_ORIGINS: "${BACKEND_CORS_ORIGINS}"
  POSTGRES_SERVER: "${POSTGRES_SERVER}"
  POSTGRES_PORT: "${POSTGRES_PORT}"
  POSTGRES_USER: "${POSTGRES_USER}"
  POSTGRES_DB: "${POSTGRES_DB}"
  NEO4J_SERVER: "${NEO4J_SERVER}"
  NEO4J_PORT: "${NEO4J_PORT}"
  NEO4J_USERNAME: "${NEO4J_USERNAME}"
  NEO4J_AUTH: "${NEO4J_AUTH}"
  NEO4J_BOLT: "${NEO4J_BOLT}"
  USERS_OPEN_REGISTRATION: "${USERS_OPEN_REGISTRATION}"
  NEO4J_FORCE_TIMEZONE: "${NEO4J_FORCE_TIMEZONE}"
  NEO4J_AUTO_INSTALL_LABELS: "${NEO4J_AUTO_INSTALL_LABELS}"
  NEO4J_MAX_CONNECTION_POOL_SIZE: "${NEO4J_MAX_CONNECTION_POOL_SIZE}"
  MULTI_MAX: "${MULTI_MAX}"
  EMAIL_RESET_TOKEN_EXPIRE_HOURS: "${EMAIL_RESET_TOKEN_EXPIRE_HOURS}"
  EMAIL_TEMPLATES_DIR: "${EMAIL_TEMPLATES_DIR}"
  EMAIL_TEST_USER: "${EMAIL_TEST_USER}"

  # ========================================================================
  # Nuxt SSR 运行配置（非敏感）
  # ========================================================================
  NODE_ENV: "${NODE_ENV:-production}"
  APP_ENV: "${APP_ENV:-production}"
  NITRO_HOST: "${NITRO_HOST:-0.0.0.0}"
  NITRO_PORT: "${NITRO_PORT:-3000}"
  NUXT_TELEMETRY_DISABLED: "${NUXT_TELEMETRY_DISABLED:-1}"

  # ========================================================================
  # 业务 BFF 配置（联动配置 - 调用 llmops-app-bff）
  # ========================================================================
  VUE_APP_BFF_URL: "${VUE_APP_BFF_URL:-http://llmops-app-bff:8000}"
  LLMOPS_BFF_URL: "${LLMOPS_BFF_URL:-http://llmops-app-bff:8000}"
  VUE_APP_API_VERSION: "${VUE_APP_API_VERSION:-v1}"

  # ========================================================================
  # Session 配置（联动配置 - 必须与 auth-app-bff 一致）
  # ========================================================================
  SESSION_COOKIE_NAME: "${SESSION_COOKIE_NAME:-sunmoonai_session}"
