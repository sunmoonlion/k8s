apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodebullworker-tools-web-backend
  namespace: app-platform-dev
  labels:
    app: nodebullworker-tools-web-backend
    component: node-bull-worker
    backend: tools-web-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodebullworker-tools-web-backend
  template:
    metadata:
      labels:
        app: nodebullworker-tools-web-backend
        component: node-bull-worker
        backend: tools-web-backend
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: nodebullworker
        image: harbor.sunmoonai.com:30443/app-images/tools-web-backend:1.0.0
        imagePullPolicy: Always
        command:
        - sh
        - -c
        - |
          set -eu
          export NODE_ENV="${NODE_ENV:-production}"
          export QUEUE_ON="${QUEUE_ON:-true}"
          exec node dist/worker
        envFrom:
        - configMapRef:
            name: tools-web-backend-config
        - secretRef:
            name: tools-web-backend-secret
        - configMapRef:
            name: nodebullworker-tools-web-backend-config
        - secretRef:
            name: nodebullworker-tools-web-backend-secret
        - secretRef:
            name: tools-web-backend-nodebull-redis-conn
        env:
        - name: QUEUE_WORKER_SERVICE
          value: "tools-web-backend"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
