apiVersion: apps/v1
kind: Deployment
metadata:
  name: incubator-app-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: incubator-app-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: incubator-app-backend
  template:
    metadata:
      labels:
        app: incubator-app-backend
    spec:
      imagePullSecrets:
      - name: ${INCUBATOR_BFF_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: incubator-app-backend
        # Harbor 镜像路径：仓库/项目/镜像名:标签
        # 由 deploy-incubator-backend.sh 计算并导出 INCUBATOR_BFF_FULL_IMAGE_NAME
        image: ${INCUBATOR_BFF_FULL_IMAGE_NAME}
        # Always: 总是从 Harbor 拉取最新版本，避免使用节点本地的旧镜像
        # 注意：如果使用 IfNotPresent，节点本地有旧镜像时不会从 Harbor 拉取，导致使用旧版本
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: incubator-app-backend-config
        - secretRef:
            name: incubator-app-backend-secret
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data-storage
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
          subPath: ${PVC_SUB_PATH:-}
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-incubator-app-backend-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: incubator-app-backend
  namespace: ${NAMESPACE:-app-platform-dev}
  labels:
    app: incubator-app-backend
spec:
  selector:
    app: incubator-app-backend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
