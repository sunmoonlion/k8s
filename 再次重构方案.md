# 再次重构方案：Kind 存储设计简化

> 更新时间：2026-04-14

---

## 一、现有设计的问题

| 问题 | 说明 |
|--|--|
| init/reuse 双模式 | NFS 时代的历史包袱，解决的是 UUID 路径记录问题，固定路径后该问题不存在 |
| 模式概念本身错误 | reuse 逻辑上要求先 init，但两者数据路径不通，实际上是两条死路 |
| `PERSIST_MODE` 开关 | 这个概念不应存在于 Kind 部署里，目录状态由文件系统决定，不由开关决定 |
| 远程集群保护逻辑 | 专门阻断"remote + reuse"的组合，这个保护本身说明设计有问题 |

---

## 二、重构后的设计

**核心原则：按环境分路，每条路只有一种做法，无开关**

```
Kind   → 静态 hostPath PV + existingClaim（dev-values-kind.yaml）
Remote → 动态 local-path SC（dev-values.yaml）
Prod   → fast-ssd SC（prod-values.yaml）
```

**Kind 部署流程（唯一，无分支）**

```
1. kubectl apply *-kind-pv-pvc.yaml   ← 幂等，已存在则跳过
2. helm upgrade --install -f dev-values-kind.yaml
```

目录为空 → 数据库自己初始化；目录有数据 → 数据库自己加载。K8s 层不感知，不需要开关。

**想清空数据**：手动 `rm -rf /data/kind-local-storage/<component>/*`，无需改部署方式。

---

## 三、受影响组件分组

经过对实际代码的梳理，受影响组件分为两组，工作量差别较大：

### 第一组：已有 PERSIST_MODE 逻辑（6 个）

> 这组已有静态 PV 文件和 dev-values-persist.yaml，只需重命名 + 删除 PERSIST_MODE 分支。

| 组件 | 平台 | 现有文件 |
|--|--|--|
| PostgreSQL | data-platform | `dev-values-persist.yaml` + `postgresql-dev-pv-pvc.yaml` |
| Redis | data-platform | `dev-values-persist.yaml` + `redis-dev-pv-pvc.yaml` |
| MongoDB | data-platform | `dev-values-persist.yaml` + `mongodb-dev-pv-pvc.yaml` |
| Neo4j | data-platform | `dev-values-persist.yaml` + `neo4j-dev-pv-pvc.yaml` |
| Elasticsearch | data-platform | `dev-values-persist.yaml` + `elasticsearch-dev-pv-pvc.yaml` |
| RabbitMQ | messaging-platform | `dev-values-persist.yaml` + `rabbitmq-dev-pv-pvc.yaml` |

### 第二组：当前无静态 PV 支持（4 个）

> 这组 Kind 和 Remote 共用同一个 dev-values.yaml（动态 local-path），从未有过静态 PV。
> 需要新建文件 + 修改部署脚本（加入 CLUSTER=kind 分支）。

| 组件 | 平台 | 当前状态 | WSL 数据目录 |
|--|--|--|--|
| Harbor | cicd-platform | 仅 `dev-values.yaml`，无 Kind 分支 | `/data/kind-local-storage/harbor` |
| Jenkins | cicd-platform | 仅 `dev-values.yaml`，无 Kind 分支 | `/data/kind-local-storage/jenkins` |
| Casdoor | app-platform | 仅 `dev-values.yaml`，无 Kind 分支 | `/data/kind-local-storage/casdoor` |
| pgAdmin | ops-platform | 仅 `dev-values.yaml`，无 Kind 分支 | `/data/kind-local-storage/pgadmin` |

---

## 四、文件变化

### 4.1 重命名（第一组，各 6 个）

| 旧文件名 | 新文件名 |
|--|--|
| `dev-values-persist.yaml` | `dev-values-kind.yaml` |
| `postgresql-dev-pv-pvc.yaml` | `postgresql-kind-pv-pvc.yaml` |
| `redis-dev-pv-pvc.yaml` | `redis-kind-pv-pvc.yaml` |
| `mongodb-dev-pv-pvc.yaml` | `mongodb-kind-pv-pvc.yaml` |
| `neo4j-dev-pv-pvc.yaml` | `neo4j-kind-pv-pvc.yaml` |
| `elasticsearch-dev-pv-pvc.yaml` | `elasticsearch-kind-pv-pvc.yaml` |
| `rabbitmq-dev-pv-pvc.yaml` | `rabbitmq-kind-pv-pvc.yaml` |

### 4.2 新增（第二组，各 2 个文件）

| 组件 | 需新增文件 |
|--|--|
| Harbor | `harbor-kind-pv-pvc.yaml` + `dev-values-kind.yaml` |
| Jenkins | `jenkins-kind-pv-pvc.yaml` + `dev-values-kind.yaml` |
| Casdoor | `casdoor-kind-pv-pvc.yaml` + `dev-values-kind.yaml` |
| pgAdmin | `pgadmin-kind-pv-pvc.yaml` + `dev-values-kind.yaml` |

### 4.3 其他文件

| 文件 | 变化 | 原因 |
|--|--|--|
| `dev-values.yaml`（所有组件）| 保留，不改 | 仅供 Remote 使用 |
| `prod-values.yaml`（所有组件）| 不动 | 无关 |
| `kind-cluster.yaml` | extraMounts 从 control-plane 移到第一个 worker | 有状态组件应跑在 worker |

### 4.4 nodeAffinity 统一改为 `kind-worker`

Kind 第一个 worker 节点名为 `kind-worker`（第二个为 `kind-worker2`）。

所有 `*-kind-pv-pvc.yaml` 中：
```yaml
# 改前
values: [kind-control-plane]
# 改后
values: [kind-worker]
```

`kind-cluster.yaml` 中：
```yaml
# 改前：extraMounts 在 control-plane
- role: control-plane
  extraMounts:
    - hostPath: /data/kind-local-storage
      containerPath: /data/kind-local-storage

# 改后：extraMounts 移到第一个 worker
- role: control-plane
  # 无 extraMounts
- role: worker
  extraMounts:
    - hostPath: /data/kind-local-storage
      containerPath: /data/kind-local-storage
- role: worker
  # 无 extraMounts
```

---

## 五、脚本变化

### 5.1 第一组（6 个脚本）：删除 PERSIST_MODE 逻辑

**删除以下内容**：
- `PERSIST_MODE` / `SUNMOONAI_GLOBAL_PERSIST_MODE` / `<COMPONENT>_PERSIST_MODE` 环境变量及读取逻辑
- init/reuse 分支判断
- 远程集群保护逻辑（`CLUSTER != kind → 强制 init`）

**简化后**：
```bash
if [[ "$CLUSTER" == "kind" ]]; then
    kubectl apply -f *-kind-pv-pvc.yaml
    helm upgrade --install ... -f dev-values-kind.yaml
else
    helm upgrade --install ... -f dev-values.yaml
fi
```

### 5.2 第二组（4 个脚本）：新增 Kind 分支

这 4 个脚本当前对 Kind/Remote 无区分，需要加入 CLUSTER 判断：

```bash
# 改前（harbor/jenkins/casdoor/pgadmin 当前逻辑）
helm upgrade --install ... -f dev-values.yaml

# 改后
if [[ "$CLUSTER" == "kind" ]]; then
    kubectl apply -f *-kind-pv-pvc.yaml
    helm upgrade --install ... -f dev-values-kind.yaml
else
    helm upgrade --install ... -f dev-values.yaml
fi
```

---

## 六、WSL 目录初始化

```bash
sudo mkdir -p /data/kind-local-storage/{postgresql,redis,mongodb,neo4j,elasticsearch,rabbitmq,harbor,jenkins,casdoor,pgadmin}
sudo chmod -R 777 /data/kind-local-storage
```

---

## 七、三层环境对比（重构后）

| | Kind | Remote | Prod |
|--|--|--|--|
| 供应方式 | 静态 hostPath PV | 动态 local-path | 动态 fast-ssd |
| values 文件 | `dev-values-kind.yaml` | `dev-values.yaml` | `prod-values.yaml` |
| PV/PVC 文件 | `*-kind-pv-pvc.yaml` | 无（动态创建）| 无（动态创建）|
| 集群重建后数据 | 保留（WSL 固定路径）| 丢失（动态 UUID）| 不涉及 |
| 节点亲和性 | 手动写死 kind-worker | provisioner 调度后自动写 | 无要求 |

---

## 八、实施步骤

### 第一组（6 个组件，工作量小）
1. 重命名 `dev-values-persist.yaml` → `dev-values-kind.yaml`
2. 重命名 `*-dev-pv-pvc.yaml` → `*-kind-pv-pvc.yaml`
3. 更新 `*-kind-pv-pvc.yaml` 中 nodeAffinity：`kind-control-plane` → `kind-worker`
4. 简化部署脚本：删除 PERSIST_MODE 逻辑，改为 CLUSTER=kind 分支
5. grep 确认无残留引用（`dev-pv-pvc`、`dev-values-persist`、`PERSIST_MODE`）

### 第二组（4 个组件，工作量较大）
6. 新建 `dev-values-kind.yaml`（基于 `dev-values.yaml`，persistence 改用 existingClaim）
7. 新建 `*-kind-pv-pvc.yaml`（静态 hostPath PV，nodeAffinity → kind-worker）
8. 修改部署脚本：加入 CLUSTER=kind 分支

### 公共
9. 更新 `kind-cluster.yaml`：extraMounts 从 control-plane 移到第一个 worker
10. 更新 WSL 目录初始化命令（补充 harbor、jenkins、casdoor、pgadmin）
11. **验证**：Kind 部署一遍，确认 PV 绑定，数据写入 `/data/kind-local-storage/`
12. **验证重建**：`kind delete cluster` + 重建 + 重新部署，确认数据恢复
