apiVersion: v1
kind: Secret
metadata:
  name: redis-auth-secret
  namespace: data-platform-dev
type: Opaque
stringData:
  # Redis 密码键（Bitnami Redis 期望短横线键）
  redis-password: "CHANGE_ME_STRONG_REDIS_PASSWORD"
---
# 仅归档/占位用途：实际由统一密管创建与更新；本文件用于结构对齐

