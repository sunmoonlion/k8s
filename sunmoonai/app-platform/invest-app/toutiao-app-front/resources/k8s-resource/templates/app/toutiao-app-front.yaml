apiVersion: apps/v1
kind: Deployment
metadata:
  name: toutiao-app-front
  namespace: ${NAMESPACE:-invest-app-dev}
  labels:
    app: toutiao-app-front
spec:
  replicas: 1
  selector:
    matchLabels:
      app: toutiao-app-front
  template:
    metadata:
      labels:
        app: toutiao-app-front
    spec:
      imagePullSecrets:
      - name: ${TOUTIAO_FRONT_IMAGE_PULL_SECRET_NAME:-harbor-registry-secret}
      containers:
      - name: toutiao-app-front
        image: ${TOUTIAO_FRONT_FULL_IMAGE_NAME}
        imagePullPolicy: ${IMAGE_PULL_POLICY:-Always}
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: ${TOUTIAO_FRONT_CONFIGMAP_NAME:-toutiao-app-front-config}
        - secretRef:
            name: ${TOUTIAO_FRONT_SECRET_NAME:-toutiao-app-front-secret}
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data-storage
          mountPath: ${PVC_MOUNT_PATH:-/app/data}
          subPath: ${PVC_SUB_PATH:-}
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ${PVC_NAME:-toutiao-app-front-pvc}
---
apiVersion: v1
kind: Service
metadata:
  name: toutiao-app-front
  namespace: ${NAMESPACE:-invest-app-dev}
  labels:
    app: toutiao-app-front
spec:
  selector:
    app: toutiao-app-front
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
